window.TWO_POINTERS_DATA = [
  {
    id: 10,
    name: "Valid Palindrome",
    category: "Two Pointers",
    categoryId: "two-pointers",
    difficulty: "Easy",
    explanation: "<p><strong>Problem:</strong> A phrase is a <strong>palindrome</strong> if, after converting all uppercase letters to lowercase and removing all non-alphanumeric characters, it reads the same forward and backward.</p><p>Given a string <code>s</code>, return <code>true</code> if it is a palindrome, or <code>false</code> otherwise.</p><p><strong>Examples:</strong></p><ul><li><code>\"A man, a plan, a canal: Panama\"</code> → <code>true</code> (\"amanaplanacanalpanama\")</li><li><code>\"race a car\"</code> → <code>false</code> (\"raceacar\")</li><li><code>\" \"</code> → <code>true</code> (empty string after cleaning is a palindrome)</li></ul><p><strong>Key insight:</strong> Use two pointers — one from the left, one from the right. Skip non-alphanumeric characters, compare letters in a case-insensitive manner.</p>",
    bruteForce: "<p><strong>Brute Force — Clean then Compare O(n)</strong></p><ol><li>Filter the string to only alphanumeric characters and convert to lowercase.</li><li>Check if the cleaned string equals its reverse.</li></ol><pre><code>String cleaned = s.toLowerCase().replaceAll(\"[^a-z0-9]\", \"\");\nreturn cleaned.equals(new StringBuilder(cleaned).reverse().toString());</code></pre><p><strong>Time:</strong> O(n) — but creates extra strings (3 extra strings: cleaned, reversed, compared).</p><p><strong>Space:</strong> O(n) — the cleaned string.</p>",
    bruteForceInefficiency: "<p><strong>Why the cleaning approach uses extra space:</strong></p><ul><li>Creating a new cleaned string requires O(n) extra memory.</li><li>Reversing it creates another O(n) string.</li><li>We can avoid both allocations by using <strong>two pointers in-place</strong> on the original string.</li><li>The two-pointer approach skips non-alphanumeric characters on the fly — no new string needed.</li></ul>",
    optimalApproach: "<p><strong>Optimal — Two Pointers O(n) time, O(1) space</strong></p><p><strong>Pattern:</strong> Two pointers from both ends, skip invalid chars</p><ol><li>Initialize <code>l = 0</code>, <code>r = s.length() - 1</code>.</li><li>While <code>l &lt; r</code>:<ul><li>Advance <code>l</code> while <code>!Character.isLetterOrDigit(s.charAt(l))</code> and <code>l &lt; r</code>.</li><li>Retreat <code>r</code> while <code>!Character.isLetterOrDigit(s.charAt(r))</code> and <code>l &lt; r</code>.</li><li>Compare <code>Character.toLowerCase(s.charAt(l))</code> vs <code>Character.toLowerCase(s.charAt(r))</code>.</li><li>If not equal → return <code>false</code>.</li><li>Move both: <code>l++; r--;</code></li></ul></li><li>Return <code>true</code>.</li></ol><p><strong>Time:</strong> O(n) — each character visited at most once.</p><p><strong>Space:</strong> O(1) — only two pointer variables.</p>",
    javaCode: "class Solution {\n    public boolean isPalindrome(String s) {\n        int l = 0;              // left pointer starts at beginning\n        int r = s.length() - 1; // right pointer starts at end\n        \n        while (l < r) { // continue until pointers meet\n            // skip non-alphanumeric characters from the left\n            while (l < r && !Character.isLetterOrDigit(s.charAt(l))) {\n                l++; // move left pointer right past non-alphanumeric\n            }\n            // skip non-alphanumeric characters from the right\n            while (l < r && !Character.isLetterOrDigit(s.charAt(r))) {\n                r--; // move right pointer left past non-alphanumeric\n            }\n            // compare characters (case-insensitive)\n            if (Character.toLowerCase(s.charAt(l)) != Character.toLowerCase(s.charAt(r))) {\n                return false; // mismatch -> not a palindrome\n            }\n            l++; // move left pointer inward\n            r--; // move right pointer inward\n        }\n        \n        return true; // all characters matched, it's a palindrome\n    }\n}",
    dryRun: "<p><strong>Input:</strong> <code>\"A man, a plan, a canal: Panama\"</code></p><table border='1' cellpadding='6' style='border-collapse:collapse'><tr><th>l</th><th>r</th><th>s[l] (cleaned)</th><th>s[r] (cleaned)</th><th>match?</th></tr><tr><td>0</td><td>29</td><td>'A'→'a'</td><td>'a'</td><td>✓</td></tr><tr><td>1 (skip ' ')</td><td>28 (skip ' ')</td><td>'m'</td><td>'m'</td><td>✓</td></tr><tr><td>2</td><td>27 (skip ':')</td><td>'a'</td><td>'a'</td><td>✓</td></tr><tr><td>...</td><td>...</td><td>...</td><td>...</td><td>✓</td></tr></table><p>All matching → <strong>return <code>true</code></strong> ✓</p><p><strong>Input:</strong> <code>\"race a car\"</code></p><p>After skipping spaces: compare 'r' vs 'r' ✓, 'a' vs 'a' ✓, 'c' vs 'c' ✓, 'e' vs 'a' ✗ → <strong>return <code>false</code></strong> ✓</p>",
    interviewTips: "<p><strong>Interview Script:</strong></p><ol><li><strong>Clarify:</strong> \"Alphanumeric only? Case-insensitive? What about empty string after cleaning?\" (empty string is palindrome)</li><li><strong>Simple approach:</strong> \"I could clean the string and compare to its reverse — O(n) time but O(n) space.\"</li><li><strong>Better:</strong> \"Two pointers on the original string — skip non-alphanumeric in-place, O(1) extra space.\"</li><li><strong>Key methods:</strong> <code>Character.isLetterOrDigit()</code> and <code>Character.toLowerCase()</code>.</li></ol><p><strong>Common mistake:</strong> Forgetting to check <code>l &lt; r</code> inside the inner while loops — otherwise pointers can cross.</p>",
    importantPoints: "<ul><li><strong>Always check <code>l &lt; r</code></strong> in the inner skip loops — prevents the pointers from crossing and causing incorrect comparisons.</li><li><code>Character.isLetterOrDigit(c)</code> checks for both letters and digits in one call.</li><li><code>Character.toLowerCase(c)</code> handles case-insensitive comparison.</li><li><strong>Edge cases:</strong> Single character → always palindrome. All non-alphanumeric → empty string → palindrome.</li><li><strong>Two pointers pattern</strong> is key here — remember: move BOTH inward after a successful comparison.</li></ul>"
  },
  {
    id: 11,
    name: "Two Sum II - Input Array Is Sorted",
    category: "Two Pointers",
    categoryId: "two-pointers",
    difficulty: "Medium",
    explanation: "<p><strong>Problem:</strong> Given a <strong>1-indexed</strong> sorted array of integers <code>numbers</code>, find two numbers that add up to <code>target</code>. Return their indices as <code>[index1, index2]</code> where <code>1 ≤ index1 &lt; index2 ≤ numbers.length</code>.</p><p>Must use <strong>O(1) extra space</strong>.</p><p><strong>Examples:</strong></p><ul><li>Input: <code>numbers=[2,7,11,15], target=9</code> → Output: <code>[1,2]</code> (2+7=9)</li><li>Input: <code>numbers=[2,3,4], target=6</code> → Output: <code>[1,3]</code> (2+4=6)</li><li>Input: <code>numbers=[-1,0], target=-1</code> → Output: <code>[1,2]</code></li></ul><p><strong>Key insight:</strong> Because the array is sorted, we can use two pointers. If sum is too small, move left pointer right (increase sum). If sum is too large, move right pointer left (decrease sum).</p>",
    bruteForce: "<p><strong>Brute Force — Nested Loops O(n²)</strong></p><p>Try all pairs until we find one summing to target:</p><pre><code>for i from 0 to n-1:\n  for j from i+1 to n-1:\n    if numbers[i] + numbers[j] == target:\n      return [i+1, j+1]  // 1-indexed</code></pre><p><strong>Time:</strong> O(n²) — doesn't exploit the sorted property.</p><p><strong>Space:</strong> O(1).</p>",
    bruteForceInefficiency: "<p><strong>Why we should exploit the sorted order:</strong></p><ul><li>Sorting gives us a <em>monotonic</em> property: <code>numbers[i] + numbers[j]</code> increases when i increases, and decreases when j decreases.</li><li>If current sum is too small, we <em>know</em> we need a larger value — move left pointer right (only useful increments).</li><li>If current sum is too large, we <em>know</em> we need a smaller value — move right pointer left.</li><li>This eliminates half the search space in each step, giving O(n) total instead of O(n²).</li></ul>",
    optimalApproach: "<p><strong>Optimal — Two Pointers O(n) time, O(1) space</strong></p><p><strong>Pattern:</strong> Two pointers converging from both ends on a sorted array</p><ol><li>Initialize <code>l = 0</code>, <code>r = numbers.length - 1</code>.</li><li>While <code>l &lt; r</code>:<ul><li>Compute <code>sum = numbers[l] + numbers[r]</code>.</li><li>If <code>sum == target</code> → return <code>[l+1, r+1]</code> (1-indexed).</li><li>If <code>sum &lt; target</code> → <code>l++</code> (need larger left value).</li><li>If <code>sum &gt; target</code> → <code>r--</code> (need smaller right value).</li></ul></li></ol><p><strong>Correctness:</strong> The problem guarantees exactly one solution. The two-pointer approach never misses it — if the pair exists, both pointers converge to it.</p><p><strong>Time:</strong> O(n) — each pointer moves at most n steps total.</p><p><strong>Space:</strong> O(1) — just two variables.</p>",
    javaCode: "class Solution {\n    public int[] twoSum(int[] numbers, int target) {\n        int l = 0;                    // left pointer at start\n        int r = numbers.length - 1;   // right pointer at end\n        \n        while (l < r) { // continue while pointers haven't crossed\n            int sum = numbers[l] + numbers[r]; // compute current pair sum\n            \n            if (sum == target) {\n                // found the pair! return 1-indexed positions\n                return new int[]{l + 1, r + 1};\n            } else if (sum < target) {\n                // sum is too small: move left pointer right to increase sum\n                // (array is sorted, so numbers[l+1] > numbers[l])\n                l++;\n            } else {\n                // sum is too large: move right pointer left to decrease sum\n                // (array is sorted, so numbers[r-1] < numbers[r])\n                r--;\n            }\n        }\n        \n        // problem guarantees exactly one solution, so we always return above\n        return new int[]{};\n    }\n}",
    dryRun: "<p><strong>Input:</strong> <code>numbers=[2,7,11,15], target=9</code></p><table border='1' cellpadding='6' style='border-collapse:collapse'><tr><th>l</th><th>r</th><th>numbers[l]</th><th>numbers[r]</th><th>sum</th><th>action</th></tr><tr><td>0</td><td>3</td><td>2</td><td>15</td><td>17</td><td>17&gt;9, r-- → r=2</td></tr><tr><td>0</td><td>2</td><td>2</td><td>11</td><td>13</td><td>13&gt;9, r-- → r=1</td></tr><tr><td>0</td><td>1</td><td>2</td><td>7</td><td>9</td><td>== target! return [1,2]</td></tr></table><p><strong>Output:</strong> <code>[1, 2]</code> ✓</p><p><strong>Input:</strong> <code>numbers=[2,3,4], target=6</code></p><table border='1' cellpadding='6' style='border-collapse:collapse'><tr><th>l</th><th>r</th><th>sum</th><th>action</th></tr><tr><td>0</td><td>2</td><td>6</td><td>== target! return [1,3]</td></tr></table><p><strong>Output:</strong> <code>[1, 3]</code> ✓</p>",
    interviewTips: "<p><strong>Interview Script:</strong></p><ol><li><strong>Note the sorted property:</strong> \"Since the array is sorted, I can use two pointers — this is more efficient than a HashMap because it's O(1) space.\"</li><li><strong>Logic:</strong> \"If sum is too small, the left pointer needs a bigger value — increment it. If sum is too large, the right pointer needs a smaller value — decrement it.\"</li><li><strong>1-indexed output:</strong> \"The problem is 1-indexed, so I return l+1 and r+1.\"</li><li><strong>Compare with Two Sum I:</strong> \"In unsorted Two Sum, I'd use a HashMap — O(n) space. Here, sorted array enables O(1) space with two pointers.\"</li></ol>",
    importantPoints: "<ul><li><strong>1-indexed output</strong> — a common trap. Return <code>l+1</code> and <code>r+1</code>, not <code>l</code> and <code>r</code>.</li><li><strong>Sorted array = two pointers opportunity</strong> — whenever you see a sorted array and need pairs, think two pointers.</li><li><strong>Why it works:</strong> Each step we eliminate at least one element. When sum is too small, we can never pair numbers[l] with any right-side element smaller than numbers[r] to get target — so numbers[l] is eliminated.</li><li><strong>Contrast with Two Sum I:</strong> HashMap for unsorted O(n) space; two pointers for sorted O(1) space.</li></ul>"
  },
  {
    id: 12,
    name: "3Sum",
    category: "Two Pointers",
    categoryId: "two-pointers",
    difficulty: "Medium",
    explanation: "<p><strong>Problem:</strong> Given an integer array <code>nums</code>, return all triplets <code>[nums[i], nums[j], nums[k]]</code> such that <code>i != j != k</code> and <code>nums[i] + nums[j] + nums[k] == 0</code>. The solution set must not contain duplicate triplets.</p><p><strong>Examples:</strong></p><ul><li>Input: <code>[-1,0,1,2,-1,-4]</code> → Output: <code>[[-1,-1,2],[-1,0,1]]</code></li><li>Input: <code>[0,1,1]</code> → Output: <code>[]</code></li><li>Input: <code>[0,0,0]</code> → Output: <code>[[0,0,0]]</code></li></ul><p><strong>Key insight:</strong> Sort the array. Fix one element (<code>nums[i]</code>), then use two pointers on the remaining subarray to find pairs summing to <code>-nums[i]</code>. Skip duplicates carefully.</p>",
    bruteForce: "<p><strong>Brute Force — Three Nested Loops O(n³)</strong></p><p>Try all triplets:</p><pre><code>for i in range(n):\n  for j in range(i+1, n):\n    for k in range(j+1, n):\n      if nums[i]+nums[j]+nums[k]==0:\n        add triplet (deduplicate)</code></pre><p><strong>Time:</strong> O(n³) — for n=1000, that's a billion operations.</p><p><strong>Space:</strong> O(n) for the result + deduplication set.</p>",
    bruteForceInefficiency: "<p><strong>Why O(n³) is too slow, and the optimization:</strong></p><ul><li>The innermost loop is doing Two Sum for each pair (i,j) — we can replace it with Two Pointers after sorting.</li><li><strong>Reduce to Two Sum:</strong> Sort the array. For each fixed index <code>i</code>, find pairs in <code>nums[i+1..n-1]</code> that sum to <code>-nums[i]</code> using two pointers — O(n) per i.</li><li>Total: O(n) choices for i × O(n) two-pointer = O(n²).</li><li><strong>Deduplication:</strong> Sorting groups duplicates together — easily skip them by checking if the current element equals the previous one.</li></ul>",
    optimalApproach: "<p><strong>Optimal — Sort + Two Pointers O(n²) time, O(1) extra space</strong></p><p><strong>Pattern:</strong> Fix one element, two-pointer for the rest</p><ol><li>Sort <code>nums</code>.</li><li>For each index <code>i</code> from 0 to n-3:<ul><li>Skip duplicate: if <code>i &gt; 0 &amp;&amp; nums[i] == nums[i-1]</code>, continue.</li><li>Early exit: if <code>nums[i] &gt; 0</code>, break (no three positives sum to 0).</li><li>Use two pointers: <code>l = i+1</code>, <code>r = n-1</code>, find pairs summing to <code>-nums[i]</code>.</li><li>When found: add triplet, then skip duplicate left elements and right elements before moving pointers.</li></ul></li></ol><p><strong>Time:</strong> O(n log n) sort + O(n²) = O(n²).</p><p><strong>Space:</strong> O(1) extra (output doesn't count).</p>",
    javaCode: "import java.util.*; // import Arrays, List, ArrayList\n\nclass Solution {\n    public List<List<Integer>> threeSum(int[] nums) {\n        Arrays.sort(nums); // sort to enable two-pointer and deduplication\n        List<List<Integer>> res = new ArrayList<>(); // result list\n        \n        // fix the first element of the triplet\n        for (int i = 0; i < nums.length - 2; i++) {\n            // skip duplicate values for the first element\n            // e.g., [-1,-1,0,1] -> skip second -1 at i=1\n            if (i > 0 && nums[i] == nums[i - 1]) continue;\n            \n            // optimization: if smallest element is positive, no zero sum possible\n            if (nums[i] > 0) break;\n            \n            int l = i + 1;           // left pointer (just after i)\n            int r = nums.length - 1; // right pointer (at end)\n            \n            while (l < r) {\n                int sum = nums[i] + nums[l] + nums[r]; // compute triplet sum\n                \n                if (sum == 0) {\n                    // found a valid triplet\n                    res.add(Arrays.asList(nums[i], nums[l], nums[r]));\n                    \n                    // skip duplicates from the left side\n                    while (l < r && nums[l] == nums[l + 1]) l++;\n                    // skip duplicates from the right side\n                    while (l < r && nums[r] == nums[r - 1]) r--;\n                    \n                    l++; // move past the duplicate-skipped position\n                    r--; // move past the duplicate-skipped position\n                } else if (sum < 0) {\n                    l++; // sum too small, increase by moving left pointer right\n                } else {\n                    r--; // sum too large, decrease by moving right pointer left\n                }\n            }\n        }\n        \n        return res; // return all unique triplets\n    }\n}",
    dryRun: "<p><strong>Input:</strong> <code>[-1, 0, 1, 2, -1, -4]</code></p><p><strong>After sort:</strong> <code>[-4, -1, -1, 0, 1, 2]</code></p><table border='1' cellpadding='6' style='border-collapse:collapse'><tr><th>i</th><th>nums[i]</th><th>l,r</th><th>sum</th><th>action</th></tr><tr><td>0</td><td>-4</td><td>1,5</td><td>-4+-1+2=-3</td><td>l++</td></tr><tr><td>0</td><td>-4</td><td>2,5</td><td>-4+-1+2=-3</td><td>l++</td></tr><tr><td>0</td><td>-4</td><td>3,5</td><td>-4+0+2=-2</td><td>l++</td></tr><tr><td>0</td><td>-4</td><td>4,5</td><td>-4+1+2=-1</td><td>l++ (l=r stop)</td></tr><tr><td>1</td><td>-1</td><td>2,5</td><td>-1+-1+2=0</td><td>add [-1,-1,2], skip dups, l=3,r=4</td></tr><tr><td>1</td><td>-1</td><td>3,4</td><td>-1+0+1=0</td><td>add [-1,0,1], l=4,r=3 stop</td></tr><tr><td>2</td><td>-1</td><td>—</td><td>—</td><td>skip (dup of nums[1])</td></tr><tr><td>3</td><td>0</td><td>4,5</td><td>0+1+2=3</td><td>r--</td></tr><tr><td>3</td><td>0</td><td>4,4</td><td>—</td><td>l==r, stop</td></tr></table><p><strong>Output:</strong> <code>[[-1,-1,2], [-1,0,1]]</code> ✓</p>",
    interviewTips: "<p><strong>Interview Script:</strong></p><ol><li><strong>Reduce to Two Sum:</strong> \"Fix one element, then solve Two Sum on the remaining sorted subarray with two pointers.\"</li><li><strong>Deduplication:</strong> \"Sort first, then skip consecutive duplicates at each pointer position after finding a valid triplet.\"</li><li><strong>Complexity:</strong> \"O(n²) time, O(1) extra space.\"</li></ol><p><strong>Tricky parts to explain:</strong></p><ul><li>Why <code>i &gt; 0 &amp;&amp; nums[i] == nums[i-1]</code>? — The first time we see a value is fine; we skip it on repeats.</li><li>Why skip duplicates AFTER finding a triplet? — We advance past all copies of nums[l] and nums[r] before incrementing, so we don't add the same triplet twice.</li></ul>",
    importantPoints: "<ul><li><strong>Sort first</strong> — enables two pointers AND makes deduplication easy (duplicates are adjacent).</li><li><strong>Deduplication at i level:</strong> <code>if (i &gt; 0 &amp;&amp; nums[i] == nums[i-1]) continue</code> — skip after the first occurrence.</li><li><strong>Deduplication at l,r level:</strong> After finding a triplet, advance past all duplicate values for l and r before incrementing.</li><li><strong>Early exit:</strong> If <code>nums[i] &gt; 0</code>, all remaining values are positive — can't sum to 0.</li><li>This is one of the most commonly asked medium problems. Practice until the deduplication logic is automatic.</li></ul>"
  },
  {
    id: 13,
    name: "Container With Most Water",
    category: "Two Pointers",
    categoryId: "two-pointers",
    difficulty: "Medium",
    explanation: "<p><strong>Problem:</strong> Given <code>n</code> non-negative integers <code>height[0..n-1]</code> representing vertical lines, find the two lines that together with the x-axis form a container that holds the most water.</p><p>The container's water area = <code>min(height[l], height[r]) × (r - l)</code>.</p><p><strong>Examples:</strong></p><ul><li>Input: <code>[1,8,6,2,5,4,8,3,7]</code> → Output: <code>49</code> (lines at index 1 and 8: min(8,7)×7=49)</li><li>Input: <code>[1,1]</code> → Output: <code>1</code></li></ul><p><strong>Key insight:</strong> Start with widest container (pointers at both ends). To potentially improve, we must increase the height — move the shorter line inward.</p>",
    bruteForce: "<p><strong>Brute Force — All Pairs O(n²)</strong></p><p>Try every pair of lines:</p><pre><code>for i in range(n):\n  for j in range(i+1, n):\n    water = min(height[i], height[j]) * (j - i)\n    maxWater = max(maxWater, water)</code></pre><p><strong>Time:</strong> O(n²) — all pairs.</p><p><strong>Space:</strong> O(1).</p>",
    bruteForceInefficiency: "<p><strong>Why we can do better:</strong></p><ul><li>The area = min(height[l], height[r]) × width. The width decreases as we move pointers inward.</li><li>If we move the <em>taller</em> pointer inward, the min (which equals the shorter height) stays the same or gets worse, and the width decreases — so area can only decrease or stay same.</li><li>Therefore, we should always move the <em>shorter</em> pointer inward — it's the only move that might increase the area.</li><li>This greedy insight lets us scan with two pointers in O(n) rather than trying all O(n²) pairs.</li></ul>",
    optimalApproach: "<p><strong>Optimal — Two Pointers O(n) time, O(1) space</strong></p><p><strong>Pattern:</strong> Greedy two pointers — always move shorter line</p><ol><li>Initialize <code>l = 0</code>, <code>r = height.length - 1</code>, <code>maxWater = 0</code>.</li><li>While <code>l &lt; r</code>:<ul><li>Compute <code>water = Math.min(height[l], height[r]) * (r - l)</code>.</li><li>Update <code>maxWater = Math.max(maxWater, water)</code>.</li><li>Move the shorter line inward: if <code>height[l] &lt; height[r]</code> → <code>l++</code>, else → <code>r--</code>.</li></ul></li><li>Return <code>maxWater</code>.</li></ol><p><strong>Why greedy works:</strong> Moving the shorter line is the only way to possibly find a taller min-height. Moving the taller line always gives a worse or equal result (min stays at short, width decreases).</p><p><strong>Time:</strong> O(n). <strong>Space:</strong> O(1).</p>",
    javaCode: "class Solution {\n    public int maxArea(int[] height) {\n        int l = 0;                    // left pointer at the first line\n        int r = height.length - 1;    // right pointer at the last line\n        int maxWater = 0;             // track maximum water found\n        \n        while (l < r) { // continue until pointers meet\n            // water = height of shorter line * width between lines\n            int water = Math.min(height[l], height[r]) * (r - l);\n            \n            // update maximum water seen so far\n            maxWater = Math.max(maxWater, water);\n            \n            // move the shorter line inward: it's the only way to potentially find more water\n            // moving the taller line would keep min the same but decrease width -> worse\n            if (height[l] < height[r]) {\n                l++; // left line is shorter, move it inward\n            } else {\n                r--; // right line is shorter (or equal), move it inward\n            }\n        }\n        \n        return maxWater; // return the maximum water found\n    }\n}",
    dryRun: "<p><strong>Input:</strong> <code>[1, 8, 6, 2, 5, 4, 8, 3, 7]</code></p><table border='1' cellpadding='6' style='border-collapse:collapse'><tr><th>l</th><th>r</th><th>h[l]</th><th>h[r]</th><th>water</th><th>maxWater</th><th>move</th></tr><tr><td>0</td><td>8</td><td>1</td><td>7</td><td>min(1,7)*8=8</td><td>8</td><td>l++ (1&lt;7)</td></tr><tr><td>1</td><td>8</td><td>8</td><td>7</td><td>min(8,7)*7=49</td><td>49</td><td>r-- (8&gt;7)</td></tr><tr><td>1</td><td>7</td><td>8</td><td>3</td><td>min(8,3)*6=18</td><td>49</td><td>r-- (8&gt;3)</td></tr><tr><td>1</td><td>6</td><td>8</td><td>8</td><td>min(8,8)*5=40</td><td>49</td><td>r-- (equal)</td></tr><tr><td>1</td><td>5</td><td>8</td><td>4</td><td>min(8,4)*4=16</td><td>49</td><td>r--</td></tr><tr><td>1</td><td>4</td><td>8</td><td>5</td><td>min(8,5)*3=15</td><td>49</td><td>r--</td></tr><tr><td>1</td><td>3</td><td>8</td><td>2</td><td>min(8,2)*2=4</td><td>49</td><td>r--</td></tr><tr><td>1</td><td>2</td><td>8</td><td>6</td><td>min(8,6)*1=6</td><td>49</td><td>r-- (l+1=r, stop)</td></tr></table><p><strong>Output:</strong> <code>49</code> ✓</p>",
    interviewTips: "<p><strong>Interview Script:</strong></p><ol><li><strong>Formula:</strong> \"Water = min(height[l], height[r]) × (r - l).\"</li><li><strong>Brute force:</strong> \"Check all O(n²) pairs.\"</li><li><strong>Greedy insight:</strong> \"Start with widest container. When we move a pointer, width decreases. To compensate, we need a taller line. The shorter line is our bottleneck — moving it gives the only chance of improvement.\"</li><li><strong>Proof sketch:</strong> \"If we move the taller line: min stays = shorter height, width decreases → area can only decrease. So moving taller is always suboptimal.\"</li></ol>",
    importantPoints: "<ul><li><strong>Area formula:</strong> <code>min(h[l], h[r]) × (r - l)</code> — the min is the bottleneck (water spills over the shorter wall).</li><li><strong>Always move shorter pointer:</strong> Moving the taller one can never improve the result.</li><li><strong>Handle tie:</strong> If heights are equal, it doesn't matter which pointer you move — moving either is fine.</li><li>This greedy approach is non-obvious — practice explaining WHY moving the shorter line is correct.</li></ul>"
  },
  {
    id: 14,
    name: "Trapping Rain Water",
    category: "Two Pointers",
    categoryId: "two-pointers",
    difficulty: "Hard",
    explanation: "<p><strong>Problem:</strong> Given <code>n</code> non-negative integers representing an elevation map where each bar has width 1, compute how much water it can trap after raining.</p><p>Water trapped at position <code>i</code> = <code>min(maxLeft[i], maxRight[i]) - height[i]</code> (if positive).</p><p><strong>Examples:</strong></p><ul><li>Input: <code>[0,1,0,2,1,0,1,3,2,1,2,1]</code> → Output: <code>6</code></li><li>Input: <code>[4,2,0,3,2,5]</code> → Output: <code>9</code></li></ul><p><strong>Intuition:</strong> Water above bar i is bounded by the tallest bars to its left and right. Specifically: <code>water[i] = max(0, min(maxL, maxR) - height[i])</code>. The challenge is computing maxLeft and maxRight efficiently.</p>",
    bruteForce: "<p><strong>Brute Force — For Each Bar, Scan Left and Right O(n²)</strong></p><p>For each bar, find the tallest bar to its left and right, then compute water:</p><pre><code>for i in range(n):\n  maxL = max(height[0..i])\n  maxR = max(height[i..n-1])\n  water += max(0, min(maxL, maxR) - height[i])</code></pre><p><strong>Time:</strong> O(n²) — for each of n bars, two linear scans.</p><p><strong>Space:</strong> O(1).</p>",
    bruteForceInefficiency: "<p><strong>Why O(n²) is wasteful and better approaches:</strong></p><ul><li><strong>O(n) with arrays:</strong> Precompute <code>maxLeft[i]</code> and <code>maxRight[i]</code> in two passes. Then compute water in a third pass. O(n) time, O(n) space.</li><li><strong>O(n) O(1) space with two pointers:</strong> We can compute maxLeft and maxRight on the fly. Key insight: water at position i depends only on <code>min(maxLeft, maxRight)</code>. The two-pointer approach processes from the side with the smaller maximum — we know the water at that side is determined by its own max (the other side is guaranteed to be taller).</li></ul>",
    optimalApproach: "<p><strong>Optimal — Two Pointers O(n) time, O(1) space</strong></p><p><strong>Pattern:</strong> Two pointers, process from smaller-max side</p><p><strong>Key insight:</strong> If <code>maxL &lt;= maxR</code>, the water at the left pointer is determined by <code>maxL</code> (not maxR, because maxR is at least as tall). So we can safely process the left side.</p><ol><li>Initialize <code>l=0, r=n-1, maxL=0, maxR=0, water=0</code>.</li><li>While <code>l &lt; r</code>:<ul><li>If <code>height[l] &lt;= height[r]</code>:<ul><li>Update <code>maxL = max(maxL, height[l])</code>.</li><li>Add <code>maxL - height[l]</code> to water (water above bar l).</li><li><code>l++</code>.</li></ul></li><li>Else (height[r] &lt; height[l]):<ul><li>Update <code>maxR = max(maxR, height[r])</code>.</li><li>Add <code>maxR - height[r]</code> to water.</li><li><code>r--</code>.</li></ul></li></ul></li><li>Return <code>water</code>.</li></ol><p><strong>Time:</strong> O(n). <strong>Space:</strong> O(1).</p>",
    javaCode: "class Solution {\n    public int trap(int[] height) {\n        int l = 0;                    // left pointer\n        int r = height.length - 1;   // right pointer\n        int maxL = 0;                // max height seen from the left\n        int maxR = 0;                // max height seen from the right\n        int water = 0;               // total water trapped\n        \n        while (l < r) {\n            if (height[l] <= height[r]) {\n                // left side is bounded by maxL (right side is at least height[r] >= height[l])\n                // so the limiting factor for bar l is maxL\n                maxL = Math.max(maxL, height[l]); // update max height seen from left\n                water += maxL - height[l]; // water above bar l = maxL - current height\n                // (if maxL == height[l], this adds 0 - that's fine, no water on peaks)\n                l++; // process left bar, move pointer inward\n            } else {\n                // right side is bounded by maxR (left side is taller)\n                // so the limiting factor for bar r is maxR\n                maxR = Math.max(maxR, height[r]); // update max height seen from right\n                water += maxR - height[r]; // water above bar r = maxR - current height\n                r--; // process right bar, move pointer inward\n            }\n        }\n        \n        return water; // total trapped water\n    }\n}",
    dryRun: "<p><strong>Input:</strong> <code>[4, 2, 0, 3, 2, 5]</code></p><table border='1' cellpadding='6' style='border-collapse:collapse'><tr><th>l</th><th>r</th><th>h[l]</th><th>h[r]</th><th>maxL</th><th>maxR</th><th>water added</th><th>total</th></tr><tr><td>0</td><td>5</td><td>4</td><td>5</td><td>max(0,4)=4</td><td>0</td><td>4-4=0</td><td>0</td></tr><tr><td>1</td><td>5</td><td>2</td><td>5</td><td>max(4,2)=4</td><td>0</td><td>4-2=2</td><td>2</td></tr><tr><td>2</td><td>5</td><td>0</td><td>5</td><td>max(4,0)=4</td><td>0</td><td>4-0=4</td><td>6</td></tr><tr><td>3</td><td>5</td><td>3</td><td>5</td><td>max(4,3)=4</td><td>0</td><td>4-3=1</td><td>7</td></tr><tr><td>4</td><td>5</td><td>2</td><td>5</td><td>max(4,2)=4</td><td>0</td><td>4-2=2</td><td>9</td></tr><tr><td>5</td><td>5</td><td colspan='5'>l==r, stop</td></tr></table><p><strong>Output:</strong> <code>9</code> ✓</p>",
    interviewTips: "<p><strong>Interview Script:</strong></p><ol><li><strong>Start with O(n) space solution:</strong> \"Precompute maxLeft[] and maxRight[] arrays in two passes, then compute water in a third pass.\" This is easier to explain and gets you a working solution quickly.</li><li><strong>Optimize to O(1) space:</strong> \"I can eliminate the arrays using two pointers. When height[l] &lt;= height[r], maxL is the bottleneck — compute water for l. Otherwise maxR is the bottleneck — compute water for r.\"</li><li><strong>Why correct?</strong> \"When height[l] &lt;= height[r], we know maxR is at least height[r] which is &gt;= height[l]. So the effective water level at l is min(maxL, maxR) = maxL.\"</li></ol><p><strong>This is a Hard problem</strong> — if you can explain the two-pointer logic clearly, that's impressive. Start with the array precomputation approach as backup.</p>",
    importantPoints: "<ul><li><strong>Water formula per bar:</strong> <code>min(maxLeft, maxRight) - height[i]</code> (0 if negative).</li><li><strong>Two-pointer key insight:</strong> When <code>height[l] &lt;= height[r]</code>, the right side has at least as tall a wall, so maxL determines water at l — we don't need to know the actual maxR.</li><li><strong>maxL and maxR are inclusive</strong> of the current bar — update BEFORE computing water.</li><li><strong>O(n) space approach</strong> (precompute arrays) is easier to explain and a valid solution. The two-pointer is the optimization.</li><li><strong>Stack-based approach</strong> also exists but is more complex — stick with two pointers for interviews.</li></ul>"
  }
];
