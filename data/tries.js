window.TRIES_DATA = [
  {
    id: 61,
    name: "Implement Trie (Prefix Tree)",
    category: "Tries",
    categoryId: "tries",
    difficulty: "Medium",
    explanation: "<p><strong>Implement Trie (Prefix Tree)</strong>: design a data structure that supports inserting words, searching for exact words, and checking if any word starts with a given prefix.</p><p>A <strong>Trie</strong> (pronounced 'try') is a tree-like structure where each node represents a character. Paths from root to nodes represent prefixes or complete words. Each node has up to 26 children (one per letter) and a flag indicating if a complete word ends there.</p><p><strong>Operations:</strong></p><ul><li><code>insert(word)</code>: add word to the trie.</li><li><code>search(word)</code>: return true if the exact word exists.</li><li><code>startsWith(prefix)</code>: return true if any word starts with the prefix.</li></ul>",
    bruteForce: "<p>Store all words in a <code>HashSet</code>. For <code>search</code>: O(1) average. For <code>startsWith</code>: iterate over all stored words and check if any starts with the prefix — O(n * m) where n = number of words and m = prefix length. This is inefficient for prefix queries.</p>",
    bruteForceInefficiency: "<p>The HashSet approach makes prefix queries O(n * m) — you must check every stored word against the prefix. A Trie makes prefix queries O(m) by directly navigating to the prefix's endpoint in the trie structure, regardless of how many words are stored.</p>",
    optimalApproach: "<p>Build a <strong>TrieNode</strong> class with an array of 26 child nodes and a boolean <code>isEnd</code> flag. For all three operations, traverse from the root character by character (index = char - 'a'). Insert creates missing nodes; search/startsWith just traverse and check existence.</p><ul><li><strong>Insert/Search/StartsWith Time:</strong> O(m) where m = word/prefix length.</li><li><strong>Space:</strong> O(total characters across all words) in the worst case.</li></ul>",
    javaCode: "class Trie {\n    private TrieNode root = new TrieNode();\n    class TrieNode {\n        TrieNode[] children = new TrieNode[26];\n        boolean isEnd = false;\n    }\n    public void insert(String word) {\n        TrieNode node = root;\n        for (char c : word.toCharArray()) {\n            if (node.children[c-'a'] == null) node.children[c-'a'] = new TrieNode();\n            node = node.children[c-'a'];\n        }\n        node.isEnd = true;\n    }\n    public boolean search(String word) {\n        TrieNode node = root;\n        for (char c : word.toCharArray()) {\n            if (node.children[c-'a'] == null) return false;\n            node = node.children[c-'a'];\n        }\n        return node.isEnd;\n    }\n    public boolean startsWith(String prefix) {\n        TrieNode node = root;\n        for (char c : prefix.toCharArray()) {\n            if (node.children[c-'a'] == null) return false;\n            node = node.children[c-'a'];\n        }\n        return true;\n    }\n}",
    dryRun: "<p><strong>Operations:</strong> insert('apple'), search('apple'), search('app'), startsWith('app')</p><ol><li><strong>insert('apple'):</strong> Create nodes for a→p→p→l→e, mark 'e' node as isEnd=true.</li><li><strong>search('apple'):</strong> Traverse a→p→p→l→e, node exists, isEnd=true → <strong>true</strong></li><li><strong>search('app'):</strong> Traverse a→p→p, node exists, isEnd=false → <strong>false</strong></li><li><strong>startsWith('app'):</strong> Traverse a→p→p, node exists → <strong>true</strong> (don't check isEnd)</li></ol>",
    interviewTips: "<ul><li>Explain the tradeoff: Trie uses more memory than HashSet but enables O(m) prefix queries.</li><li>The <code>TrieNode</code> array approach (size 26) is simple; a <code>HashMap&lt;Character, TrieNode&gt;</code> generalizes to any character set.</li><li>Mention use cases: autocomplete, spell checker, IP routing, word games.</li><li>Follow-up: how would you delete a word? (Mark isEnd=false and prune unused branches.)</li></ul>",
    importantPoints: "<ul><li><code>search</code> requires <code>node.isEnd == true</code>; <code>startsWith</code> only requires the path to exist.</li><li>Character index: <code>c - 'a'</code> maps 'a'→0, 'b'→1, ..., 'z'→25.</li><li>Root node is empty (doesn't represent any character) — it's just the entry point.</li><li>Memory: each TrieNode has 26 pointers — for sparse tries, a HashMap children is more memory-efficient.</li></ul>"
  },
  {
    id: 62,
    name: "Design Add and Search Words Data Structure",
    category: "Tries",
    categoryId: "tries",
    difficulty: "Medium",
    explanation: "<p><strong>Design Add and Search Words Data Structure</strong>: design a data structure that supports adding words and searching with wildcard patterns. A '.' in the search pattern matches any single character.</p><p>This extends the basic Trie with wildcard matching. When a '.' is encountered during search, we must explore all possible child branches — requiring DFS instead of simple traversal.</p><p><strong>Example:</strong> Add 'bad', 'dad', 'mad'. search('pad')→false, search('.ad')→true, search('b..')→true.</p>",
    bruteForce: "<p>Store all words in a list. For each search, iterate over all words and check if the pattern matches using character-by-character comparison (handling '.' as wildcard). Time: O(n * m) per search where n = number of words, m = word length.</p>",
    bruteForceInefficiency: "<p>The linear scan approach doesn't benefit from the prefix structure. Words sharing a common prefix are rechecked from scratch for each query. A Trie lets us share prefix traversal and prune non-matching branches early.</p>",
    optimalApproach: "<p>Use a <strong>Trie with recursive DFS search</strong>. For normal characters, traverse the specific child. For '.', recursively search ALL non-null children. Return true immediately if any branch succeeds.</p><ul><li><strong>addWord Time:</strong> O(m).</li><li><strong>search Time:</strong> O(26^m) worst case for all-dot patterns, but O(m) for exact words in practice.</li><li><strong>Space:</strong> O(total characters).</li></ul>",
    javaCode: "class WordDictionary {\n    private TrieNode root = new TrieNode();\n    class TrieNode {\n        TrieNode[] children = new TrieNode[26];\n        boolean isEnd = false;\n    }\n    public void addWord(String word) {\n        TrieNode node = root;\n        for (char c : word.toCharArray()) {\n            if (node.children[c-'a'] == null) node.children[c-'a'] = new TrieNode();\n            node = node.children[c-'a'];\n        }\n        node.isEnd = true;\n    }\n    public boolean search(String word) { return dfs(word, 0, root); }\n    private boolean dfs(String word, int i, TrieNode node) {\n        if (i == word.length()) return node.isEnd;\n        char c = word.charAt(i);\n        if (c == '.') {\n            for (TrieNode child : node.children) if (child != null && dfs(word, i+1, child)) return true;\n            return false;\n        }\n        if (node.children[c-'a'] == null) return false;\n        return dfs(word, i+1, node.children[c-'a']);\n    }\n}",
    dryRun: "<p><strong>After adding 'bad', 'dad', 'mad'. Search '.ad':</strong></p><ol><li>dfs('.ad', 0, root): c='.', try all children of root.</li><li>child['b'-'a'] exists (from 'bad'): dfs('ad', 1, b_node). c='a', go to a_node. dfs('d', 2, a_node). c='d', go to d_node. dfs('', 3, d_node). i==length, isEnd=true → <strong>return true!</strong></li><li>Short-circuits — no need to check 'd' and 'm' branches.</li></ol><p><strong>Output:</strong> true ✓</p>",
    interviewTips: "<ul><li>The key change from basic Trie: when '.' is encountered, fan out to all children and OR the results.</li><li>The DFS helper takes the current index in the word and the current TrieNode — both must be parameters.</li><li>Short-circuit: return true as soon as any branch succeeds (don't explore all branches unnecessarily).</li><li>Follow-up: what if '*' matches zero or more characters? (Requires DP or more complex backtracking.)</li></ul>",
    importantPoints: "<ul><li>Base case: when index reaches word length, return <code>node.isEnd</code> — a complete word must have ended here.</li><li>For '.', iterate through all 26 children (skip nulls) and recurse.</li><li>For normal chars, fail fast if the child doesn't exist.</li><li>The <code>dfs</code> method is called with the next index <code>i+1</code> — advancing through the pattern.</li></ul>"
  },
  {
    id: 63,
    name: "Word Search II",
    category: "Tries",
    categoryId: "tries",
    difficulty: "Hard",
    explanation: "<p><strong>Word Search II</strong>: given an m×n board of characters and a list of words, find all words on the board. A word can be formed by sequentially adjacent cells (horizontally or vertically), and each cell may not be used more than once per word.</p><p>This combines a Trie (for efficient multi-word matching) with DFS backtracking on the board (for path exploration).</p><p><strong>Example:</strong> board=[['o','a','a','n'],['e','t','a','e'],['i','h','k','r'],['i','f','l','v']], words=['oath','pea','eat','rain'] → ['eat','oath'].</p>",
    bruteForce: "<p>For each word in the list, run a separate DFS on the board (as in Word Search I). For each starting cell that matches the word's first character, explore all paths. Time: O(words × m × n × 4^L) where L = max word length — extremely slow for large word lists.</p>",
    bruteForceInefficiency: "<p>Running separate DFS for each word means identical board traversals are repeated for every word with a shared prefix. A Trie consolidates all words into a single structure: when we traverse the board, we simultaneously match against all words that share the current path as a prefix, eliminating redundant traversals.</p>",
    optimalApproach: "<p><strong>Build a Trie</strong> from all words. Then DFS from every cell on the board. At each step, check if the current character exists in the current TrieNode's children. If a TrieNode has a non-null <code>word</code> field, we've found a complete word. Mark visited cells with '#' to prevent reuse, restore on backtrack.</p><p><strong>Optimization:</strong> set <code>node.word = null</code> after finding a word to avoid duplicate results.</p><ul><li><strong>Time:</strong> O(m × n × 4^L) where L = max word length — shared prefix traversal reduces constants significantly.</li><li><strong>Space:</strong> O(total characters in words) for the Trie.</li></ul>",
    javaCode: "class Solution {\n    int[][] dirs = {{0,1},{0,-1},{1,0},{-1,0}};\n    public List<String> findWords(char[][] board, String[] words) {\n        TrieNode root = buildTrie(words);\n        List<String> res = new ArrayList<>();\n        for (int i = 0; i < board.length; i++)\n            for (int j = 0; j < board[0].length; j++)\n                dfs(board, i, j, root, res);\n        return res;\n    }\n    private void dfs(char[][] board, int i, int j, TrieNode node, List<String> res) {\n        char c = board[i][j];\n        if (c == '#' || node.children[c-'a'] == null) return;\n        node = node.children[c-'a'];\n        if (node.word != null) { res.add(node.word); node.word = null; }\n        board[i][j] = '#';\n        for (int[] d : dirs) {\n            int ni = i+d[0], nj = j+d[1];\n            if (ni>=0 && ni<board.length && nj>=0 && nj<board[0].length) dfs(board, ni, nj, node, res);\n        }\n        board[i][j] = c;\n    }\n    private TrieNode buildTrie(String[] words) {\n        TrieNode root = new TrieNode();\n        for (String w : words) {\n            TrieNode node = root;\n            for (char c : w.toCharArray()) {\n                if (node.children[c-'a'] == null) node.children[c-'a'] = new TrieNode();\n                node = node.children[c-'a'];\n            }\n            node.word = w;\n        }\n        return root;\n    }\n    class TrieNode { TrieNode[] children = new TrieNode[26]; String word; }\n}",
    dryRun: "<p><strong>Finding 'eat' on the board:</strong></p><ol><li>DFS starts at cell (1,0)='e'. Trie has 'e' child. Move to node['e'].</li><li>Mark board[1][0]='#'. Explore neighbors: (0,0)='o' (not in trie children), (2,0)='i' (not in trie), (1,1)='t' (check trie: 'et'... not a prefix of 'eat').</li><li>Actually from 'e' node, try (0,0)='o'→no, (2,0)='i'→no, (1,1)='t'→'et' not in trie.</li><li>DFS from cell (1,3)='e': neighbor (0,3)='n'→no. (1,2)='a'→yes! node=node['a']. Then (0,2)='a'→'eaa' no. (1,1)='t'→'eat' found! node.word='eat' → add 'eat' to res, set node.word=null.</li></ol><p><strong>Output:</strong> ['eat','oath'] ✓</p>",
    interviewTips: "<ul><li>Store the complete word string in the TrieNode's <code>word</code> field — avoids reconstructing the word from the path.</li><li>Set <code>node.word = null</code> after adding to results to prevent duplicates without needing a visited set.</li><li>The '#' trick for marking visited cells avoids a separate boolean visited array — restore it after backtracking.</li><li>Advanced optimization: prune dead Trie branches (nodes with no remaining words) to speed up future DFS calls.</li></ul>",
    importantPoints: "<ul><li>Build the Trie ONCE from all words, then do a SINGLE pass over the board — don't restart for each word.</li><li>Bounds check: ensure <code>ni</code> and <code>nj</code> are within the board before recursive DFS call.</li><li>Backtracking is essential: restore <code>board[i][j] = c</code> after DFS so other paths can use that cell.</li><li>The <code>c == '#'</code> check prevents revisiting cells already on the current path.</li></ul>"
  }
];
