window.SLIDING_WINDOW_DATA = [
  {
    id: 15,
    name: "Best Time to Buy and Sell Stock",
    category: "Sliding Window",
    categoryId: "sliding-window",
    difficulty: "Easy",
    explanation: "<p><strong>Problem:</strong> You are given an array <code>prices</code> where <code>prices[i]</code> is the price of a stock on day <code>i</code>. You want to maximize profit by choosing a single day to buy and a single day in the <em>future</em> to sell.</p><p>Return the maximum profit. If no profit is possible, return 0.</p><p><strong>Examples:</strong></p><ul><li>Input: <code>[7,1,5,3,6,4]</code> → Output: <code>5</code> (buy day 2 at 1, sell day 5 at 6)</li><li>Input: <code>[7,6,4,3,1]</code> → Output: <code>0</code> (prices only decrease)</li></ul><p><strong>Key insight:</strong> Track the minimum price seen so far. At each day, the best profit we can make is <code>price - minPrice</code>. Update the global maximum as we go.</p>",
    bruteForce: "<p><strong>Brute Force — All Pairs O(n²)</strong></p><p>Try every buy day and sell day combination:</p><pre><code>for i in range(n):\n  for j in range(i+1, n):\n    profit = prices[j] - prices[i]\n    maxProfit = max(maxProfit, profit)</code></pre><p><strong>Time:</strong> O(n²) — n×(n-1)/2 pairs.</p><p><strong>Space:</strong> O(1).</p>",
    bruteForceInefficiency: "<p><strong>Why we can do O(n):</strong></p><ul><li>We're recalculating the minimum buy price for each sell day.</li><li>Instead, we can track the minimum price seen so far in a single pass.</li><li>At each sell day, profit = <code>currentPrice - minPriceSoFar</code>.</li><li>We track the running minimum and running maximum profit simultaneously in one pass.</li></ul>",
    optimalApproach: "<p><strong>Optimal — One Pass O(n) time, O(1) space</strong></p><p><strong>Pattern:</strong> Track running minimum (or think of it as a sliding window with <code>l</code>=buy day, <code>r</code>=sell day)</p><ol><li>Initialize <code>minPrice = Integer.MAX_VALUE</code>, <code>maxProfit = 0</code>.</li><li>For each <code>price</code> in <code>prices</code>:<ul><li>If <code>price &lt; minPrice</code> → update <code>minPrice = price</code> (better buy day).</li><li>Else → compute <code>profit = price - minPrice</code>, update <code>maxProfit = max(maxProfit, profit)</code>.</li></ul></li><li>Return <code>maxProfit</code>.</li></ol><p><strong>Sliding window view:</strong> <code>l</code>=minPrice index, <code>r</code>=current sell day. When prices[r] &lt; prices[l], reset l=r (move buy day to new minimum).</p><p><strong>Time:</strong> O(n). <strong>Space:</strong> O(1).</p>",
    javaCode: "class Solution {\n    public int maxProfit(int[] prices) {\n        int minPrice = Integer.MAX_VALUE; // track the minimum price seen so far (best buy day)\n        int maxProfit = 0;               // track the maximum profit seen so far\n        \n        for (int price : prices) {\n            // update minimum price if today's price is cheaper\n            minPrice = Math.min(minPrice, price);\n            \n            // compute profit if we sell today (buy at minPrice, sell at price)\n            // update maximum profit if this profit is better\n            maxProfit = Math.max(maxProfit, price - minPrice);\n        }\n        \n        return maxProfit; // return best profit (0 if no profitable trade possible)\n    }\n}",
    dryRun: "<p><strong>Input:</strong> <code>[7, 1, 5, 3, 6, 4]</code></p><table border='1' cellpadding='6' style='border-collapse:collapse'><tr><th>day</th><th>price</th><th>minPrice</th><th>profit today</th><th>maxProfit</th></tr><tr><td>0</td><td>7</td><td>7</td><td>7-7=0</td><td>0</td></tr><tr><td>1</td><td>1</td><td>1</td><td>1-1=0</td><td>0</td></tr><tr><td>2</td><td>5</td><td>1</td><td>5-1=4</td><td>4</td></tr><tr><td>3</td><td>3</td><td>1</td><td>3-1=2</td><td>4</td></tr><tr><td>4</td><td>6</td><td>1</td><td>6-1=<strong>5</strong></td><td><strong>5</strong></td></tr><tr><td>5</td><td>4</td><td>1</td><td>4-1=3</td><td>5</td></tr></table><p><strong>Output:</strong> <code>5</code> ✓ (buy at day 1 for $1, sell at day 4 for $6)</p>",
    interviewTips: "<p><strong>Interview Script:</strong></p><ol><li><strong>Clarify:</strong> \"Can I buy and sell on the same day? Can I hold multiple stocks?\" (no to both)</li><li><strong>Insight:</strong> \"At each day, the best profit I can make is today's price minus the cheapest price I've seen before today.\"</li><li><strong>Algorithm:</strong> \"Track the running minimum price and running maximum profit in one pass.\"</li><li><strong>Edge case:</strong> \"If prices only decrease (like [7,6,5,4]), we never update maxProfit above 0, so we correctly return 0.\"</li></ol>",
    importantPoints: "<ul><li><strong>Initialize minPrice to Integer.MAX_VALUE</strong> — ensures the first price always updates it.</li><li><strong>Return 0 for decreasing arrays</strong> — maxProfit starts at 0, so if no trade is profitable, we return 0.</li><li><strong>One-pass approach:</strong> We update minPrice AND maxProfit in the same loop iteration — no separate passes needed.</li><li>Can be framed as sliding window: left pointer = cheapest buy, right pointer = current sell day. Move left when right &lt; left (cheaper buy found).</li></ul>"
  },
  {
    id: 16,
    name: "Longest Substring Without Repeating Characters",
    category: "Sliding Window",
    categoryId: "sliding-window",
    difficulty: "Medium",
    explanation: "<p><strong>Problem:</strong> Given a string <code>s</code>, find the length of the <strong>longest substring</strong> without repeating characters.</p><p><strong>Examples:</strong></p><ul><li>Input: <code>\"abcabcbb\"</code> → Output: <code>3</code> (\"abc\")</li><li>Input: <code>\"bbbbb\"</code> → Output: <code>1</code> (\"b\")</li><li>Input: <code>\"pwwkew\"</code> → Output: <code>3</code> (\"wke\")</li></ul><p><strong>Key insight:</strong> Use a sliding window. When we encounter a character already in the window, shrink the window from the left until the duplicate is removed. Track the last seen index of each character in a HashMap to jump the left pointer directly (instead of shrinking one step at a time).</p>",
    bruteForce: "<p><strong>Brute Force — All Substrings O(n²) or O(n³)</strong></p><p>Try all substrings and check for duplicates:</p><pre><code>for i in range(n):\n  for j in range(i, n):\n    if hasNoDuplicates(s[i..j]):\n      maxLen = max(maxLen, j - i + 1)</code></pre><p><strong>Time:</strong> O(n³) with naive duplicate check, O(n²) with HashSet.</p><p><strong>Space:</strong> O(min(n, charset)) for the HashSet.</p>",
    bruteForceInefficiency: "<p><strong>Why we can do O(n):</strong></p><ul><li>When we find that <code>s[i..j]</code> has a duplicate, we don't need to restart from scratch for <code>s[i+1..j+1]</code> — the window can just shrink from the left.</li><li><strong>Sliding window:</strong> Maintain a window <code>[l..r]</code> with no duplicates. When <code>s[r]</code> is a duplicate, advance <code>l</code> until the duplicate is gone.</li><li><strong>Optimization:</strong> Store the last-seen index of each character. When a duplicate is found at <code>r</code>, jump <code>l</code> directly to <code>map.get(s[r]) + 1</code> instead of advancing one step at a time.</li></ul>",
    optimalApproach: "<p><strong>Optimal — Sliding Window with HashMap O(n) time, O(1) space</strong></p><p><strong>Pattern:</strong> Variable-size sliding window; shrink when constraint violated</p><ol><li>Create <code>Map&lt;Character, Integer&gt; map</code> (char → last seen index).</li><li>Initialize <code>l=0, maxLen=0</code>.</li><li>For each <code>r</code> from 0 to n-1:<ul><li>If <code>map.containsKey(s[r]) &amp;&amp; map.get(s[r]) &gt;= l</code>: jump <code>l = map.get(s[r]) + 1</code> (move left past the duplicate).</li><li>Update <code>map.put(s[r], r)</code> (record latest index of this char).</li><li>Update <code>maxLen = max(maxLen, r - l + 1)</code>.</li></ul></li></ol><p><strong>Why <code>&gt;= l</code> check?</strong> The map may contain a stale entry for a character that was evicted from the window by a previous l-jump. We must only care if the char is within the current window.</p><p><strong>Time:</strong> O(n). <strong>Space:</strong> O(min(n, 128)) for ASCII character map.</p>",
    javaCode: "import java.util.HashMap; // import HashMap\nimport java.util.Map;     // import Map interface\n\nclass Solution {\n    public int lengthOfLongestSubstring(String s) {\n        Map<Character, Integer> map = new HashMap<>(); // char -> last seen index\n        int maxLen = 0; // track the maximum window size found\n        int l = 0;      // left boundary of the sliding window\n        \n        for (int r = 0; r < s.length(); r++) { // r = right boundary\n            char c = s.charAt(r); // current character\n            \n            // if c is already in the window (its last seen index >= l)\n            // jump l past the previous occurrence to remove the duplicate\n            if (map.containsKey(c) && map.get(c) >= l) {\n                l = map.get(c) + 1; // move l to just after the duplicate\n            }\n            \n            // update the last seen index of c to current position r\n            map.put(c, r);\n            \n            // update max window size: current window is s[l..r] of length r-l+1\n            maxLen = Math.max(maxLen, r - l + 1);\n        }\n        \n        return maxLen; // return length of longest valid substring\n    }\n}",
    dryRun: "<p><strong>Input:</strong> <code>\"abcabcbb\"</code></p><table border='1' cellpadding='6' style='border-collapse:collapse'><tr><th>r</th><th>c</th><th>map[c] &gt;= l?</th><th>l</th><th>window</th><th>maxLen</th></tr><tr><td>0</td><td>'a'</td><td>No (not in map)</td><td>0</td><td>\"a\"</td><td>1</td></tr><tr><td>1</td><td>'b'</td><td>No</td><td>0</td><td>\"ab\"</td><td>2</td></tr><tr><td>2</td><td>'c'</td><td>No</td><td>0</td><td>\"abc\"</td><td>3</td></tr><tr><td>3</td><td>'a'</td><td>Yes, map[a]=0 &gt;= 0 → l=1</td><td>1</td><td>\"bca\"</td><td>3</td></tr><tr><td>4</td><td>'b'</td><td>Yes, map[b]=1 &gt;= 1 → l=2</td><td>2</td><td>\"cab\"</td><td>3</td></tr><tr><td>5</td><td>'c'</td><td>Yes, map[c]=2 &gt;= 2 → l=3</td><td>3</td><td>\"abc\"</td><td>3</td></tr><tr><td>6</td><td>'b'</td><td>Yes, map[b]=4 &gt;= 3 → l=5</td><td>5</td><td>\"cb\"</td><td>3</td></tr><tr><td>7</td><td>'b'</td><td>Yes, map[b]=6 &gt;= 5 → l=7</td><td>7</td><td>\"b\"</td><td>3</td></tr></table><p><strong>Output:</strong> <code>3</code> ✓</p>",
    interviewTips: "<p><strong>Interview Script:</strong></p><ol><li><strong>Pattern recognition:</strong> \"This is a sliding window problem — maintain a window of unique characters.\"</li><li><strong>Algorithm:</strong> \"Use a HashMap to store the last index of each character. When we see a duplicate inside the window, jump the left pointer to just past the previous occurrence.\"</li><li><strong>Important check:</strong> \"I check <code>map.get(c) &gt;= l</code> to make sure the previous occurrence is actually inside the current window — stale map entries are possible.\"</li><li><strong>Complexity:</strong> \"O(n) time — each character processed once. O(1) space — at most 128 ASCII characters in the map.\"</li></ol>",
    importantPoints: "<ul><li><strong>The <code>&gt;= l</code> check is crucial</strong> — without it, you'd incorrectly jump l backwards when a character's last occurrence was before the current window.</li><li><strong>HashMap vs HashSet:</strong> Using a HashMap (char→index) allows O(1) jump of l. A HashSet would require advancing l one step at a time — still O(n) but with higher constant.</li><li><strong>Window invariant:</strong> At all times, <code>s[l..r]</code> contains only unique characters.</li><li><strong>Space O(1):</strong> Since there are only 128 ASCII (or 26 lowercase) characters, the map has a constant bound.</li></ul>"
  },
  {
    id: 17,
    name: "Longest Repeating Character Replacement",
    category: "Sliding Window",
    categoryId: "sliding-window",
    difficulty: "Medium",
    explanation: "<p><strong>Problem:</strong> Given a string <code>s</code> and an integer <code>k</code>, you can replace at most <code>k</code> characters in the string to make it all the same letter. Return the length of the longest substring that can be achieved.</p><p><strong>Examples:</strong></p><ul><li>Input: <code>s=\"ABAB\", k=2</code> → Output: <code>4</code> (replace both A's or both B's)</li><li>Input: <code>s=\"AABABBA\", k=1</code> → Output: <code>4</code> (\"AABA\" → replace B at index 3)</li></ul><p><strong>Key insight:</strong> In a window of length L with the most frequent character appearing <code>maxFreq</code> times, we need to replace <code>L - maxFreq</code> characters. If <code>L - maxFreq &lt;= k</code>, the window is valid.</p>",
    bruteForce: "<p><strong>Brute Force — All Substrings O(n²)</strong></p><p>Try every substring; for each, count max frequency and check if replacements needed &lt;= k:</p><pre><code>for i in range(n):\n  count = int[26]\n  for j in range(i, n):\n    count[s[j]-'A']++\n    maxFreq = max(count)\n    if (j - i + 1) - maxFreq &lt;= k:\n      maxLen = max(maxLen, j - i + 1)</code></pre><p><strong>Time:</strong> O(n²) — all O(n²) substrings, each processed in O(26)=O(1) incremental update.</p>",
    bruteForceInefficiency: "<p><strong>Why sliding window works:</strong></p><ul><li>As we extend the window (move r right), the window validity depends on: <code>windowLen - maxFreq &lt;= k</code>.</li><li>When the window becomes invalid, we shrink it from the left (move l right).</li><li><strong>Key insight:</strong> We never need to shrink the window to <em>smaller than the current best</em> — we only care about finding a <em>longer</em> valid window.</li><li>So instead of shrinking until valid again, we just shift the window (l++ when invalid) — window size stays the same or grows.</li></ul>",
    optimalApproach: "<p><strong>Optimal — Sliding Window O(n) time, O(1) space</strong></p><p><strong>Pattern:</strong> Variable sliding window; valid window condition: <code>(r - l + 1) - maxFreq &lt;= k</code></p><ol><li>Create <code>int[] count = new int[26]</code>, <code>maxFreq=0, maxLen=0, l=0</code>.</li><li>For each <code>r</code> from 0 to n-1:<ul><li>Increment <code>count[s[r]-'A']</code>.</li><li>Update <code>maxFreq = max(maxFreq, count[s[r]-'A'])</code>.</li><li>While <code>(r - l + 1) - maxFreq &gt; k</code>: shrink from left: <code>count[s[l]-'A']--; l++;</code></li><li>Update <code>maxLen = max(maxLen, r - l + 1)</code>.</li></ul></li></ol><p><strong>Note:</strong> We don't update <code>maxFreq</code> when shrinking. This means maxFreq can be a slight overestimate — but that's safe: it only prevents us from expanding the window (we still shift it), never causes incorrect results.</p><p><strong>Time:</strong> O(n). <strong>Space:</strong> O(1) — int[26] is constant.</p>",
    javaCode: "class Solution {\n    public int characterReplacement(String s, int k) {\n        int[] count = new int[26]; // frequency count of each letter in window (A=0, B=1, ...)\n        int maxFreq = 0;           // max frequency of any single char in current window\n        int maxLen = 0;            // result: length of longest valid window\n        int l = 0;                 // left boundary of sliding window\n        \n        for (int r = 0; r < s.length(); r++) { // r = right boundary\n            // expand window: add character at r\n            count[s.charAt(r) - 'A']++;\n            \n            // update maxFreq to include the newly added character\n            maxFreq = Math.max(maxFreq, count[s.charAt(r) - 'A']);\n            \n            // check if window is valid: we can fill non-maxFreq chars with k replacements\n            // if characters needing replacement (window size - maxFreq) > k, shrink window\n            while ((r - l + 1) - maxFreq > k) {\n                count[s.charAt(l) - 'A']--; // remove leftmost char from window\n                l++;                         // shrink window from left\n                // note: we don't update maxFreq here (slight overestimate is safe)\n            }\n            \n            // window s[l..r] is valid: update maximum length\n            maxLen = Math.max(maxLen, r - l + 1);\n        }\n        \n        return maxLen; // return length of longest valid substring\n    }\n}",
    dryRun: "<p><strong>Input:</strong> <code>s=\"AABABBA\", k=1</code></p><table border='1' cellpadding='6' style='border-collapse:collapse'><tr><th>r</th><th>s[r]</th><th>count[A],count[B]</th><th>maxFreq</th><th>winLen</th><th>winLen-maxFreq</th><th>valid?</th><th>maxLen</th></tr><tr><td>0</td><td>A</td><td>1,0</td><td>1</td><td>1</td><td>0</td><td>✓</td><td>1</td></tr><tr><td>1</td><td>A</td><td>2,0</td><td>2</td><td>2</td><td>0</td><td>✓</td><td>2</td></tr><tr><td>2</td><td>B</td><td>2,1</td><td>2</td><td>3</td><td>1</td><td>✓</td><td>3</td></tr><tr><td>3</td><td>A</td><td>3,1</td><td>3</td><td>4</td><td>1</td><td>✓</td><td>4</td></tr><tr><td>4</td><td>B</td><td>3,2</td><td>3</td><td>5</td><td>2&gt;1→shrink: l=1, A:[2,2]</td><td>✓ after</td><td>4</td></tr><tr><td>5</td><td>B</td><td>2,3</td><td>3</td><td>5</td><td>2&gt;1→shrink: l=2, A:[1,3]</td><td>✓ after</td><td>4</td></tr><tr><td>6</td><td>A</td><td>2,3</td><td>3</td><td>5</td><td>2&gt;1→shrink: l=3, A:[1,3]</td><td>✓ after</td><td>4</td></tr></table><p><strong>Output:</strong> <code>4</code> ✓</p>",
    interviewTips: "<p><strong>Interview Script:</strong></p><ol><li><strong>Key insight:</strong> \"In any window, the minimum replacements needed = window length - frequency of most common character.\"</li><li><strong>Valid window condition:</strong> <code>windowLen - maxFreq &lt;= k</code>.</li><li><strong>Why not update maxFreq when shrinking?</strong> \"When we shrink, maxFreq might decrease. But if it decreases, we'll never find a LONGER valid window until maxFreq increases again — so keeping the overestimate just means we shift the window size rather than shrink it. The result is still correct.\"</li><li><strong>Complexity:</strong> O(n) time, O(1) space.</li></ol>",
    importantPoints: "<ul><li><strong>Window validity:</strong> <code>(r - l + 1) - maxFreq &lt;= k</code> — this is the core formula to memorize.</li><li><strong>Not updating maxFreq on shrink</strong> is intentional — it's a key optimization. The window only grows or shifts, never shrinks below its historical maximum size.</li><li><strong>count[] is over current window</strong> — when we shrink (l++), we decrement count[s[l]-'A'].</li><li><strong>Uppercase letters</strong> — subtract 'A' (not 'a') for the array index.</li></ul>"
  },
  {
    id: 18,
    name: "Permutation in String",
    category: "Sliding Window",
    categoryId: "sliding-window",
    difficulty: "Medium",
    explanation: "<p><strong>Problem:</strong> Given two strings <code>s1</code> and <code>s2</code>, return <code>true</code> if <code>s2</code> contains a permutation of <code>s1</code> as a substring.</p><p>Equivalently: does any window of size <code>s1.length()</code> in <code>s2</code> have the same character frequencies as <code>s1</code>?</p><p><strong>Examples:</strong></p><ul><li>Input: <code>s1=\"ab\", s2=\"eidbaooo\"</code> → Output: <code>true</code> (\"ba\" is permutation of \"ab\")</li><li>Input: <code>s1=\"ab\", s2=\"eidboaoo\"</code> → Output: <code>false</code></li></ul><p><strong>Key insight:</strong> Use a fixed-size sliding window of size <code>s1.length()</code> over <code>s2</code>. Maintain a character frequency array for the window and compare to s1's frequency array.</p>",
    bruteForce: "<p><strong>Brute Force — Check Every Substring O(n·k log k)</strong></p><p>For each window of size k=s1.length() in s2, sort it and compare to sorted s1:</p><pre><code>sortedS1 = sort(s1)\nfor i in range(len(s2) - k + 1):\n  if sort(s2[i:i+k]) == sortedS1: return true\nreturn false</code></pre><p><strong>Time:</strong> O(n·k log k) — n windows, each sorted in k log k.</p><p><strong>Better brute force:</strong> Compare frequency arrays — O(26) per window = O(26n).</p>",
    bruteForceInefficiency: "<p><strong>Why we can do better:</strong></p><ul><li>When the window slides one position right, only <strong>one character is added</strong> (at the right) and <strong>one is removed</strong> (from the left).</li><li>Instead of recomputing the full frequency array each time, we can update it incrementally in O(1) per slide.</li><li>Then compare the two frequency arrays in O(26) = O(1) per window.</li><li>Total: O(n) — much better than O(26n) for large windows.</li></ul>",
    optimalApproach: "<p><strong>Optimal — Fixed-Size Sliding Window O(n) time, O(1) space</strong></p><p><strong>Pattern:</strong> Fixed-size sliding window with frequency array</p><ol><li>If <code>s1.length() &gt; s2.length()</code> → return false immediately.</li><li>Build <code>int[] need</code> (s1's frequencies) and <code>int[] have</code> (current window's frequencies).</li><li>Initialize the first window of size s1.length() in s2.</li><li>Slide the window: for each new right char added, remove the outgoing left char:<ul><li>Add s2[r] to have.</li><li>If window too big: remove s2[l] from have, increment l.</li><li>Compare <code>Arrays.equals(need, have)</code>.</li></ul></li></ol><p><strong>Time:</strong> O(n) where n=s2.length(). <code>Arrays.equals(int[26], int[26])</code> is O(26)=O(1).</p><p><strong>Space:</strong> O(1) — two arrays of size 26.</p>",
    javaCode: "import java.util.Arrays; // import Arrays for Arrays.equals\n\nclass Solution {\n    public boolean checkInclusion(String s1, String s2) {\n        // if s1 is longer than s2, no permutation can fit inside s2\n        if (s1.length() > s2.length()) return false;\n        \n        int[] need = new int[26]; // frequency of each char needed (from s1)\n        int[] have = new int[26]; // frequency of each char in current window (s2 window)\n        \n        // build the 'need' frequency array from s1\n        for (char c : s1.toCharArray()) {\n            need[c - 'a']++;\n        }\n        \n        int l = 0; // left boundary of sliding window in s2\n        \n        for (int r = 0; r < s2.length(); r++) {\n            // add the new right character to the window\n            have[s2.charAt(r) - 'a']++;\n            \n            // if window size exceeds s1.length(), shrink from the left\n            if (r - l + 1 > s1.length()) {\n                have[s2.charAt(l) - 'a']--; // remove leftmost char from window\n                l++;                         // slide the window right\n            }\n            \n            // check if current window is a permutation of s1\n            // Arrays.equals compares all 26 frequencies in O(26) = O(1)\n            if (Arrays.equals(need, have)) {\n                return true; // found a permutation!\n            }\n        }\n        \n        return false; // no permutation found in s2\n    }\n}",
    dryRun: "<p><strong>Input:</strong> <code>s1=\"ab\", s2=\"eidbaooo\"</code></p><p><strong>need:</strong> a=1, b=1, others=0</p><table border='1' cellpadding='6' style='border-collapse:collapse'><tr><th>r</th><th>s2[r]</th><th>window</th><th>have(a,b,d,e,i,o)</th><th>match need?</th></tr><tr><td>0</td><td>'e'</td><td>\"e\"</td><td>e=1</td><td>No (size 1&lt;2)</td></tr><tr><td>1</td><td>'i'</td><td>\"ei\"</td><td>e=1,i=1</td><td>No</td></tr><tr><td>2</td><td>'d'</td><td>\"id\"(removed e)</td><td>d=1,i=1</td><td>No</td></tr><tr><td>3</td><td>'b'</td><td>\"db\"(removed i)</td><td>b=1,d=1</td><td>No</td></tr><tr><td>4</td><td>'a'</td><td>\"ba\"(removed d)</td><td>a=1,b=1</td><td><strong>Yes!</strong></td></tr></table><p><strong>Output:</strong> <code>true</code> ✓ (window \"ba\" is a permutation of \"ab\")</p>",
    interviewTips: "<p><strong>Interview Script:</strong></p><ol><li><strong>Reframe:</strong> \"A permutation of s1 is just any substring of s2 with the same character frequencies as s1.\"</li><li><strong>Fixed window:</strong> \"The window size is fixed at s1.length() — I slide it across s2, updating frequencies incrementally.\"</li><li><strong>Comparison:</strong> \"Compare frequency arrays using <code>Arrays.equals()</code> — O(26)=O(1).\"</li><li><strong>Complexity:</strong> \"O(n) time, O(1) space (constant-size arrays).\"</li></ol><p><strong>Alternative:</strong> Instead of <code>Arrays.equals</code>, maintain a counter of how many characters are \"satisfied\" (have[c] == need[c]). Increment/decrement as the window slides. If satisfied count == 26 → match found. This avoids the O(26) comparison per step.</p>",
    importantPoints: "<ul><li><strong>Fixed window size</strong> = s1.length(). This is a classic fixed-size sliding window pattern.</li><li><strong>Arrays.equals(int[], int[])</code> compares element by element</strong> — O(26) = O(1) for character frequency arrays.</li><li><strong>Lowercase letters only</strong> — subtract 'a' for array index (0-25).</li><li><strong>Edge case:</strong> If s1.length() &gt; s2.length(), return false immediately.</li><li>This same pattern applies to any \"find anagram/permutation in string\" variant.</li></ul>"
  },
  {
    id: 19,
    name: "Minimum Window Substring",
    category: "Sliding Window",
    categoryId: "sliding-window",
    difficulty: "Hard",
    explanation: "<p><strong>Problem:</strong> Given two strings <code>s</code> and <code>t</code>, return the <strong>minimum window substring</strong> of <code>s</code> such that every character in <code>t</code> (including duplicates) is included in the window. If no such window exists, return <code>\"\"</code>.</p><p><strong>Examples:</strong></p><ul><li>Input: <code>s=\"ADOBECODEBANC\", t=\"ABC\"</code> → Output: <code>\"BANC\"</code></li><li>Input: <code>s=\"a\", t=\"a\"</code> → Output: <code>\"a\"</code></li><li>Input: <code>s=\"a\", t=\"aa\"</code> → Output: <code>\"\"</code> (not enough 'a's)</li></ul><p><strong>Key insight:</strong> Variable-size sliding window. Expand right until window contains all chars of t. Then shrink left to find minimum valid window. Track when a character's count in window exactly meets the requirement (<code>formed</code> counter).</p>",
    bruteForce: "<p><strong>Brute Force — All Substrings O(n²)</strong></p><p>Try every substring of s and check if it contains all chars of t:</p><pre><code>for i in range(n):\n  for j in range(i, n):\n    if containsAll(s[i..j], t):\n      update minimum window</code></pre><p><strong>Time:</strong> O(n² · |t|) — O(n²) substrings, each checked in O(|t|).</p><p><strong>Space:</strong> O(|t|) for frequency map.</p>",
    bruteForceInefficiency: "<p><strong>Why sliding window is better:</strong></p><ul><li>When we find a valid window, we want to shrink it — but brute force starts from scratch for each left boundary.</li><li><strong>Sliding window:</strong> Expand right to find a valid window, then shrink left while still valid, then expand right again. Each character is added/removed at most once → O(n).</li><li><strong>The tricky part:</strong> How do we efficiently know when the window is valid (contains all of t)? Use a <code>formed</code> counter that tracks how many unique characters are fully satisfied in the window.</li></ul>",
    optimalApproach: "<p><strong>Optimal — Sliding Window with formed counter O(n) time, O(|t|) space</strong></p><p><strong>Pattern:</strong> Expand until valid, shrink while valid, track minimum</p><ol><li>Build <code>need</code> map from t (char → required count). <code>required = need.size()</code>.</li><li>Use <code>have</code> map for window, <code>formed=0</code> (# chars fully satisfied).</li><li>Sliding window: expand r; if <code>have[c] == need[c]</code>, <code>formed++</code>.</li><li>When <code>formed == required</code> (window valid): record minimum window; shrink from l; if removing l makes a char unsatisfied, <code>formed--</code>.</li><li>Continue until r reaches end.</li></ol><p><strong>Time:</strong> O(|s| + |t|). <strong>Space:</strong> O(|t|) for the need/have maps.</p>",
    javaCode: "import java.util.HashMap; // import HashMap\nimport java.util.Map;     // import Map interface\n\nclass Solution {\n    public String minWindow(String s, String t) {\n        // build frequency map of required characters from t\n        Map<Character, Integer> need = new HashMap<>();\n        for (char c : t.toCharArray()) {\n            need.merge(c, 1, Integer::sum);\n        }\n        \n        Map<Character, Integer> have = new HashMap<>(); // current window's char frequencies\n        int formed = 0;            // number of unique chars in t that are fully satisfied in window\n        int required = need.size(); // number of unique chars we need to satisfy\n        \n        int l = 0;                     // left boundary of window\n        int minLen = Integer.MAX_VALUE; // length of minimum valid window found\n        int minL = 0;                   // starting index of minimum valid window\n        \n        for (int r = 0; r < s.length(); r++) {\n            char c = s.charAt(r); // add character at r to window\n            have.merge(c, 1, Integer::sum); // increment its frequency in window\n            \n            // check if this character's requirement is now fully met\n            // (have[c] == need[c] means we have exactly enough of this char)\n            if (need.containsKey(c) && have.get(c).equals(need.get(c))) {\n                formed++; // one more required character is now fully satisfied\n            }\n            \n            // try to shrink the window from the left while it's still valid\n            while (formed == required) {\n                // record this window if it's the smallest valid window so far\n                if (r - l + 1 < minLen) {\n                    minLen = r - l + 1; // update minimum length\n                    minL = l;           // record starting index\n                }\n                \n                // remove leftmost character from window\n                char lc = s.charAt(l++);\n                have.merge(lc, -1, Integer::sum); // decrement its count\n                \n                // if removing lc breaks a requirement, decrement formed\n                if (need.containsKey(lc) && have.get(lc) < need.get(lc)) {\n                    formed--; // this char is no longer fully satisfied\n                }\n            }\n        }\n        \n        // return minimum window, or \"\" if no valid window was found\n        return minLen == Integer.MAX_VALUE ? \"\" : s.substring(minL, minL + minLen);\n    }\n}",
    dryRun: "<p><strong>Input:</strong> <code>s=\"ADOBECODEBANC\", t=\"ABC\"</code></p><p><strong>need:</strong> {A:1, B:1, C:1}, required=3</p><table border='1' cellpadding='6' style='border-collapse:collapse'><tr><th>r</th><th>s[r]</th><th>formed</th><th>action</th><th>window / minLen</th></tr><tr><td>0-4</td><td>A,D,O,B,E</td><td>0→1(A)→2(B)</td><td>expand</td><td>\"ADOBE\" formed=2</td></tr><tr><td>5</td><td>C</td><td>3</td><td>formed==3! shrink: remove A(l=1)→formed=2</td><td>minLen=6 \"ADOBEC\"</td></tr><tr><td>6-9</td><td>O,D,E,B</td><td>2</td><td>expand</td><td>\"DOBECODEB\"</td></tr><tr><td>10</td><td>A</td><td>3</td><td>formed==3! shrink: remove D(l=2)→ok, O→ok, B(l=4)→formed=2</td><td>minLen=min(6,7)=6, then tried smaller</td></tr><tr><td>11</td><td>N</td><td>2</td><td>expand</td><td>—</td></tr><tr><td>12</td><td>C</td><td>3</td><td>formed==3! shrink: remove E(l=5)→ok, C(l=6)→formed=2</td><td>minLen=min(6,4)=4, minL=9 → \"BANC\"</td></tr></table><p><strong>Output:</strong> <code>\"BANC\"</code> ✓</p>",
    interviewTips: "<p><strong>Interview Script:</strong></p><ol><li><strong>Pattern:</strong> \"Expand right until window is valid (contains all of t), then shrink left while still valid.\"</li><li><strong>Formed counter:</strong> \"I track how many unique characters in t are fully satisfied in my window. When formed==required, the window is valid.\"</li><li><strong>Why .equals() not ==?</strong> \"For Integer objects, use .equals() for value comparison — == compares references (unreliable for large values).\"</li><li><strong>Complexity:</strong> \"O(|s| + |t|) time, O(|t|) space.\"</li></ol><p><strong>Common mistakes:</strong></p><ul><li>Forgetting to check <code>have.get(lc) &lt; need.get(lc)</code> when removing from left — must check AFTER decrement.</li><li>Using == instead of .equals() for Integer comparison.</li></ul>",
    importantPoints: "<ul><li><strong>formed counter</strong> is the elegance of this solution — avoids checking all characters on every window change.</li><li><strong>formed increments</strong> when <code>have[c] == need[c]</code> (exactly met, not exceeded).</li><li><strong>formed decrements</strong> when removing a char makes <code>have[c] &lt; need[c]</code> (now under-satisfied).</li><li><strong>Integer.equals()</strong> — always use .equals() for Integer object comparison in Java, not ==.</li><li><strong>Sliding window summary:</strong> Two nested loops but each element is added/removed at most once → O(n) total.</li></ul>"
  },
  {
    id: 20,
    name: "Sliding Window Maximum",
    category: "Sliding Window",
    categoryId: "sliding-window",
    difficulty: "Hard",
    explanation: "<p><strong>Problem:</strong> Given an integer array <code>nums</code> and a sliding window of size <code>k</code> moving from left to right, return an array of the maximum value in each window position.</p><p><strong>Examples:</strong></p><ul><li>Input: <code>nums=[1,3,-1,-3,5,3,6,7], k=3</code> → Output: <code>[3,3,5,5,6,7]</code></li><li>Window positions: [1,3,-1]→3, [3,-1,-3]→3, [-1,-3,5]→5, [-3,5,3]→5, [5,3,6]→6, [3,6,7]→7</li></ul><p><strong>Key insight:</strong> Use a <strong>monotonic decreasing deque</strong>. The deque stores indices of elements in decreasing order of value. When the window slides, the front of the deque is always the maximum of the current window.</p>",
    bruteForce: "<p><strong>Brute Force — Scan Each Window O(n·k)</strong></p><p>For each window position, find the maximum by scanning all k elements:</p><pre><code>for i in range(n - k + 1):\n  max_val = max(nums[i:i+k])\n  result.append(max_val)</code></pre><p><strong>Time:</strong> O(n·k) — n-k+1 windows, each scanned in k steps.</p><p><strong>Space:</strong> O(1) extra.</p><p>For n=100,000 and k=50,000, this is 5 billion operations — too slow.</p>",
    bruteForceInefficiency: "<p><strong>Why O(n·k) is slow and the monotonic deque insight:</strong></p><ul><li>When the window slides right by 1, the maximum can only come from 3 sources: the new element, or a previous element still in the window.</li><li><strong>Key observation:</strong> If element A is to the left of element B and A &lt;= B, then A can NEVER be the maximum of any future window (B will always be there to outmatch it as long as B is in the window). So A is useless and can be discarded.</li><li>This means we can maintain a <strong>decreasing deque</strong> — elements are kept in decreasing order. The front is always the window's maximum.</li></ul>",
    optimalApproach: "<p><strong>Optimal — Monotonic Deque O(n) time, O(k) space</strong></p><p><strong>Pattern:</strong> Monotonic decreasing deque for range maximum queries</p><ol><li>Use <code>Deque&lt;Integer&gt; dq</code> storing <em>indices</em> (not values) in decreasing order of <code>nums[index]</code>.</li><li>For each right index <code>r</code>:<ul><li><strong>Clean back:</strong> Remove from back while <code>nums[dq.peekLast()] &lt; nums[r]</code> (current element is better).</li><li>Add <code>r</code> to back of deque.</li><li><strong>Clean front:</strong> Remove front if <code>dq.peekFirst() &lt; l</code> (left boundary — out of window).</li><li>Once window reaches size k: <code>res[l] = nums[dq.peekFirst()]</code> (front is the max), advance l.</li></ul></li></ol><p><strong>Time:</strong> O(n) — each element added/removed from deque at most once.</p><p><strong>Space:</strong> O(k) — deque holds at most k indices.</p>",
    javaCode: "import java.util.ArrayDeque; // import ArrayDeque\nimport java.util.Deque;      // import Deque interface\n\nclass Solution {\n    public int[] maxSlidingWindow(int[] nums, int k) {\n        int n = nums.length;          // total elements\n        int[] res = new int[n - k + 1]; // result array: one max per window\n        Deque<Integer> dq = new ArrayDeque<>(); // monotonic deque stores INDICES\n        int l = 0; // left boundary of current window\n        \n        for (int r = 0; r < n; r++) { // r = right boundary (expand window)\n            // maintain decreasing monotonic order in deque:\n            // remove indices from the back whose values are <= nums[r]\n            // (they can never be the max as long as nums[r] is in the window)\n            while (!dq.isEmpty() && nums[dq.peekLast()] < nums[r]) {\n                dq.pollLast(); // remove smaller/equal elements from back\n            }\n            dq.addLast(r); // add current index to back of deque\n            \n            // remove front index if it's outside the current window\n            if (dq.peekFirst() < l) {\n                dq.pollFirst(); // front is out of window, remove it\n            }\n            \n            // once we have a full window of size k, record the maximum\n            // front of deque is always the index of the maximum in current window\n            if (r - l + 1 == k) {\n                res[l] = nums[dq.peekFirst()]; // max of window starting at l\n                l++; // slide window right\n            }\n        }\n        \n        return res; // array of window maximums\n    }\n}",
    dryRun: "<p><strong>Input:</strong> <code>nums=[1,3,-1,-3,5,3,6,7], k=3</code></p><table border='1' cellpadding='6' style='border-collapse:collapse'><tr><th>r</th><th>nums[r]</th><th>dq (indices)</th><th>nums[dq front]</th><th>window full?</th><th>res</th></tr><tr><td>0</td><td>1</td><td>[0]</td><td>1</td><td>No</td><td>—</td></tr><tr><td>1</td><td>3</td><td>pop 0 (1&lt;3), [1]</td><td>3</td><td>No</td><td>—</td></tr><tr><td>2</td><td>-1</td><td>[1,2]</td><td>3</td><td>Yes(l=0): res[0]=3, l=1</td><td>[3]</td></tr><tr><td>3</td><td>-3</td><td>[1,2,3]</td><td>3</td><td>Yes(l=1): res[1]=3, l=2</td><td>[3,3]</td></tr><tr><td>4</td><td>5</td><td>pop 3(-3),2(-1),1(3)→[4]</td><td>5</td><td>Yes(l=2): res[2]=5, l=3</td><td>[3,3,5]</td></tr><tr><td>5</td><td>3</td><td>[4,5]</td><td>5</td><td>Yes(l=3): res[3]=5, l=4</td><td>[3,3,5,5]</td></tr><tr><td>6</td><td>6</td><td>pop 5(3), pop 4(5)&lt;6→[6]</td><td>6</td><td>Yes(l=4): res[4]=6, l=5</td><td>[3,3,5,5,6]</td></tr><tr><td>7</td><td>7</td><td>pop 6(6)&lt;7→[7]</td><td>7</td><td>Yes(l=5): res[5]=7, l=6</td><td>[3,3,5,5,6,7]</td></tr></table><p><strong>Output:</strong> <code>[3,3,5,5,6,7]</code> ✓</p>",
    interviewTips: "<p><strong>Interview Script:</strong></p><ol><li><strong>Brute force:</strong> O(n·k) — scan each window. Too slow for large inputs.</li><li><strong>Key insight:</strong> \"If I'm at element B and A is to the left with A &lt;= B, then A can never be a maximum in any remaining window — B will always beat it. So I discard A.\"</li><li><strong>Deque stores indices</strong> (not values) — needed to check if elements have left the window.</li><li><strong>Deque invariant:</strong> Elements at front → back are in decreasing order of value. Front is always the current window's maximum.</li><li><strong>Two cleanup steps:</strong> (1) Remove smaller elements from back before inserting. (2) Remove out-of-window elements from front.</li></ol><p><strong>This is Hard</strong> because the monotonic deque concept is non-intuitive. Practice explaining the \"why discard smaller elements\" logic.</p>",
    importantPoints: "<ul><li><strong>Monotonic decreasing deque</strong> — elements kept in descending order of value; front is always the maximum.</li><li><strong>Store indices, not values</strong> — needed to check if the front element has fallen outside the window (<code>dq.peekFirst() &lt; l</code>).</li><li><strong>Two cleanup steps:</strong> Back cleanup (remove smaller) happens BEFORE adding. Front cleanup (remove out-of-window) happens AFTER adding.</li><li><strong>O(n) amortized:</strong> Each element is added to and removed from the deque at most once.</li><li><code>ArrayDeque</code> is Java's efficient double-ended queue — use <code>addLast/pollLast</code> for back, <code>peekFirst/pollFirst</code> for front.</li></ul>"
  }
];
