window.HEAP_DATA = [
  {
    id: 64,
    name: "Kth Largest Element in a Stream",
    category: "Heap / Priority Queue",
    categoryId: "heap",
    difficulty: "Easy",
    explanation: "<p><strong>Kth Largest Element in a Stream</strong>: design a class that finds the kth largest element in a stream of numbers. The class must support: constructor (takes k and an initial array), and <code>add(val)</code> (adds a value and returns the kth largest).</p><p>The kth largest means if all elements were sorted in descending order, we want the element at index k-1 (0-indexed).</p><p><strong>Example:</strong> k=3, initial=[4,5,8,2]. add(3)→4, add(5)→5, add(10)→5, add(9)→8, add(4)→8.</p>",
    bruteForce: "<p>Store all elements in a sorted list. On each <code>add</code>, insert in sorted order and return the element at index <code>size - k</code>. Insert in a sorted array takes O(n), making each add operation O(n).</p>",
    bruteForceInefficiency: "<p>Keeping a fully sorted array requires O(n) insertion time and O(n) space for all elements — but we only ever care about the top k elements. A min-heap of size k maintains exactly what we need with O(log k) add operations.</p>",
    optimalApproach: "<p>Maintain a <strong>min-heap of size k</strong>. The heap always holds the k largest elements seen so far. The minimum of this heap (the heap's top) is the kth largest. On <code>add</code>: push the new value, then if heap size exceeds k, remove the minimum. The kth largest is always <code>heap.peek()</code>.</p><ul><li><strong>Add Time:</strong> O(log k).</li><li><strong>Space:</strong> O(k).</li></ul>",
    javaCode: "class KthLargest {\n    private PriorityQueue<Integer> heap;\n    private int k;\n    public KthLargest(int k, int[] nums) {\n        this.k = k;\n        heap = new PriorityQueue<>();\n        for (int n : nums) add(n);\n    }\n    public int add(int val) {\n        heap.offer(val);\n        if (heap.size() > k) heap.poll();\n        return heap.peek();\n    }\n}",
    dryRun: "<p><strong>k=3, nums=[4,5,8,2]</strong></p><ol><li>add(4): heap=[4], size=1 &lt;=3, peek=4</li><li>add(5): heap=[4,5], size=2 &lt;=3, peek=4</li><li>add(8): heap=[4,5,8], size=3=k, peek=4</li><li>add(2): heap=[2,4,5,8], size=4&gt;3 → poll min(2) → heap=[4,5,8], peek=<strong>4</strong></li></ol><p>Now add(3): heap=[3,4,5,8]→poll(3)→[4,5,8], peek=<strong>4</strong>. add(5): heap=[4,5,5,8]→poll(4)→[5,5,8], peek=<strong>5</strong>.</p>",
    interviewTips: "<ul><li>Explain the key insight: a min-heap of size k always has the kth largest at its top — because k-1 elements are larger than it (above it in sorted order).</li><li>Handle the edge case in the constructor where <code>nums.length &lt; k</code> — the heap may have fewer than k elements initially.</li><li>The <code>add</code> method in the constructor call handles initialization cleanly, avoiding duplicate code.</li></ul>",
    importantPoints: "<ul><li>Java's <code>PriorityQueue</code> is a min-heap by default — the smallest element is at the top.</li><li>After adding and potentially removing, <code>heap.peek()</code> is the kth largest.</li><li>Calling <code>add(n)</code> from the constructor reuses the same logic — no need for separate initialization code.</li><li>Edge: if nums contains fewer than k elements, <code>heap.peek()</code> is still valid once the stream provides enough elements.</li></ul>"
  },
  {
    id: 65,
    name: "Last Stone Weight",
    category: "Heap / Priority Queue",
    categoryId: "heap",
    difficulty: "Easy",
    explanation: "<p><strong>Last Stone Weight</strong>: you have stones with positive integer weights. Each turn, smash the two heaviest stones together. If they have equal weight, both are destroyed. If they're different (y &gt;= x), the stone of weight y becomes y-x. Repeat until at most one stone remains. Return the weight of the last stone, or 0 if none remain.</p><p>This is a simulation problem where we repeatedly need the two maximum elements — perfect for a max-heap.</p><p><strong>Example:</strong> stones=[2,7,4,1,8,1]. Smash 8 and 7 → 1. Stones=[2,4,1,1,1]. Smash 4 and 2 → 2. Continue... → final stone = 1.</p>",
    bruteForce: "<p>Each turn, scan the array to find the two largest elements, smash them, and update the array. Finding two maximums takes O(n) per round and there can be up to O(n) rounds → O(n²) total time.</p>",
    bruteForceInefficiency: "<p>Linear scanning for the maximum each round is wasteful — we're repeatedly doing work that a heap can do in O(log n). The heap maintains the ordering automatically so we always have instant access to the maximum.</p>",
    optimalApproach: "<p>Use a <strong>max-heap</strong> (in Java: PriorityQueue with reverse order comparator). Each iteration: extract the two largest stones. If equal, discard both. If different, push back the difference. Stop when 0 or 1 stone remains.</p><ul><li><strong>Time:</strong> O(n log n) — n/2 rounds, each with O(log n) heap operations.</li><li><strong>Space:</strong> O(n).</li></ul>",
    javaCode: "class Solution {\n    public int lastStoneWeight(int[] stones) {\n        PriorityQueue<Integer> pq = new PriorityQueue<>(Collections.reverseOrder());\n        for (int s : stones) pq.offer(s);\n        while (pq.size() > 1) {\n            int a = pq.poll(), b = pq.poll();\n            if (a != b) pq.offer(a - b);\n        }\n        return pq.isEmpty() ? 0 : pq.poll();\n    }\n}",
    dryRun: "<p><strong>Input:</strong> stones=[2,7,4,1,8,1]</p><ol><li>Max-heap: [8,7,4,2,1,1]. Poll 8 and 7: 8-7=1, push 1. Heap: [4,2,1,1,1].</li><li>Poll 4 and 2: 4-2=2, push 2. Heap: [2,1,1,1].</li><li>Poll 2 and 1: 2-1=1, push 1. Heap: [1,1,1].</li><li>Poll 1 and 1: equal, both destroyed. Heap: [1].</li><li>Size=1, stop. Return <strong>1</strong>.</li></ol><p><strong>Output:</strong> 1 ✓</p>",
    interviewTips: "<ul><li>Java doesn't have a built-in max-heap — use <code>Collections.reverseOrder()</code> or <code>(a, b) -&gt; b - a</code> comparator.</li><li>After the while loop, the heap has 0 or 1 elements — handle both cases.</li><li>The problem guarantees positive weights, so <code>a - b</code> is always positive when <code>a != b</code> (since we poll largest first, a &gt;= b).</li></ul>",
    importantPoints: "<ul><li>Use <code>Collections.reverseOrder()</code> to make Java's PriorityQueue a max-heap.</li><li>The first polled element <code>a</code> is always >= the second polled element <code>b</code>.</li><li>Only push back <code>a - b</code> if they're different; if equal, both vanish.</li><li>Return 0 if the heap is empty at the end (all stones destroyed each other).</li></ul>"
  },
  {
    id: 66,
    name: "K Closest Points to Origin",
    category: "Heap / Priority Queue",
    categoryId: "heap",
    difficulty: "Medium",
    explanation: "<p><strong>K Closest Points to Origin</strong>: given an array of points on a 2D plane, return the k points closest to the origin (0,0). Distance is Euclidean: sqrt(x²+y²). The order of returned points doesn't matter.</p><p>We don't need the actual distance values, just which k points have the smallest distances — so we can compare x²+y² directly (avoiding the sqrt computation).</p><p><strong>Example:</strong> points=[[1,3],[-2,2]], k=1 → [[-2,2]] (distance sqrt(8) &lt; sqrt(10)).</p>",
    bruteForce: "<p>Compute the squared distance for all points, sort the array by distance, and return the first k elements. Time: O(n log n) for sorting. Space: O(n).</p>",
    bruteForceInefficiency: "<p>Sorting all n points when we only need the k closest is wasteful for large n with small k. A max-heap of size k processes each point in O(log k), giving O(n log k) total — significantly better when k &lt;&lt; n.</p>",
    optimalApproach: "<p>Use a <strong>max-heap of size k</strong> ordered by distance. For each point, add it to the heap. If heap size exceeds k, remove the farthest point (max distance). The remaining k points in the heap are the k closest.</p><ul><li><strong>Time:</strong> O(n log k) — process each of n points with O(log k) heap operations.</li><li><strong>Space:</strong> O(k).</li></ul>",
    javaCode: "class Solution {\n    public int[][] kClosest(int[][] points, int k) {\n        PriorityQueue<int[]> pq = new PriorityQueue<>((a, b) ->\n            (b[0]*b[0]+b[1]*b[1]) - (a[0]*a[0]+a[1]*a[1]));\n        for (int[] p : points) {\n            pq.offer(p);\n            if (pq.size() > k) pq.poll();\n        }\n        return pq.toArray(new int[k][]);\n    }\n}",
    dryRun: "<p><strong>Input:</strong> points=[[3,3],[5,-1],[-2,4]], k=2</p><p>Distances squared: [18, 26, 20]</p><ol><li>Add [3,3] (dist=18): heap=[[3,3]], size=1.</li><li>Add [5,-1] (dist=26): heap=[[5,-1],[3,3]], size=2.</li><li>Add [-2,4] (dist=20): heap=[[5,-1],[3,3],[-2,4]], size=3 &gt; k=2. Poll max (dist=26=[5,-1]). heap=[[−2,4],[3,3]].</li><li>Result: [[3,3],[-2,4]] ✓</li></ol>",
    interviewTips: "<ul><li>Emphasize: we compare x²+y² to avoid computing sqrt (no floating point needed).</li><li>Max-heap of size k: keeps the k smallest distances by evicting the current maximum when size exceeds k.</li><li>Alternative: QuickSelect achieves O(n) average time but O(n²) worst case — heap is more predictable.</li><li>The comparator <code>(b[0]*b[0]+b[1]*b[1]) - (a[0]*a[0]+a[1]*a[1])</code> orders by DESCENDING distance (max-heap).</li></ul>",
    importantPoints: "<ul><li>The comparator for the max-heap: put the LARGER distance first → <code>b's dist - a's dist</code> (descending).</li><li><code>pq.toArray(new int[k][])</code> converts the heap directly to a 2D array result.</li><li>No need to sort the final result — problem states order doesn't matter.</li><li>Integer overflow: x²+y² can overflow int if x,y are large — use long if values are large.</li></ul>"
  },
  {
    id: 67,
    name: "Kth Largest Element in an Array",
    category: "Heap / Priority Queue",
    categoryId: "heap",
    difficulty: "Medium",
    explanation: "<p><strong>Kth Largest Element in an Array</strong>: find the kth largest element in an unsorted array. Note: kth largest means the kth element in sorted descending order (not the kth unique element).</p><p><strong>Example:</strong> nums=[3,2,1,5,6,4], k=2 → 5 (sorted desc: [6,5,4,3,2,1], 2nd = 5). nums=[3,2,3,1,2,4,5,5,6], k=4 → 4.</p>",
    bruteForce: "<p>Sort the array in descending order and return the element at index k-1. Time: O(n log n) for sorting, Space: O(1) or O(n) depending on sort algorithm.</p>",
    bruteForceInefficiency: "<p>Sorting the entire array is O(n log n) but we only need one element. A min-heap of size k finds the answer in O(n log k). QuickSelect finds the answer in O(n) average. For interviews, the heap solution is clean and commonly expected.</p>",
    optimalApproach: "<p>Use a <strong>min-heap of size k</strong>. Process each number: push to heap, if size exceeds k, poll the minimum. After processing all numbers, the kth largest is at the heap's top (it's the minimum of the k largest elements).</p><ul><li><strong>Time:</strong> O(n log k).</li><li><strong>Space:</strong> O(k).</li></ul>",
    javaCode: "class Solution {\n    public int findKthLargest(int[] nums, int k) {\n        PriorityQueue<Integer> pq = new PriorityQueue<>();\n        for (int n : nums) {\n            pq.offer(n);\n            if (pq.size() > k) pq.poll();\n        }\n        return pq.peek();\n    }\n}",
    dryRun: "<p><strong>Input:</strong> nums=[3,2,1,5,6,4], k=2</p><ol><li>Add 3: heap=[3]</li><li>Add 2: heap=[2,3]</li><li>Add 1: heap=[1,3,2], size=3&gt;2 → poll 1 → heap=[2,3]</li><li>Add 5: heap=[2,3,5], size=3&gt;2 → poll 2 → heap=[3,5]</li><li>Add 6: heap=[3,5,6], size=3&gt;2 → poll 3 → heap=[5,6]</li><li>Add 4: heap=[4,6,5], size=3&gt;2 → poll 4 → heap=[5,6]</li><li>peek() = <strong>5</strong></li></ol><p><strong>Output:</strong> 5 ✓</p>",
    interviewTips: "<ul><li>Mention QuickSelect as an O(n) average alternative — it shows depth of knowledge.</li><li>The heap approach is O(n log k) which is actually better than sorting when k is small.</li><li>For very large n with small k, the heap's O(n log k) is nearly O(n) — very efficient.</li><li>LeetCode follow-up specifically asks: can you solve in O(n) average? Answer: QuickSelect.</li></ul>",
    importantPoints: "<ul><li>Min-heap of size k: the top is the kth largest because k-1 elements are larger and in the heap above it.</li><li>Java's default PriorityQueue is a min-heap — no comparator needed here.</li><li>Contrast with Kth Largest in Stream: same structure, but here we process all elements at once.</li><li>QuickSelect alternative: partition around a pivot (like QuickSort) and recurse only into the relevant partition.</li></ul>"
  },
  {
    id: 68,
    name: "Task Scheduler",
    category: "Heap / Priority Queue",
    categoryId: "heap",
    difficulty: "Medium",
    explanation: "<p><strong>Task Scheduler</strong>: given a list of CPU tasks (each identified by a letter A-Z) and a cooldown interval n, find the minimum number of CPU intervals needed to complete all tasks. Between two instances of the same task, there must be at least n intervals (can be idle or other tasks).</p><p><strong>Key insight:</strong> the most frequent task dictates the minimum time. If the most frequent task appears <code>maxFreq</code> times, we need at least <code>(maxFreq-1) * (n+1)</code> intervals for it, plus the number of tasks that also have <code>maxFreq</code> frequency.</p><p><strong>Example:</strong> tasks=['A','A','A','B','B','B'], n=2 → 8 (A_B_AB or ABABAB... → 8 or 6 → max(6,8)=8).</p>",
    bruteForce: "<p>Simulate the scheduling: use a max-heap of (frequency, task), a cooldown queue, and a timer. Each tick, if the heap is non-empty and the cooldown allows, execute the highest-frequency task. This simulation is O(total intervals) which can be O(n * tasks.length).</p>",
    bruteForceInefficiency: "<p>Full simulation runs for the actual number of intervals and requires careful queue management. The mathematical formula gives the answer directly in O(26) time by observing the structure of the optimal schedule.</p>",
    optimalApproach: "<p>Mathematical formula: find <code>maxFreq</code> (highest task frequency) and <code>maxCount</code> (number of tasks with that frequency). The minimum intervals = <code>max(tasks.length, (maxFreq-1) * (n+1) + maxCount)</code>.</p><p>Why: the <code>maxFreq</code> tasks create <code>maxFreq-1</code> 'chunks' of size <code>n+1</code>. The final chunk needs <code>maxCount</code> slots. But if we have more tasks than gaps, we can always interleave them without idle time.</p><ul><li><strong>Time:</strong> O(n) to count frequencies.</li><li><strong>Space:</strong> O(1) (26 letter buckets).</li></ul>",
    javaCode: "class Solution {\n    public int leastInterval(char[] tasks, int n) {\n        int[] freq = new int[26];\n        for (char t : tasks) freq[t-'A']++;\n        int maxFreq = 0;\n        for (int f : freq) maxFreq = Math.max(maxFreq, f);\n        int maxCount = 0;\n        for (int f : freq) if (f == maxFreq) maxCount++;\n        return Math.max(tasks.length, (maxFreq - 1) * (n + 1) + maxCount);\n    }\n}",
    dryRun: "<p><strong>Input:</strong> tasks=['A','A','A','B','B','B'], n=2</p><ol><li>freq: A=3, B=3. maxFreq=3, maxCount=2 (both A and B appear 3 times).</li><li>Formula: max(tasks.length=6, (3-1)*(2+1)+2) = max(6, 2*3+2) = max(6, 8) = <strong>8</strong>.</li></ol><p>Optimal schedule: A B _ A B _ A B = 8 intervals ✓</p><p><strong>tasks=['A','A','A','B','B','B','C','C','C'], n=2:</strong> maxFreq=3, maxCount=3. max(9, 2*3+3)=max(9,9)=9. No idle needed ✓</p>",
    interviewTips: "<ul><li>The formula is non-obvious — walk through the reasoning: each 'frame' of size (n+1) fits the most frequent task plus n others or idles.</li><li>The <code>max(tasks.length, formula)</code> handles the case where we have enough diverse tasks to fill all cooldown gaps without any idle time.</li><li>The greedy simulation approach (using max-heap) is also valid and more general — mention it as an alternative.</li></ul>",
    importantPoints: "<ul><li>The formula: <code>(maxFreq - 1) * (n + 1) + maxCount</code> counts the minimum slots needed for the 'frame' structure.</li><li>Taking max with <code>tasks.length</code> handles cases where tasks are diverse enough to avoid any idle.</li><li>Multiple tasks can share <code>maxFreq</code> — they each need a slot in the last partial frame (<code>maxCount</code>).</li><li>When n=0, the answer is always <code>tasks.length</code> (no cooldown needed).</li></ul>"
  },
  {
    id: 69,
    name: "Design Twitter",
    category: "Heap / Priority Queue",
    categoryId: "heap",
    difficulty: "Medium",
    explanation: "<p><strong>Design Twitter</strong>: implement a simplified version of Twitter with these operations:</p><ul><li><code>postTweet(userId, tweetId)</code>: post a new tweet.</li><li><code>getNewsFeed(userId)</code>: retrieve the 10 most recent tweet IDs from the user and users they follow.</li><li><code>follow(followerId, followeeId)</code>: follow a user.</li><li><code>unfollow(followerId, followeeId)</code>: unfollow a user.</li></ul><p>The news feed must be sorted by recency (most recent first) across all relevant users' tweets.</p>",
    bruteForce: "<p>For <code>getNewsFeed</code>, collect ALL tweets from the user and followed users into a single list, sort by timestamp, and return the top 10. This is O(T log T) where T = total tweets from all relevant users — potentially very slow for prolific users.</p>",
    bruteForceInefficiency: "<p>Sorting all tweets every time the news feed is requested is expensive. A max-heap can efficiently merge the most recent tweets from multiple users without sorting all of them — we only need the top 10, so we stop after extracting 10 elements.</p>",
    optimalApproach: "<p>Store tweets per user as a list of <code>[timestamp, tweetId]</code>. For <code>getNewsFeed</code>: collect all tweets from the user + followed users into a max-heap ordered by timestamp. Extract up to 10 elements.</p><ul><li>Use a global incrementing <code>time</code> counter for ordering.</li><li>Store following relationships in a <code>Map&lt;Integer, Set&lt;Integer&gt;&gt;</code>.</li></ul><ul><li><strong>postTweet:</strong> O(1).</li><li><strong>getNewsFeed:</strong> O(T log T) where T = tweets from relevant users (but stops at 10 extractions).</li><li><strong>follow/unfollow:</strong> O(1).</li></ul>",
    javaCode: "class Twitter {\n    private Map<Integer, List<int[]>> tweets = new HashMap<>();\n    private Map<Integer, Set<Integer>> following = new HashMap<>();\n    private int time = 0;\n    public void postTweet(int userId, int tweetId) {\n        tweets.computeIfAbsent(userId, k -> new ArrayList<>()).add(new int[]{time++, tweetId});\n    }\n    public List<Integer> getNewsFeed(int userId) {\n        PriorityQueue<int[]> pq = new PriorityQueue<>((a,b) -> b[0]-a[0]);\n        Set<Integer> users = new HashSet<>();\n        users.add(userId);\n        if (following.containsKey(userId)) users.addAll(following.get(userId));\n        for (int u : users) if (tweets.containsKey(u)) pq.addAll(tweets.get(u));\n        List<Integer> res = new ArrayList<>();\n        while (!pq.isEmpty() && res.size() < 10) res.add(pq.poll()[1]);\n        return res;\n    }\n    public void follow(int f, int ee) { following.computeIfAbsent(f, k -> new HashSet<>()).add(ee); }\n    public void unfollow(int f, int ee) { if (following.containsKey(f)) following.get(f).remove(ee); }\n}",
    dryRun: "<p><strong>Operations:</strong></p><ol><li>postTweet(1, 5): tweets={1:[[0,5]]}. time=1.</li><li>postTweet(1, 3): tweets={1:[[0,5],[1,3]]}. time=2.</li><li>postTweet(2, 7): tweets={2:[[2,7]]}. time=3.</li><li>follow(1, 2): following={1:{2}}.</li><li>getNewsFeed(1): users={1,2}. Collect all tweets from user 1 and 2. heap=[[0,5],[1,3],[2,7]]. Extract top 10: poll [2,7]→tweetId=7, poll [1,3]→3, poll [0,5]→5. Return [7,3,5].</li></ol>",
    interviewTips: "<ul><li>A global timestamp ensures tweets from different users can be compared chronologically.</li><li>The heap approach naturally merges sorted tweet lists from multiple users.</li><li>Advanced: store only the most recent 10 tweets per user to bound the heap size — but this is an optimization not required for correctness.</li><li>Follow/unfollow uses a Set to ensure idempotent follow and O(1) membership operations.</li></ul>",
    importantPoints: "<ul><li><code>computeIfAbsent</code> lazily initializes the tweet list/following set for new users.</li><li>Use a HashSet for following to allow O(1) follow/unfollow/membership checks.</li><li>The heap comparator <code>(a,b) -&gt; b[0]-a[0]</code> creates a max-heap by timestamp (most recent first).</li><li>A user sees their own tweets in their news feed — include <code>userId</code> in the <code>users</code> set.</li></ul>"
  },
  {
    id: 70,
    name: "Find Median from Data Stream",
    category: "Heap / Priority Queue",
    categoryId: "heap",
    difficulty: "Hard",
    explanation: "<p><strong>Find Median from Data Stream</strong>: design a data structure that supports adding numbers from a data stream and finding the median at any point.</p><p>The median is the middle value of a sorted sequence. For even-length sequences, it's the average of the two middle values.</p><p><strong>Key insight:</strong> maintain two heaps — a max-heap for the lower half and a min-heap for the upper half. The median is either the top of one heap or the average of both tops.</p><p><strong>Example:</strong> addNum(1), addNum(2), findMedian()→1.5, addNum(3), findMedian()→2.0.</p>",
    bruteForce: "<p>Store all numbers in a list. For each <code>findMedian</code> call, sort the list and return the middle element(s). Sort is O(n log n) per query — terrible for frequent median queries on growing streams.</p>",
    bruteForceInefficiency: "<p>Re-sorting the entire list on every findMedian call is O(n log n) each time. We only need the median, which means we only need the 'boundary' between the lower and upper halves. Two heaps maintain this boundary: add in O(log n), find median in O(1).</p>",
    optimalApproach: "<p>Maintain two heaps:</p><ul><li><strong>small</strong> (max-heap): stores the lower half of numbers. Top = largest of lower half.</li><li><strong>large</strong> (min-heap): stores the upper half of numbers. Top = smallest of upper half.</li></ul><p>Invariant: <code>small.size() == large.size()</code> or <code>small.size() == large.size() + 1</code>. Median = small.peek() if sizes differ, else average of both tops.</p><p>On <code>addNum</code>: always add to <code>small</code> first. If <code>small.peek() &gt; large.peek()</code>, rebalance. Then balance sizes.</p><ul><li><strong>addNum:</strong> O(log n).</li><li><strong>findMedian:</strong> O(1).</li></ul>",
    javaCode: "class MedianFinder {\n    private PriorityQueue<Integer> small = new PriorityQueue<>(Collections.reverseOrder()); // max-heap\n    private PriorityQueue<Integer> large = new PriorityQueue<>(); // min-heap\n    public void addNum(int num) {\n        small.offer(num);\n        if (!large.isEmpty() && small.peek() > large.peek()) large.offer(small.poll());\n        if (small.size() > large.size() + 1) large.offer(small.poll());\n        if (large.size() > small.size()) small.offer(large.poll());\n    }\n    public double findMedian() {\n        if (small.size() == large.size()) return (small.peek() + large.peek()) / 2.0;\n        return small.peek();\n    }\n}",
    dryRun: "<p><strong>addNum(1), addNum(2), findMedian(), addNum(3), findMedian()</strong></p><ol><li>addNum(1): small=[1], large=[]. No rebalance needed. small.size=1, large.size=0 ✓</li><li>addNum(2): small=[2,1] (offer 2). small.peek()=2 &gt; large is empty? large is empty so skip rebalance check. small.size=2 &gt; large.size+1=1 → move to large: large.offer(small.poll()=2) → small=[1], large=[2]. ✓</li><li>findMedian(): small.size=1==large.size=1 → (1+2)/2.0=<strong>1.5</strong> ✓</li><li>addNum(3): small=[3,1] (offer 3). small.peek()=3 &gt; large.peek()=2 → large.offer(small.poll()=3) → small=[1], large=[2,3]. large.size=2 &gt; small.size=1 → small.offer(large.poll()=2) → small=[2,1], large=[3]. ✓</li><li>findMedian(): small.size=2 != large.size=1 → return small.peek()=<strong>2.0</strong> ✓</li></ol>",
    interviewTips: "<ul><li>The two-heap approach is the classic optimal solution — know the invariants: small is max-heap, large is min-heap, sizes differ by at most 1.</li><li>Always add to small first to ensure the element goes to the right heap after rebalancing.</li><li>The rebalancing steps handle three cases: value ordering between heaps, then size balance.</li><li>Follow-up: what if 99% of numbers are from [0,100]? Use bucket counting for O(1) median with specific constraints.</li></ul>",
    importantPoints: "<ul><li>small = max-heap (lower half): largest of lower half is at top.</li><li>large = min-heap (upper half): smallest of upper half is at top.</li><li>The median is always accessible in O(1) via heap tops.</li><li>Use <code>/ 2.0</code> (not <code>/ 2</code>) for the average to avoid integer division truncation.</li><li>The invariant <code>small.size() &gt;= large.size()</code> (small holds the extra element for odd counts) ensures <code>small.peek()</code> is the median when sizes differ.</li></ul>"
  }
];
