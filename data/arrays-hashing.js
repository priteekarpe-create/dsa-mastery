window.ARRAYS_HASHING_DATA = [
  {
    id: 1,
    name: "Contains Duplicate",
    category: "Arrays & Hashing",
    categoryId: "arrays-hashing",
    difficulty: "Easy",
    explanation: "<p><strong>Problem:</strong> Given an integer array <code>nums</code>, return <code>true</code> if any value appears <strong>at least twice</strong> in the array, and return <code>false</code> if every element is distinct.</p><p><strong>Example:</strong></p><ul><li>Input: <code>nums = [1,2,3,1]</code> → Output: <code>true</code> (1 appears twice)</li><li>Input: <code>nums = [1,2,3,4]</code> → Output: <code>false</code> (all distinct)</li><li>Input: <code>nums = [1,1,1,3,3,4,3,2,4,2]</code> → Output: <code>true</code></li></ul><p><strong>Constraints:</strong> <code>1 ≤ nums.length ≤ 10⁵</code>, <code>-10⁹ ≤ nums[i] ≤ 10⁹</code></p><p>This is a classic <strong>hashing</strong> problem. The key insight is: if we can quickly check whether we have seen a number before, we can detect duplicates in a single pass.</p>",
    bruteForce: "<p><strong>Brute Force — Nested Loops O(n²)</strong></p><p>Compare every pair of elements:</p><pre><code>for i from 0 to n-1:\n  for j from i+1 to n-1:\n    if nums[i] == nums[j]: return true\nreturn false</code></pre><p><strong>Time:</strong> O(n²) — for n=100,000 that's 10 billion operations.</p><p><strong>Space:</strong> O(1) — no extra memory used.</p>",
    bruteForceInefficiency: "<p><strong>Why is the brute force slow?</strong></p><ul><li>For every element at index <code>i</code>, we re-scan all elements after it — this is <strong>redundant work</strong>.</li><li>If the array has 100,000 elements, the inner loop runs ~5 billion times total.</li><li>We are not <em>remembering</em> what we've already seen — we throw that information away after each outer iteration.</li></ul><p><strong>The fix:</strong> Use a data structure that <em>remembers</em> previously seen values and answers \"have I seen this?\" in <strong>O(1)</strong> time → a <strong>HashSet</strong>.</p>",
    optimalApproach: "<p><strong>Optimal — HashSet O(n) time, O(n) space</strong></p><p><strong>Pattern:</strong> \"Have I seen this before?\" → HashSet</p><ol><li>Create an empty <code>HashSet&lt;Integer&gt;</code>.</li><li>For each number in <code>nums</code>:<ul><li>Try to <strong>add</strong> it to the set.</li><li><code>Set.add()</code> returns <code>false</code> if the element already exists → duplicate found! Return <code>true</code>.</li></ul></li><li>If the loop finishes without finding a duplicate, return <code>false</code>.</li></ol><p><strong>Time:</strong> O(n) — single pass, each HashSet operation is O(1) average.</p><p><strong>Space:</strong> O(n) — the set can hold at most n elements.</p><p><em>Note: Alternatively, sort the array in O(n log n) and check adjacent elements — no extra space but slower.</em></p>",
    javaCode: "import java.util.HashSet; // import HashSet from java.util\nimport java.util.Set;     // import Set interface\n\nclass Solution {\n    public boolean containsDuplicate(int[] nums) {\n        // create a HashSet to track numbers we have seen\n        Set<Integer> seen = new HashSet<>();\n        \n        // iterate through every number in the array\n        for (int num : nums) {\n            // Set.add() returns false if the element is already present\n            // if it returns false, we found a duplicate\n            if (!seen.add(num)) {\n                return true; // duplicate found, return true immediately\n            }\n        }\n        \n        // if we finish the loop without finding a duplicate, return false\n        return false;\n    }\n}",
    dryRun: "<p><strong>Input:</strong> <code>nums = [1, 2, 3, 1]</code></p><table border='1' cellpadding='6' style='border-collapse:collapse'><tr><th>Step</th><th>num</th><th>seen.add(num) returns</th><th>seen set</th><th>action</th></tr><tr><td>1</td><td>1</td><td>true (new)</td><td>{1}</td><td>continue</td></tr><tr><td>2</td><td>2</td><td>true (new)</td><td>{1,2}</td><td>continue</td></tr><tr><td>3</td><td>3</td><td>true (new)</td><td>{1,2,3}</td><td>continue</td></tr><tr><td>4</td><td>1</td><td><strong>false (exists!)</strong></td><td>{1,2,3}</td><td><strong>return true</strong></td></tr></table><p><strong>Output:</strong> <code>true</code> ✓</p><p><strong>Input:</strong> <code>nums = [1, 2, 3, 4]</code> — all 4 adds return true, loop ends → return <code>false</code></p>",
    interviewTips: "<p><strong>Interview Script:</strong></p><ol><li><strong>Clarify:</strong> \"Can the array be empty? Should I return false for empty/single-element arrays?\" (yes, no duplicates possible)</li><li><strong>Brute force first:</strong> \"A naive approach is nested loops — O(n²). But we can do better.\"</li><li><strong>Optimal insight:</strong> \"If I use a HashSet, I can check for duplicates in O(1) per element, giving O(n) overall.\"</li><li><strong>Code it:</strong> Write the HashSet solution and walk through the example.</li><li><strong>Complexity:</strong> \"Time O(n), Space O(n). If space is a concern, sorting in O(n log n) and checking adjacent elements uses O(1) extra space.\"</li></ol><p><strong>Follow-up questions interviewers ask:</strong></p><ul><li>\"Can you solve it with O(1) space?\" → Sort, then check adjacent pairs.</li><li>\"What if the constraint said values are between 1 and n?\" → Could use the array itself as a visited marker.</li></ul>",
    importantPoints: "<ul><li><strong>HashSet.add() returns a boolean</strong> — <code>false</code> means the element was already present. This is a neat trick to check and insert in one step.</li><li><strong>Time vs Space tradeoff:</strong> HashSet = O(n) time + O(n) space; sorting = O(n log n) time + O(1) space.</li><li><strong>Edge cases:</strong> Single element array → always false. Empty array → always false.</li><li><strong>This pattern</strong> — \"use a set to detect duplicates / previously seen elements\" — appears in many other problems like Two Sum, Longest Consecutive Sequence, etc.</li><li>Java's <code>HashSet</code> uses a <code>HashMap</code> internally, so lookups are O(1) average, O(n) worst case due to hash collisions.</li></ul>"
  },
  {
    id: 2,
    name: "Valid Anagram",
    category: "Arrays & Hashing",
    categoryId: "arrays-hashing",
    difficulty: "Easy",
    explanation: "<p><strong>Problem:</strong> Given two strings <code>s</code> and <code>t</code>, return <code>true</code> if <code>t</code> is an <strong>anagram</strong> of <code>s</code>, and <code>false</code> otherwise.</p><p>An <strong>anagram</strong> is a word formed by rearranging the letters of another word, using each letter exactly the same number of times.</p><p><strong>Examples:</strong></p><ul><li><code>s = \"anagram\", t = \"nagaram\"</code> → <code>true</code></li><li><code>s = \"rat\", t = \"car\"</code> → <code>false</code> (different letters)</li><li><code>s = \"ab\", t = \"a\"</code> → <code>false</code> (different lengths)</li></ul><p><strong>Constraints:</strong> <code>1 ≤ s.length, t.length ≤ 5 × 10⁴</code>. Strings consist of lowercase English letters.</p><p><strong>Key insight:</strong> Two strings are anagrams if and only if they contain the <em>exact same characters with the same frequencies</em>.</p>",
    bruteForce: "<p><strong>Brute Force — Sort Both Strings O(n log n)</strong></p><p>If two strings are anagrams, sorting them produces identical strings:</p><pre><code>sort(s) and sort(t)\nreturn sorted_s == sorted_t</code></pre><p>Example: <code>\"anagram\"</code> → sorted: <code>\"aaagmnr\"</code> and <code>\"nagaram\"</code> → sorted: <code>\"aaagmnr\"</code> → equal → true.</p><p><strong>Time:</strong> O(n log n) for sorting.</p><p><strong>Space:</strong> O(n) for the sorted character arrays.</p>",
    bruteForceInefficiency: "<p><strong>Why sorting is not optimal:</strong></p><ul><li>Sorting takes O(n log n) — we can actually do this in O(n) with counting.</li><li>Sorting involves rearranging data which is more work than just counting.</li><li>When n = 50,000, sorting does ~800,000 operations; counting does exactly 50,000.</li></ul><p><strong>The better idea:</strong> Count the frequency of each character. Two strings are anagrams iff their frequency arrays are identical. Since we only have 26 lowercase letters, this is O(n) time and O(1) extra space (array of size 26 is constant).</p>",
    optimalApproach: "<p><strong>Optimal — Frequency Count Array O(n) time, O(1) space</strong></p><p><strong>Pattern:</strong> Character frequency comparison</p><ol><li>If <code>s.length() != t.length()</code>, immediately return <code>false</code>.</li><li>Create <code>int[] count = new int[26]</code> (indices 0–25 represent 'a'–'z').</li><li>Loop through <code>s</code>: increment <code>count[c - 'a']</code> for each char <code>c</code>.</li><li>Loop through <code>t</code>: decrement <code>count[c - 'a']</code> for each char <code>c</code>.</li><li>If any count is non-zero, the strings differ in that character → return <code>false</code>. Otherwise return <code>true</code>.</li></ol><p><strong>Time:</strong> O(n) — two passes through strings.</p><p><strong>Space:</strong> O(1) — array of 26 integers is constant space.</p><p><em>Note: For Unicode characters, use a HashMap instead of a fixed-size array.</em></p>",
    javaCode: "class Solution {\n    public boolean isAnagram(String s, String t) {\n        // if lengths differ, cannot be anagrams\n        if (s.length() != t.length()) return false;\n        \n        // create a frequency counter for 26 lowercase letters\n        // index 0 = 'a', index 1 = 'b', ..., index 25 = 'z'\n        int[] count = new int[26];\n        \n        // increment count for each character in s\n        for (char c : s.toCharArray()) {\n            count[c - 'a']++; // e.g., 'a'-'a'=0, 'b'-'a'=1\n        }\n        \n        // decrement count for each character in t\n        for (char c : t.toCharArray()) {\n            count[c - 'a']--; // should cancel out if same frequency\n        }\n        \n        // if any count is non-zero, frequencies differ -> not anagram\n        for (int c : count) {\n            if (c != 0) return false;\n        }\n        \n        return true; // all counts balanced, it's an anagram\n    }\n}",
    dryRun: "<p><strong>Input:</strong> <code>s = \"anagram\", t = \"nagaram\"</code></p><p><strong>Step 1:</strong> Lengths equal (7 == 7), continue.</p><p><strong>Step 2:</strong> Process <code>s = \"anagram\"</code>:</p><table border='1' cellpadding='6' style='border-collapse:collapse'><tr><th>char</th><th>index</th><th>count change</th></tr><tr><td>a</td><td>0</td><td>count[0]=1</td></tr><tr><td>n</td><td>13</td><td>count[13]=1</td></tr><tr><td>a</td><td>0</td><td>count[0]=2</td></tr><tr><td>g</td><td>6</td><td>count[6]=1</td></tr><tr><td>r</td><td>17</td><td>count[17]=1</td></tr><tr><td>a</td><td>0</td><td>count[0]=3</td></tr><tr><td>m</td><td>12</td><td>count[12]=1</td></tr></table><p><strong>Step 3:</strong> Process <code>t = \"nagaram\"</code> — decrements exactly cancel all counts back to 0.</p><p><strong>Step 4:</strong> All 26 counts are 0 → return <code>true</code> ✓</p>",
    interviewTips: "<p><strong>Interview Script:</strong></p><ol><li><strong>Clarify:</strong> \"Are characters only lowercase English letters, or could they be Unicode?\" This determines if we use int[26] or a HashMap.</li><li><strong>Quick check:</strong> \"First I'll check if lengths are equal — if not, they can't be anagrams.\"</li><li><strong>Approach:</strong> \"I'll count character frequencies — increment for s, decrement for t, then check all counts are zero.\"</li><li><strong>Complexity:</strong> \"O(n) time, O(1) space since the count array is always size 26.\"</li></ol><p><strong>Follow-up:</strong> \"What if the inputs contain Unicode characters?\"</p><ul><li>Use <code>HashMap&lt;Character, Integer&gt;</code> instead of int[26].</li><li>Same algorithm, just replace array indexing with map.getOrDefault().</li></ul>",
    importantPoints: "<ul><li><strong>Length check first</strong> — always do this as an early return; it's O(1) and eliminates a whole class of inputs.</li><li><strong>int[26] vs HashMap:</strong> For lowercase English only → int[26] (O(1) space). For Unicode → HashMap (O(k) where k = unique chars).</li><li><strong>The trick:</strong> Increment for one string, decrement for the other. Net zero = anagram.</li><li><strong>Sorting alternative:</strong> Sort both strings and compare — valid but O(n log n). Mention it as a simpler-to-remember fallback.</li><li><strong>Related problems:</strong> Group Anagrams (use sorted string as HashMap key), Permutation in String (sliding window + frequency).</li></ul>"
  },
  {
    id: 3,
    name: "Two Sum",
    category: "Arrays & Hashing",
    categoryId: "arrays-hashing",
    difficulty: "Easy",
    explanation: "<p><strong>Problem:</strong> Given an array of integers <code>nums</code> and an integer <code>target</code>, return the <strong>indices</strong> of the two numbers that add up to <code>target</code>.</p><p>You may assume exactly one solution exists. You may not use the same element twice.</p><p><strong>Examples:</strong></p><ul><li><code>nums = [2,7,11,15], target = 9</code> → <code>[0,1]</code> (2+7=9)</li><li><code>nums = [3,2,4], target = 6</code> → <code>[1,2]</code> (2+4=6)</li><li><code>nums = [3,3], target = 6</code> → <code>[0,1]</code></li></ul><p><strong>Key insight:</strong> For each element <code>x</code>, we need to find <code>target - x</code> (the <em>complement</em>). If we store seen elements in a HashMap (value → index), we can look up the complement in O(1).</p>",
    bruteForce: "<p><strong>Brute Force — Nested Loops O(n²)</strong></p><p>Try every pair of indices:</p><pre><code>for i from 0 to n-1:\n  for j from i+1 to n-1:\n    if nums[i] + nums[j] == target:\n      return [i, j]</code></pre><p><strong>Time:</strong> O(n²) — for n=10,000 that's 50 million pair checks.</p><p><strong>Space:</strong> O(1) — no extra memory.</p>",
    bruteForceInefficiency: "<p><strong>Why nested loops are slow:</strong></p><ul><li>For each element, we scan the <em>entire remaining array</em> looking for its complement.</li><li>We're repeatedly asking \"is target-nums[i] somewhere in the array?\" and answering it by linear scan each time.</li><li>If we could answer that question in O(1), we'd reduce to O(n) total.</li></ul><p><strong>The fix:</strong> Use a <strong>HashMap</strong> that maps each value to its index. Then for each element, check if its complement is already in the map — O(1) lookup.</p>",
    optimalApproach: "<p><strong>Optimal — HashMap O(n) time, O(n) space</strong></p><p><strong>Pattern:</strong> \"Find complement\" → HashMap (value → index)</p><ol><li>Create <code>Map&lt;Integer, Integer&gt; map</code> (maps value → index).</li><li>For each index <code>i</code> with value <code>nums[i]</code>:<ul><li>Compute <code>complement = target - nums[i]</code>.</li><li>If <code>map.containsKey(complement)</code> → found the pair! Return <code>{map.get(complement), i}</code>.</li><li>Otherwise, store <code>map.put(nums[i], i)</code> for future lookups.</li></ul></li></ol><p><strong>Why store <em>then</em> check?</strong> We check first so we don't use the same element twice (handles <code>[3,3], target=6</code> correctly — when we reach the second 3, the first 3 is already in the map).</p><p><strong>Time:</strong> O(n) — one pass. <strong>Space:</strong> O(n) — map stores up to n entries.</p>",
    javaCode: "import java.util.HashMap; // import HashMap\nimport java.util.Map;     // import Map interface\n\nclass Solution {\n    public int[] twoSum(int[] nums, int target) {\n        // map stores: value -> index (for O(1) complement lookup)\n        Map<Integer, Integer> map = new HashMap<>();\n        \n        // iterate through the array with index\n        for (int i = 0; i < nums.length; i++) {\n            // calculate what value we need to complete the sum\n            int complement = target - nums[i];\n            \n            // check if complement was seen in a previous iteration\n            if (map.containsKey(complement)) {\n                // found it! return both indices\n                return new int[]{map.get(complement), i};\n            }\n            \n            // store current value -> index for future lookups\n            // (we store AFTER the check to avoid using same element twice)\n            map.put(nums[i], i);\n        }\n        \n        // problem guarantees a solution exists, but return empty if not found\n        return new int[]{};\n    }\n}",
    dryRun: "<p><strong>Input:</strong> <code>nums = [2, 7, 11, 15], target = 9</code></p><table border='1' cellpadding='6' style='border-collapse:collapse'><tr><th>i</th><th>nums[i]</th><th>complement</th><th>map has complement?</th><th>map after step</th></tr><tr><td>0</td><td>2</td><td>9-2=7</td><td>No</td><td>{2:0}</td></tr><tr><td>1</td><td>7</td><td>9-7=2</td><td><strong>Yes! index 0</strong></td><td>—</td></tr></table><p><strong>Return:</strong> <code>[0, 1]</code> ✓</p><p><strong>Tricky case:</strong> <code>nums = [3, 3], target = 6</code></p><table border='1' cellpadding='6' style='border-collapse:collapse'><tr><th>i</th><th>nums[i]</th><th>complement</th><th>map has complement?</th><th>map after</th></tr><tr><td>0</td><td>3</td><td>3</td><td>No (map empty)</td><td>{3:0}</td></tr><tr><td>1</td><td>3</td><td>3</td><td><strong>Yes! index 0</strong></td><td>—</td></tr></table><p><strong>Return:</strong> <code>[0, 1]</code> ✓ (same element not used twice because we check BEFORE storing)</p>",
    interviewTips: "<p><strong>Interview Script:</strong></p><ol><li><strong>Clarify:</strong> \"Can there be multiple solutions, or exactly one?\" \"Can I use the same element twice?\" \"Can the array have negative numbers?\"</li><li><strong>Brute force:</strong> \"Nested loops give O(n²). For each element, I'd scan the rest.\"</li><li><strong>Optimization insight:</strong> \"Instead of scanning, I can store seen elements in a HashMap. For each nums[i], I check if target-nums[i] is already stored.\"</li><li><strong>Subtle point:</strong> \"I check BEFORE storing to avoid using the same index twice.\"</li><li><strong>Complexity:</strong> \"O(n) time, O(n) space.\"</li></ol><p><strong>Common follow-up:</strong> \"What if the array is sorted?\" → Two Pointers (O(1) space). This is the Two Sum II problem!</p>",
    importantPoints: "<ul><li><strong>Check BEFORE inserting</strong> into the map — this handles the duplicate case like <code>[3,3], target=6</code> correctly.</li><li><strong>Map stores value → index</strong>, not index → value. We look up by value.</li><li><strong>Complement = target - nums[i]</strong> — memorize this; it's the core of the algorithm.</li><li><strong>If sorted array:</strong> Use two pointers (O(1) space) instead of HashMap (O(n) space).</li><li>This is arguably the most famous LeetCode problem and sets the foundation for many others. Master this pattern!</li></ul>"
  },
  {
    id: 4,
    name: "Group Anagrams",
    category: "Arrays & Hashing",
    categoryId: "arrays-hashing",
    difficulty: "Medium",
    explanation: "<p><strong>Problem:</strong> Given an array of strings <code>strs</code>, group the <strong>anagrams</strong> together. Return the groups in any order.</p><p><strong>Examples:</strong></p><ul><li>Input: <code>[\"eat\",\"tea\",\"tan\",\"ate\",\"nat\",\"bat\"]</code></li><li>Output: <code>[[\"bat\"],[\"nat\",\"tan\"],[\"ate\",\"eat\",\"tea\"]]</code></li></ul><p><strong>Key insight:</strong> All anagrams of a word produce the <em>same sorted string</em>. For example: \"eat\", \"tea\", \"ate\" all sort to \"aet\". We can use this sorted string as a <strong>HashMap key</strong> to group anagrams together.</p><p><strong>Constraints:</strong> <code>1 ≤ strs.length ≤ 10⁴</code>, <code>0 ≤ strs[i].length ≤ 100</code>, all lowercase English letters.</p>",
    bruteForce: "<p><strong>Brute Force — Compare All Pairs O(n² · k log k)</strong></p><p>For every pair of strings, check if they are anagrams by sorting both and comparing:</p><pre><code>groups = []\nvisited = boolean[n]\nfor i in range(n):\n  if visited[i]: continue\n  group = [strs[i]]\n  for j in range(i+1, n):\n    if isAnagram(strs[i], strs[j]):\n      group.add(strs[j])\n      visited[j] = true\n  groups.add(group)</code></pre><p><strong>Time:</strong> O(n² · k log k) where k = max string length.</p><p><strong>Space:</strong> O(n) for the visited array.</p>",
    bruteForceInefficiency: "<p><strong>Why comparing all pairs is slow:</strong></p><ul><li>We sort every string multiple times (once for each comparison).</li><li>n² comparisons means for n=10,000 strings we do 50 million comparisons.</li><li>We're not leveraging the fact that all anagrams have the <em>same canonical form</em> — if we compute it once, we never need to compare strings again.</li></ul><p><strong>The fix:</strong> Sort each string <em>once</em> to get its canonical key, then use a HashMap to group all strings with the same key. This reduces to O(n · k log k) — linear in n.</p>",
    optimalApproach: "<p><strong>Optimal — HashMap with Sorted Key O(n · k log k)</strong></p><p><strong>Pattern:</strong> Canonical form as HashMap key</p><ol><li>Create <code>Map&lt;String, List&lt;String&gt;&gt; map</code>.</li><li>For each string <code>s</code> in <code>strs</code>:<ul><li>Sort its characters to get key: <code>char[] arr = s.toCharArray(); Arrays.sort(arr); String key = new String(arr);</code></li><li>Add <code>s</code> to the list at <code>map.get(key)</code>.</li></ul></li><li>Return <code>new ArrayList&lt;&gt;(map.values())</code>.</li></ol><p><strong>Trick:</strong> <code>map.computeIfAbsent(key, k -&gt; new ArrayList&lt;&gt;()).add(s)</code> creates the list if absent and adds in one line.</p><p><strong>Time:</strong> O(n · k log k) — sort each string once.</p><p><strong>Space:</strong> O(n · k) — storing all strings in the map.</p><p><em>Alternative:</em> Use a character frequency array (int[26]) as the key instead of sorting — O(n · k) time.</p>",
    javaCode: "import java.util.*; // import all java.util classes\n\nclass Solution {\n    public List<List<String>> groupAnagrams(String[] strs) {\n        // map: sorted_string -> list of anagrams\n        Map<String, List<String>> map = new HashMap<>();\n        \n        // process each string in the input array\n        for (String s : strs) {\n            // convert string to char array so we can sort it\n            char[] chars = s.toCharArray();\n            \n            // sort the char array to get canonical/key form\n            Arrays.sort(chars);  // \"eat\" -> ['a','e','t']\n            \n            // convert sorted char array back to string as the key\n            String key = new String(chars); // \"aet\"\n            \n            // if key doesn't exist, create new list; then add s to the list\n            map.computeIfAbsent(key, k -> new ArrayList<>()).add(s);\n        }\n        \n        // return all grouped lists as the result\n        return new ArrayList<>(map.values());\n    }\n}",
    dryRun: "<p><strong>Input:</strong> <code>[\"eat\",\"tea\",\"tan\",\"ate\",\"nat\",\"bat\"]</code></p><table border='1' cellpadding='6' style='border-collapse:collapse'><tr><th>s</th><th>sorted key</th><th>map after</th></tr><tr><td>\"eat\"</td><td>\"aet\"</td><td>{\"aet\":[\"eat\"]}</td></tr><tr><td>\"tea\"</td><td>\"aet\"</td><td>{\"aet\":[\"eat\",\"tea\"]}</td></tr><tr><td>\"tan\"</td><td>\"ant\"</td><td>{\"aet\":[\"eat\",\"tea\"], \"ant\":[\"tan\"]}</td></tr><tr><td>\"ate\"</td><td>\"aet\"</td><td>{\"aet\":[\"eat\",\"tea\",\"ate\"], \"ant\":[\"tan\"]}</td></tr><tr><td>\"nat\"</td><td>\"ant\"</td><td>{\"aet\":[...], \"ant\":[\"tan\",\"nat\"]}</td></tr><tr><td>\"bat\"</td><td>\"abt\"</td><td>{\"aet\":[...], \"ant\":[...], \"abt\":[\"bat\"]}</td></tr></table><p><strong>Output:</strong> <code>[[\"eat\",\"tea\",\"ate\"],[\"tan\",\"nat\"],[\"bat\"]]</code> ✓</p>",
    interviewTips: "<p><strong>Interview Script:</strong></p><ol><li><strong>Clarify:</strong> \"Can strings be empty? Are they all lowercase?\"</li><li><strong>Key observation:</strong> \"Anagrams have the same letters — if I sort each string, anagrams produce identical keys.\"</li><li><strong>Algorithm:</strong> \"I'll use a HashMap: sorted string → list of original strings.\"</li><li><strong>Mention alternative:</strong> \"Instead of sorting, I could use a character frequency tuple as the key for O(n·k) instead of O(n·k log k).\"</li></ol><p><strong>The frequency-key alternative:</strong> Create a string key like <code>\"1#0#0#...\"</code> from int[26] counts. More complex but slightly faster.</p>",
    importantPoints: "<ul><li><strong>Canonical form</strong> is the key concept — sorted string is the most common canonical form for anagram grouping.</li><li><code>computeIfAbsent</code> is very useful here — it creates the list if the key is absent and returns the list in one call.</li><li><strong>Arrays.sort on char[]</strong> sorts in-place; then <code>new String(charArray)</code> converts back.</li><li><strong>Time complexity:</strong> O(n · k log k). The log k comes from sorting each string of length k.</li><li><strong>Return type:</strong> <code>List&lt;List&lt;String&gt;&gt;</code> — <code>map.values()</code> returns a <code>Collection</code>, so wrap it in <code>new ArrayList&lt;&gt;()</code>.</li></ul>"
  },
  {
    id: 5,
    name: "Top K Frequent Elements",
    category: "Arrays & Hashing",
    categoryId: "arrays-hashing",
    difficulty: "Medium",
    explanation: "<p><strong>Problem:</strong> Given an integer array <code>nums</code> and integer <code>k</code>, return the <code>k</code> most frequent elements. The answer may be in any order.</p><p><strong>Examples:</strong></p><ul><li>Input: <code>nums=[1,1,1,2,2,3], k=2</code> → Output: <code>[1,2]</code></li><li>Input: <code>nums=[1], k=1</code> → Output: <code>[1]</code></li></ul><p><strong>Constraints:</strong> <code>1 ≤ nums.length ≤ 10⁵</code>, <code>k</code> is valid (answer is unique).</p><p><strong>Key insight:</strong> Count frequencies, then find top-k by frequency. The challenge is doing the top-k part efficiently. Using bucket sort instead of a full sort reduces time from O(n log n) to O(n).</p>",
    bruteForce: "<p><strong>Brute Force — HashMap + Sort O(n log n)</strong></p><ol><li>Count frequency of each element using a HashMap.</li><li>Sort the unique elements by frequency descending.</li><li>Return the first k elements.</li></ol><pre><code>Map&lt;Integer,Integer&gt; freq = count frequencies\nList&lt;Integer&gt; keys = new ArrayList&lt;&gt;(freq.keySet())\nkeys.sort((a,b) -> freq.get(b) - freq.get(a)) // sort by freq desc\nreturn keys.subList(0, k)</code></pre><p><strong>Time:</strong> O(n log n) — dominated by sorting.</p><p><strong>Space:</strong> O(n) — HashMap and list.</p>",
    bruteForceInefficiency: "<p><strong>Why sorting is suboptimal:</strong></p><ul><li>We don't need a <em>fully sorted</em> list — we only need the top k elements.</li><li>Frequencies are bounded: they can only range from 1 to n (length of array).</li><li>Knowing frequencies are bounded lets us use <strong>bucket sort</strong> — create n+1 buckets, put each element in the bucket matching its frequency.</li><li>Then scan buckets from right to left (highest frequency first) to collect k elements.</li><li>This gives O(n) overall — no sorting needed!</li></ul>",
    optimalApproach: "<p><strong>Optimal — Bucket Sort O(n) time, O(n) space</strong></p><p><strong>Pattern:</strong> When values are bounded, use bucket sort instead of comparison sort.</p><ol><li>Count frequencies: <code>Map&lt;Integer,Integer&gt; freq</code>.</li><li>Create buckets: <code>List&lt;Integer&gt;[] bucket = new List[n+1]</code> where <code>bucket[i]</code> holds elements with frequency <code>i</code>. (Index 0 unused, max frequency is n.)</li><li>Fill buckets: for each (key, freq) in the map, add key to bucket[freq].</li><li>Collect top k: scan from <code>bucket[n]</code> down to <code>bucket[1]</code>, collecting elements until we have k.</li></ol><p><strong>Time:</strong> O(n) — counting + filling + collecting each O(n).</p><p><strong>Space:</strong> O(n) — HashMap + buckets.</p><p><em>Alternative: Use a min-heap of size k: O(n log k) time — better when k &lt;&lt; n.</em></p>",
    javaCode: "import java.util.*; // import all java.util\n\nclass Solution {\n    public int[] topKFrequent(int[] nums, int k) {\n        // step 1: count frequency of each element\n        Map<Integer, Integer> freq = new HashMap<>();\n        for (int n : nums) {\n            freq.merge(n, 1, Integer::sum); // increment count, or start at 1\n        }\n        \n        // step 2: create buckets indexed by frequency\n        // bucket[i] contains all elements that appear exactly i times\n        // max possible frequency is nums.length, so size = nums.length + 1\n        List<Integer>[] bucket = new List[nums.length + 1];\n        \n        // fill each element into its frequency bucket\n        for (int key : freq.keySet()) {\n            int f = freq.get(key); // get frequency of this element\n            if (bucket[f] == null) {\n                bucket[f] = new ArrayList<>(); // create bucket if needed\n            }\n            bucket[f].add(key); // add element to its frequency bucket\n        }\n        \n        // step 3: collect top k elements from highest-frequency buckets\n        int[] res = new int[k]; // result array\n        int idx = 0;            // index into result\n        \n        // scan from highest frequency (nums.length) down to 1\n        for (int i = bucket.length - 1; i >= 0 && idx < k; i--) {\n            if (bucket[i] != null) { // skip empty buckets\n                for (int n : bucket[i]) {\n                    if (idx < k) res[idx++] = n; // add to result\n                }\n            }\n        }\n        \n        return res; // return the k most frequent elements\n    }\n}",
    dryRun: "<p><strong>Input:</strong> <code>nums=[1,1,1,2,2,3], k=2</code></p><p><strong>Step 1 — Count frequencies:</strong> <code>freq = {1:3, 2:2, 3:1}</code></p><p><strong>Step 2 — Fill buckets</strong> (size 7 = n+1):</p><ul><li><code>bucket[3] = [1]</code> (element 1 appears 3 times)</li><li><code>bucket[2] = [2]</code> (element 2 appears 2 times)</li><li><code>bucket[1] = [3]</code> (element 3 appears 1 time)</li></ul><p><strong>Step 3 — Collect top 2:</strong></p><ul><li>i=6: null, skip</li><li>i=5: null, skip</li><li>i=4: null, skip</li><li>i=3: bucket[3]=[1] → res[0]=1, idx=1</li><li>i=2: bucket[2]=[2] → res[1]=2, idx=2 → idx==k, stop</li></ul><p><strong>Output:</strong> <code>[1, 2]</code> ✓</p>",
    interviewTips: "<p><strong>Interview Script:</strong></p><ol><li><strong>Clarify:</strong> \"Is the answer guaranteed unique? Can k equal n?\"</li><li><strong>Naive:</strong> \"Count frequencies with HashMap, then sort by frequency — O(n log n).\"</li><li><strong>Optimize:</strong> \"Since frequencies are bounded by n, I can use bucket sort to get O(n).\"</li><li><strong>Alternatives:</strong> \"A min-heap of size k gives O(n log k) — better than O(n log n) sorting but not as good as O(n) bucket sort. Worth mentioning.\"</li></ol><p><strong>Know all three approaches:</strong></p><ul><li>HashMap + sort: O(n log n) — easiest to code</li><li>HashMap + min-heap size k: O(n log k) — good when k is small</li><li>HashMap + bucket sort: O(n) — optimal</li></ul>",
    importantPoints: "<ul><li><strong>Bucket sort works</strong> because frequencies are bounded between 1 and n — this is the key insight.</li><li><code>freq.merge(n, 1, Integer::sum)</code> is a clean way to increment a count in a HashMap.</li><li>The bucket array size is <code>n+1</code> (not n) because frequency can equal n (all elements same).</li><li><strong>Min-heap alternative:</strong> <code>PriorityQueue</code> of size k, O(n log k) — mention this in interviews as a trade-off.</li><li>Time O(n) doesn't mean it's always fastest in practice — bucket sort has more overhead per operation than a sort for small n.</li></ul>"
  },
  {
    id: 6,
    name: "Encode and Decode Strings",
    category: "Arrays & Hashing",
    categoryId: "arrays-hashing",
    difficulty: "Medium",
    explanation: "<p><strong>Problem:</strong> Design an algorithm to encode a list of strings to a single string. The encoded string must be sent over a network and decoded back to the original list of strings.</p><p>Implement <code>encode(List&lt;String&gt; strs)</code> and <code>decode(String s)</code>.</p><p><strong>Challenge:</strong> Strings can contain ANY character including <code>#</code>, spaces, newlines. We can't just join with a delimiter like comma or space because those might appear inside the strings themselves.</p><p><strong>Examples:</strong></p><ul><li>Encode <code>[\"hello\",\"world\"]</code> → some encoded string → Decode → <code>[\"hello\",\"world\"]</code></li><li>Encode <code>[\"a#b\",\"c\"]</code> → must work even with # in the string</li><li>Encode <code>[\"\",\"\",\"\"]</code> → must handle empty strings</li></ul>",
    bruteForce: "<p><strong>Naive approach — Simple delimiter (FAILS)</strong></p><p>Join with a rare character like <code>|</code>:</p><pre><code>encode: join strs with \"|\"\ndecode: split by \"|\"</code></pre><p><strong>Problem:</strong> If any string contains <code>|</code>, the decode will produce wrong results. Any single-character delimiter will fail if that character appears in the strings.</p><p><strong>What about escaping?</strong> Possible but complex — escape every occurrence of the delimiter in each string. Error-prone and harder to implement correctly under interview pressure.</p>",
    bruteForceInefficiency: "<p><strong>Why simple delimiters fail:</strong></p><ul><li>No character is guaranteed to not appear in the input strings.</li><li>Escaping adds complexity and is easy to get wrong.</li><li><strong>A better approach:</strong> Instead of using a <em>special character</em> as delimiter, use the <em>length</em> of each string as a delimiter.</li><li>If we prefix each string with its length followed by a known separator, we can always parse unambiguously.</li><li>Example: <code>\"hello\"</code> becomes <code>\"5#hello\"</code>. When decoding, read the number before <code>#</code>, then read exactly that many characters.</li></ul>",
    optimalApproach: "<p><strong>Optimal — Length-Prefix Encoding</strong></p><p><strong>Pattern:</strong> Use length-prefixed framing (common in network protocols)</p><p><strong>Encode:</strong> For each string <code>s</code>, append <code>s.length() + \"#\" + s</code> to the result.</p><ul><li><code>[\"hello\",\"world\"]</code> → <code>\"5#hello5#world\"</code></li><li><code>[\"a#b\",\"c\"]</code> → <code>\"3#a#b1#c\"</code> (the # in \"a#b\" is fine — we read exactly 3 chars)</li><li><code>[\"\"]</code> → <code>\"0#\"</code></li></ul><p><strong>Decode:</strong> Start at index 0. Find the next <code>#</code>, parse the length, extract exactly that many characters, advance pointer.</p><p><strong>Time:</strong> O(n) encode, O(n) decode where n = total chars.</p><p><strong>Space:</strong> O(n) — the encoded/decoded strings.</p>",
    javaCode: "import java.util.*; // import List, ArrayList\n\nclass Codec {\n    \n    // encode: convert list of strings to single encoded string\n    public String encode(List<String> strs) {\n        StringBuilder sb = new StringBuilder(); // use StringBuilder for efficiency\n        \n        for (String s : strs) {\n            // prefix each string with its length and a '#' separator\n            // e.g., \"hello\" -> \"5#hello\", \"a#b\" -> \"3#a#b\"\n            sb.append(s.length()); // write the length\n            sb.append('#');        // write the '#' separator\n            sb.append(s);          // write the actual string\n        }\n        \n        return sb.toString(); // return the fully encoded string\n    }\n    \n    // decode: convert encoded string back to list of original strings\n    public List<String> decode(String s) {\n        List<String> res = new ArrayList<>(); // result list\n        int i = 0; // current position in encoded string\n        \n        // process until we reach the end of the encoded string\n        while (i < s.length()) {\n            // find the '#' that separates the length from the content\n            int j = s.indexOf('#', i);\n            \n            // parse the length from s[i..j-1]\n            int len = Integer.parseInt(s.substring(i, j));\n            \n            // extract exactly 'len' characters after the '#'\n            res.add(s.substring(j + 1, j + 1 + len));\n            \n            // advance i past this encoded segment\n            i = j + 1 + len;\n        }\n        \n        return res; // return the decoded list\n    }\n}",
    dryRun: "<p><strong>Encode example:</strong> <code>[\"neet\",\"code\",\"love\",\"you\"]</code></p><table border='1' cellpadding='6' style='border-collapse:collapse'><tr><th>string</th><th>length</th><th>appended</th></tr><tr><td>\"neet\"</td><td>4</td><td>\"4#neet\"</td></tr><tr><td>\"code\"</td><td>4</td><td>\"4#code\"</td></tr><tr><td>\"love\"</td><td>4</td><td>\"4#love\"</td></tr><tr><td>\"you\"</td><td>3</td><td>\"3#you\"</td></tr></table><p><strong>Encoded:</strong> <code>\"4#neet4#code4#love3#you\"</code></p><p><strong>Decode: </strong><code>\"4#neet4#code4#love3#you\"</code></p><table border='1' cellpadding='6' style='border-collapse:collapse'><tr><th>i</th><th>j (#pos)</th><th>len</th><th>extracted</th><th>next i</th></tr><tr><td>0</td><td>1</td><td>4</td><td>\"neet\"</td><td>6</td></tr><tr><td>6</td><td>7</td><td>4</td><td>\"code\"</td><td>12</td></tr><tr><td>12</td><td>13</td><td>4</td><td>\"love\"</td><td>18</td></tr><tr><td>18</td><td>19</td><td>3</td><td>\"you\"</td><td>23</td></tr></table><p><strong>Output:</strong> <code>[\"neet\",\"code\",\"love\",\"you\"]</code> ✓</p>",
    interviewTips: "<p><strong>Interview Script:</strong></p><ol><li><strong>Identify the challenge:</strong> \"I can't use a simple delimiter because any character could appear in the strings.\"</li><li><strong>Key insight:</strong> \"I'll use length-prefix encoding — store the length of each string before the string itself, separated by '#'.\"</li><li><strong>Why '#' as separator?</strong> \"The '#' between the length and the content is unambiguous because I parse the length as a number, find the '#', then read exactly that many bytes — the '#' character in the string itself doesn't matter.\"</li><li><strong>Edge cases:</strong> Empty strings (len=0), single-character strings, strings containing digits and '#'.</li></ol><p><strong>Important:</strong> This pattern is used in real network protocols (like HTTP framing headers). Knowing why it works is more impressive than just memorizing it.</p>",
    importantPoints: "<ul><li><strong>Length-prefix encoding</strong> is robust because the length tells us exactly how many characters to read — no delimiter ambiguity.</li><li>The <code>#</code> between length and content is a visual separator only — it works even if the string contains <code>#</code> because we use the length to skip past it.</li><li><strong>Edge cases:</strong> empty string (<code>\"0#\"</code>), string with only digits, string with <code>#</code> chars.</li><li><code>indexOf('#', i)</code> finds the FIRST <code>#</code> at or after position i — this correctly handles strings that start with digits.</li><li>StringBuilder is O(n) for concatenation; using <code>+=</code> in a loop would be O(n²).</li></ul>"
  },
  {
    id: 7,
    name: "Product of Array Except Self",
    category: "Arrays & Hashing",
    categoryId: "arrays-hashing",
    difficulty: "Medium",
    explanation: "<p><strong>Problem:</strong> Given an integer array <code>nums</code>, return an array <code>answer</code> such that <code>answer[i]</code> is the product of all elements of <code>nums</code> <strong>except</strong> <code>nums[i]</code>.</p><p>You must solve it <strong>without using division</strong> and in <strong>O(n)</strong> time.</p><p><strong>Examples:</strong></p><ul><li>Input: <code>[1,2,3,4]</code> → Output: <code>[24,12,8,6]</code></li><li>answer[0] = 2×3×4 = 24, answer[1] = 1×3×4 = 12, etc.</li><li>Input: <code>[-1,1,0,-3,3]</code> → Output: <code>[0,0,9,0,0]</code></li></ul><p><strong>Why no division?</strong> Division would fail with zeros in the array. The elegant solution uses prefix and suffix products.</p>",
    bruteForce: "<p><strong>Brute Force — Nested Loops O(n²)</strong></p><p>For each index i, multiply all elements except nums[i]:</p><pre><code>for i in range(n):\n  product = 1\n  for j in range(n):\n    if j != i:\n      product *= nums[j]\n  result[i] = product</code></pre><p><strong>Time:</strong> O(n²) — n × n multiplications.</p><p><strong>Space:</strong> O(1) extra (excluding output array).</p>",
    bruteForceInefficiency: "<p><strong>Why O(n²) is wasteful:</strong></p><ul><li>For each element, we multiply ALL other elements — but neighboring elements share most of their computation.</li><li><code>answer[0] = nums[1]×nums[2]×nums[3]</code> and <code>answer[1] = nums[0]×nums[2]×nums[3]</code> — they share <code>nums[2]×nums[3]</code>.</li><li><strong>Key insight:</strong> <code>answer[i] = (product of all elements to the LEFT of i) × (product of all elements to the RIGHT of i)</code></li><li>We can precompute these left-products and right-products in two linear passes, each O(n).</li></ul>",
    optimalApproach: "<p><strong>Optimal — Prefix × Suffix Products O(n) time, O(1) extra space</strong></p><p><strong>Pattern:</strong> Prefix/suffix product arrays</p><p><strong>Algorithm:</strong></p><ol><li><strong>Left pass:</strong> Fill <code>res[i]</code> with the product of all elements to the LEFT of i.<ul><li><code>res[0] = 1</code> (nothing to the left)</li><li><code>res[i] = res[i-1] * nums[i-1]</code></li></ul></li><li><strong>Right pass:</strong> Multiply <code>res[i]</code> by the product of all elements to the RIGHT of i, using a running <code>suffix</code> variable.<ul><li>Start with <code>suffix = 1</code>, iterate from right to left.</li><li><code>res[i] *= suffix; suffix *= nums[i];</code></li></ul></li></ol><p>After both passes, <code>res[i]</code> = left product × right product = product of everything except nums[i].</p><p><strong>Time:</strong> O(n). <strong>Space:</strong> O(1) extra (output array doesn't count).</p>",
    javaCode: "class Solution {\n    public int[] productExceptSelf(int[] nums) {\n        int n = nums.length; // store length for readability\n        int[] res = new int[n]; // output array\n        \n        // === LEFT PASS ===\n        // res[i] will hold the product of all elements to the LEFT of i\n        res[0] = 1; // nothing to the left of index 0, so product = 1\n        for (int i = 1; i < n; i++) {\n            // product to the left of i = product to left of (i-1) * nums[i-1]\n            res[i] = res[i - 1] * nums[i - 1];\n        }\n        // after this pass: res = [1, 1, 2, 6] for nums=[1,2,3,4]\n        \n        // === RIGHT PASS ===\n        // multiply each res[i] by the product of all elements to the RIGHT of i\n        int suffix = 1; // running product from the right side\n        for (int i = n - 1; i >= 0; i--) {\n            res[i] *= suffix; // multiply by right-side product\n            suffix *= nums[i]; // update suffix to include nums[i] for the next iteration\n        }\n        // after this pass: res = [24, 12, 8, 6] for nums=[1,2,3,4]\n        \n        return res; // res[i] = left_product * right_product = product except self\n    }\n}",
    dryRun: "<p><strong>Input:</strong> <code>nums = [1, 2, 3, 4]</code></p><p><strong>Left pass:</strong></p><table border='1' cellpadding='6' style='border-collapse:collapse'><tr><th>i</th><th>res[i] = res[i-1]*nums[i-1]</th><th>res state</th></tr><tr><td>0</td><td>res[0] = 1 (base)</td><td>[1,_,_,_]</td></tr><tr><td>1</td><td>res[1] = 1*1 = 1</td><td>[1,1,_,_]</td></tr><tr><td>2</td><td>res[2] = 1*2 = 2</td><td>[1,1,2,_]</td></tr><tr><td>3</td><td>res[3] = 2*3 = 6</td><td>[1,1,2,6]</td></tr></table><p><strong>Right pass (suffix starts at 1):</strong></p><table border='1' cellpadding='6' style='border-collapse:collapse'><tr><th>i</th><th>res[i]*=suffix</th><th>suffix*=nums[i]</th><th>res state</th></tr><tr><td>3</td><td>6*1=6</td><td>1*4=4</td><td>[1,1,2,6]</td></tr><tr><td>2</td><td>2*4=8</td><td>4*3=12</td><td>[1,1,8,6]</td></tr><tr><td>1</td><td>1*12=12</td><td>12*2=24</td><td>[1,12,8,6]</td></tr><tr><td>0</td><td>1*24=24</td><td>24*1=24</td><td>[24,12,8,6]</td></tr></table><p><strong>Output:</strong> <code>[24, 12, 8, 6]</code> ✓</p>",
    interviewTips: "<p><strong>Interview Script:</strong></p><ol><li><strong>Clarify:</strong> \"No division allowed, right? And I need O(n) time?\"</li><li><strong>Insight:</strong> \"For each position, the answer is the product of everything to its LEFT times everything to its RIGHT.\"</li><li><strong>Algorithm:</strong> \"Two passes: left-to-right for prefix products, right-to-left for suffix products. I can do the suffix pass in-place using a single variable.\"</li><li><strong>Complexity:</strong> \"O(n) time, O(1) extra space (the output array doesn't count per the problem).\"</li></ol><p><strong>Common mistake:</strong> Forgetting to initialize <code>res[0] = 1</code> (there's nothing to the left of index 0).</p>",
    importantPoints: "<ul><li><strong>No division</strong> — the clever part is prefix/suffix products avoid division entirely, which also handles zero elements correctly.</li><li><strong>Prefix product:</strong> <code>res[i] = res[i-1] * nums[i-1]</code> — this is product of nums[0..i-1].</li><li><strong>Suffix pass:</strong> A single running variable <code>suffix</code> keeps the right-side product, avoiding a separate suffix array.</li><li><strong>O(1) extra space</strong> — the output array is not counted as extra space by convention.</li><li><strong>Handles zeros:</strong> If nums[1]=0, then res[0] gets multiplied by the suffix product which includes that 0. Naturally handles it.</li></ul>"
  },
  {
    id: 8,
    name: "Valid Sudoku",
    category: "Arrays & Hashing",
    categoryId: "arrays-hashing",
    difficulty: "Medium",
    explanation: "<p><strong>Problem:</strong> Determine if a 9×9 Sudoku board is valid. A valid board means:</p><ul><li>Each <strong>row</strong> contains digits 1-9 with no repetition.</li><li>Each <strong>column</strong> contains digits 1-9 with no repetition.</li><li>Each of the nine 3×3 <strong>sub-boxes</strong> contains digits 1-9 with no repetition.</li></ul><p>The board may be partially filled; empty cells are represented by <code>'.'</code>.</p><p><strong>Note:</strong> You only need to validate — not solve — the Sudoku. A partially filled board is valid as long as no constraints are currently violated.</p><p><strong>Key insight:</strong> Track which values we've seen in each row, column, and box using HashSets (or encoded strings as keys into a single HashSet).</p>",
    bruteForce: "<p><strong>Brute Force — Three Separate Loops</strong></p><p>Check each row, column, and box separately:</p><pre><code>// Check rows (9 rows)\nfor each row: extract non-'.' values, check for duplicates\n// Check cols (9 cols)\nfor each col: extract non-'.' values, check for duplicates\n// Check boxes (9 boxes)\nfor each 3x3 box: extract non-'.' values, check for duplicates</code></pre><p>This works but requires 3 separate passes and 27 separate duplicate checks.</p><p><strong>Time:</strong> O(81) = O(1) — always 9×9 board.</p>",
    bruteForceInefficiency: "<p><strong>Why three passes is unnecessary:</strong></p><ul><li>We visit each cell 3 times (once for row, once for col, once for box).</li><li>We can do everything in <strong>one pass</strong> with a clever encoding trick.</li><li><strong>The trick:</strong> Use a single HashSet and add encoded strings that capture which value appeared in which row/column/box.</li><li>For cell (i,j) with value c: add <code>\"c in row i\"</code>, <code>\"c in col j\"</code>, <code>\"c in box i/3-j/3\"</code> to the set.</li><li>If any add returns false, we have a duplicate constraint violation.</li></ul>",
    optimalApproach: "<p><strong>Optimal — Single Pass with Encoded HashSet O(1)</strong></p><p><strong>Pattern:</strong> Encode constraints as unique strings in a HashSet</p><ol><li>Create a single <code>Set&lt;String&gt; seen</code>.</li><li>For each cell (i, j) with value c != '.':<ul><li>Try to add <code>c + \" in row \" + i</code> → if false, row has duplicate.</li><li>Try to add <code>c + \" in col \" + j</code> → if false, col has duplicate.</li><li>Try to add <code>c + \" in box \" + (i/3) + \"-\" + (j/3)</code> → if false, box has duplicate.</li></ul></li><li>If any add returns false, return false.</li></ol><p><strong>Box index trick:</strong> <code>(i/3, j/3)</code> gives a unique (row, col) pair for each 3×3 box (integer division maps 0-2→0, 3-5→1, 6-8→2).</p><p><strong>Time &amp; Space:</strong> O(1) — always a 9×9 board (at most 81 cells × 3 checks = 243 set entries).</p>",
    javaCode: "import java.util.HashSet; // import HashSet\nimport java.util.Set;     // import Set interface\n\nclass Solution {\n    public boolean isValidSudoku(char[][] board) {\n        // a single HashSet to track all seen (value, constraint) pairs\n        Set<String> seen = new HashSet<>();\n        \n        // iterate over every cell in the 9x9 board\n        for (int i = 0; i < 9; i++) {        // row index\n            for (int j = 0; j < 9; j++) {    // column index\n                char c = board[i][j];        // get cell value\n                \n                if (c == '.') continue;      // skip empty cells\n                \n                // check row constraint: has this digit appeared in row i before?\n                // check col constraint: has this digit appeared in col j before?\n                // check box constraint: has this digit appeared in this 3x3 box?\n                // i/3 and j/3 give the box's row and col index (0, 1, or 2)\n                if (!seen.add(c + \" row\" + i) ||\n                    !seen.add(c + \" col\" + j) ||\n                    !seen.add(c + \" box\" + i/3 + \"-\" + j/3)) {\n                    return false; // duplicate found in row, col, or box\n                }\n            }\n        }\n        \n        return true; // no violations found, board is valid\n    }\n}",
    dryRun: "<p><strong>Example violation:</strong> Row 0 = <code>['5','3','.','.','7','.','.','.','.']</code> — valid so far.</p><p>Cell (0,0): c='5'. Add <code>\"5 row0\"</code> ✓, <code>\"5 col0\"</code> ✓, <code>\"5 box0-0\"</code> ✓</p><p>Cell (0,1): c='3'. Add <code>\"3 row0\"</code> ✓, <code>\"3 col1\"</code> ✓, <code>\"3 box0-0\"</code> ✓</p><p><strong>Duplicate example:</strong> If cell (0,5)='5' (same row as (0,0)):</p><p>Try to add <code>\"5 row0\"</code> → <strong>already exists!</strong> Returns false → return <code>false</code> immediately.</p><p><strong>Box indexing:</strong></p><table border='1' cellpadding='6' style='border-collapse:collapse'><tr><th>i</th><th>j</th><th>i/3</th><th>j/3</th><th>box label</th></tr><tr><td>0-2</td><td>0-2</td><td>0</td><td>0</td><td>box0-0 (top-left)</td></tr><tr><td>0-2</td><td>3-5</td><td>0</td><td>1</td><td>box0-1 (top-center)</td></tr><tr><td>3-5</td><td>3-5</td><td>1</td><td>1</td><td>box1-1 (center)</td></tr></table>",
    interviewTips: "<p><strong>Interview Script:</strong></p><ol><li><strong>Clarify:</strong> \"I need to validate the current state only, not check if it's solvable, right?\"</li><li><strong>Three constraints:</strong> \"Each row, each column, each 3×3 box must have no duplicate digits.\"</li><li><strong>Approach:</strong> \"I'll use a HashSet with encoded strings. For each cell, I encode three constraints and check they're unique.\"</li><li><strong>Box trick:</strong> \"Integer division: row/3 and col/3 map each cell to one of the 9 boxes.\"</li><li><strong>Complexity:</strong> \"O(1) time and space — the board is always 9×9.\"</li></ol><p><strong>Alternative:</strong> Three 9×9 boolean arrays (rows[9][9], cols[9][9], boxes[9][9]) — more explicit, same complexity.</p>",
    importantPoints: "<ul><li><strong>Box indexing:</strong> <code>(i/3, j/3)</code> is the clever trick — integer division maps 9 rows/cols into 3 groups of 3.</li><li><strong>Skip empty cells</strong> with <code>if (c == '.') continue</code>.</li><li><strong>Short-circuit evaluation:</strong> Using <code>||</code> means if the row check fails, col and box checks don't run (already returning false).</li><li><strong>String encoding</strong> is unique because we include both the value and the constraint type.</li><li><strong>Always O(1)</strong> — Sudoku board is always 9×9; this is a constant-size problem.</li></ul>"
  },
  {
    id: 9,
    name: "Longest Consecutive Sequence",
    category: "Arrays & Hashing",
    categoryId: "arrays-hashing",
    difficulty: "Medium",
    explanation: "<p><strong>Problem:</strong> Given an unsorted array of integers <code>nums</code>, return the length of the <strong>longest consecutive sequence</strong> (elements don't need to be adjacent in the array).</p><p>You must write an algorithm that runs in <strong>O(n)</strong> time.</p><p><strong>Examples:</strong></p><ul><li>Input: <code>[100,4,200,1,3,2]</code> → Output: <code>4</code> (sequence: 1,2,3,4)</li><li>Input: <code>[0,3,7,2,5,8,4,6,0,1]</code> → Output: <code>9</code> (sequence: 0-8)</li></ul><p><strong>Key insight:</strong> Put all numbers in a HashSet. For each number, only start counting if it's the <em>beginning</em> of a sequence (i.e., <code>num-1</code> is NOT in the set). Then count how long the sequence extends from that start.</p>",
    bruteForce: "<p><strong>Brute Force — Sort O(n log n)</strong></p><ol><li>Sort the array.</li><li>Iterate through, counting consecutive elements (handling duplicates).</li><li>Track the maximum run length.</li></ol><pre><code>Arrays.sort(nums)\nfor each num: if num == prev+1, extend current streak\n             else if num != prev, reset streak to 1</code></pre><p><strong>Time:</strong> O(n log n) due to sorting.</p><p><strong>Space:</strong> O(1) extra (in-place sort).</p><p>This is clean and acceptable, but the problem asks for O(n).</p>",
    bruteForceInefficiency: "<p><strong>Why we can do better than sorting:</strong></p><ul><li>Sorting takes O(n log n) — we're paying to put all numbers in order, but we only need to find the longest run.</li><li><strong>HashSet insight:</strong> If we put all numbers in a set, we can check membership in O(1).</li><li><strong>Key observation:</strong> We only need to count from the <em>start</em> of a sequence. A number <code>n</code> is a sequence start if and only if <code>n-1</code> is NOT in the set.</li><li>By skipping non-starts, we ensure each element is part of at most one sequence count — total work is O(n).</li></ul>",
    optimalApproach: "<p><strong>Optimal — HashSet O(n) time, O(n) space</strong></p><p><strong>Pattern:</strong> Use HashSet for O(1) membership test; only start counting at sequence beginnings.</p><ol><li>Add all elements to a <code>HashSet&lt;Integer&gt;</code>.</li><li>For each number <code>n</code> in the set:<ul><li>Skip if <code>set.contains(n-1)</code> — this means n is not a sequence start.</li><li>If n IS a start: count up by checking n+1, n+2, ... until the sequence breaks.</li><li>Update the global maximum length.</li></ul></li></ol><p><strong>Why O(n)?</strong> Each element is visited at most twice — once when it appears as a non-start (skipped immediately), and once when we count from its sequence's start.</p><p><strong>Time:</strong> O(n). <strong>Space:</strong> O(n) — the HashSet.</p>",
    javaCode: "import java.util.HashSet; // import HashSet\nimport java.util.Set;     // import Set interface\n\nclass Solution {\n    public int longestConsecutive(int[] nums) {\n        // put all numbers in a HashSet for O(1) lookup\n        Set<Integer> set = new HashSet<>();\n        for (int n : nums) {\n            set.add(n); // duplicates are automatically ignored by HashSet\n        }\n        \n        int best = 0; // track the longest sequence found\n        \n        // try to start a sequence from each number in the set\n        for (int n : set) {\n            // only start counting if n is the beginning of a sequence\n            // n is a start if n-1 is NOT in the set\n            if (!set.contains(n - 1)) {\n                int cur = n;   // current number in the sequence\n                int len = 1;   // current sequence length (starts at 1 for n itself)\n                \n                // extend the sequence as long as the next number exists\n                while (set.contains(cur + 1)) {\n                    cur++; // move to next number\n                    len++; // increment length\n                }\n                \n                // update the best (longest) sequence found so far\n                best = Math.max(best, len);\n            }\n        }\n        \n        return best; // return the length of the longest consecutive sequence\n    }\n}",
    dryRun: "<p><strong>Input:</strong> <code>[100, 4, 200, 1, 3, 2]</code></p><p><strong>Set:</strong> {100, 4, 200, 1, 3, 2}</p><p><strong>Processing each element:</strong></p><table border='1' cellpadding='6' style='border-collapse:collapse'><tr><th>n</th><th>n-1 in set?</th><th>action</th><th>sequence found</th></tr><tr><td>100</td><td>99? No</td><td>Start counting: 100, 101? No</td><td>len=1</td></tr><tr><td>4</td><td>3? Yes</td><td>Skip (not a start)</td><td>—</td></tr><tr><td>200</td><td>199? No</td><td>Start counting: 200, 201? No</td><td>len=1</td></tr><tr><td>1</td><td>0? No</td><td>Start counting: 1,2✓,3✓,4✓,5? No</td><td>len=4</td></tr><tr><td>3</td><td>2? Yes</td><td>Skip (not a start)</td><td>—</td></tr><tr><td>2</td><td>1? Yes</td><td>Skip (not a start)</td><td>—</td></tr></table><p><strong>Best = max(1,1,4) = 4</strong></p><p><strong>Output:</strong> <code>4</code> ✓ (sequence 1→2→3→4)</p>",
    interviewTips: "<p><strong>Interview Script:</strong></p><ol><li><strong>Clarify:</strong> \"Elements don't need to be adjacent in the array, just consecutive in value, right?\"</li><li><strong>Brute force:</strong> \"Sorting gives O(n log n). But the problem asks for O(n).\"</li><li><strong>Key insight:</strong> \"I'll put everything in a HashSet. Then for each number that is the START of a sequence (i.e., num-1 not in set), I'll count how long the sequence goes.\"</li><li><strong>Why O(n)?</strong> \"Each number is processed at most twice — once as a non-start (O(1) check), once during its sequence's count. Total work is linear.\"</li></ol><p><strong>Common mistake:</strong> Iterating over <code>nums</code> instead of <code>set</code> — if nums has duplicates, you'd recount sequences. Iterating over the set avoids this.</p>",
    importantPoints: "<ul><li><strong>Only start from sequence beginnings</strong> — this is the key optimization that makes it O(n). Without this, every element would trigger a while loop.</li><li><strong>Iterate over the set, not the array</strong> — avoids redundant work on duplicates.</li><li><strong>HashSet.add() ignores duplicates</strong> — adding the same number twice has no effect.</li><li><strong>Why O(n) and not O(n²)?</strong> Each element is counted in exactly one sequence. The total number of <code>cur++</code> operations across all sequence starts equals n.</li><li><strong>Handles negatives and zero</strong> — no issues since HashSet works with any integers.</li></ul>"
  }
];
