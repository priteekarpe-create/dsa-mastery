window.BINARY_SEARCH_DATA = [
  {
    id: 28,
    name: "Binary Search",
    category: "Binary Search",
    categoryId: "binary-search",
    difficulty: "Easy",
    explanation: "<p><strong>Binary Search</strong> is the foundational algorithm for searching in a <strong>sorted array</strong>. Instead of checking every element (O(n)), we repeatedly divide the search space in half.</p><p>Given a sorted array <code>nums</code> and a <code>target</code>, return the index of the target or -1 if not found.</p><p>Each iteration, we look at the middle element:<ul><li>If it equals the target → found it!</li><li>If it's less than the target → target must be in the right half.</li><li>If it's greater → target must be in the left half.</li></ul>This halving means we need at most <strong>log₂(n)</strong> steps.</p>",
    bruteForce: "<p><strong>Linear Search:</strong> Scan every element from left to right.</p><pre><code>for (int i = 0; i &lt; nums.length; i++)\n    if (nums[i] == target) return i;\nreturn -1;</code></pre><p><strong>Time:</strong> O(n). <strong>Space:</strong> O(1).</p>",
    bruteForceInefficiency: "<p>Linear search ignores the sorted property of the array. For an array of 1 billion elements, linear search may take 1 billion steps while binary search takes only 30. The sorted order allows us to discard half the remaining elements with each comparison.</p>",
    optimalApproach: "<p><strong>Binary Search — O(log n) time, O(1) space:</strong></p><ol><li>Initialize <code>l = 0</code>, <code>r = nums.length - 1</code>.</li><li>While <code>l &lt;= r</code>:<ul><li>Compute <code>mid = l + (r - l) / 2</code> (avoids integer overflow).</li><li>If <code>nums[mid] == target</code> → return <code>mid</code>.</li><li>If <code>nums[mid] &lt; target</code> → <code>l = mid + 1</code> (search right half).</li><li>Else → <code>r = mid - 1</code> (search left half).</li></ul></li><li>Return -1 if not found.</li></ol>",
    javaCode: "class Solution {\n    public int search(int[] nums, int target) {\n        int l = 0, r = nums.length - 1;\n        while (l <= r) {\n            int mid = l + (r - l) / 2;\n            if (nums[mid] == target) return mid;\n            else if (nums[mid] < target) l = mid + 1;\n            else r = mid - 1;\n        }\n        return -1;\n    }\n}",
    dryRun: "<p><strong>Input:</strong> nums=[-1,0,3,5,9,12], target=9</p><table border='1' cellpadding='4'><tr><th>l</th><th>r</th><th>mid</th><th>nums[mid]</th><th>Action</th></tr><tr><td>0</td><td>5</td><td>2</td><td>3</td><td>3&lt;9 → l=3</td></tr><tr><td>3</td><td>5</td><td>4</td><td>9</td><td>9==9 → return 4</td></tr></table><p>Return <strong>4</strong> ✓</p>",
    interviewTips: "<ul><li><strong>Use <code>l + (r - l) / 2</code></strong> instead of <code>(l + r) / 2</code> to prevent integer overflow.</li><li><strong>Loop condition:</strong> <code>l &lt;= r</code> (not <code>l &lt; r</code>) because when l==r, there's still one element to check.</li><li><strong>Off-by-one:</strong> Use <code>l = mid + 1</code> and <code>r = mid - 1</code> to avoid infinite loops.</li><li>This is a template — variations like \"first/last occurrence\" or \"insert position\" are minor tweaks.</li></ul>",
    importantPoints: "<ul><li>Only works on <strong>sorted</strong> arrays.</li><li>Use <code>l + (r - l) / 2</code> to avoid overflow.</li><li><strong>Time:</strong> O(log n). <strong>Space:</strong> O(1).</li><li>The loop invariant: the target, if it exists, is always in [l, r].</li></ul>"
  },
  {
    id: 29,
    name: "Search a 2D Matrix",
    category: "Binary Search",
    categoryId: "binary-search",
    difficulty: "Medium",
    explanation: "<p><strong>Search a 2D Matrix:</strong> Given an m×n matrix where each row is sorted and the first element of each row is greater than the last element of the previous row, determine if a target value exists.</p><p>The key insight: this matrix is essentially a <strong>sorted 1D array</strong> laid out in rows. Element at position <code>k</code> in the flattened array maps to <code>matrix[k/n][k%n]</code>.</p><p>We can run binary search on this virtual 1D array without actually flattening it.</p>",
    bruteForce: "<p><strong>Linear Scan:</strong> Check every element in the matrix.</p><pre><code>for (int[] row : matrix)\n    for (int val : row)\n        if (val == target) return true;\nreturn false;</code></pre><p><strong>Time:</strong> O(m×n). <strong>Space:</strong> O(1).</p>",
    bruteForceInefficiency: "<p>Ignores both the row-sorted and inter-row sorted properties. Binary search on the flattened virtual array brings this down to O(log(m×n)) = O(log m + log n).</p>",
    optimalApproach: "<p><strong>Binary Search on Flattened Matrix — O(log(m×n)) time, O(1) space:</strong></p><ol><li>Treat the m×n matrix as a 1D sorted array of length m×n.</li><li>Set <code>l = 0</code>, <code>r = m*n - 1</code>.</li><li>Standard binary search: compute <code>mid</code>, convert to 2D index: <code>row = mid/n</code>, <code>col = mid%n</code>.</li><li>Compare <code>matrix[row][col]</code> with target and adjust l/r.</li></ol>",
    javaCode: "class Solution {\n    public boolean searchMatrix(int[][] matrix, int target) {\n        int m = matrix.length, n = matrix[0].length;\n        int l = 0, r = m * n - 1;\n        while (l <= r) {\n            int mid = l + (r - l) / 2;\n            int val = matrix[mid / n][mid % n];\n            if (val == target) return true;\n            else if (val < target) l = mid + 1;\n            else r = mid - 1;\n        }\n        return false;\n    }\n}",
    dryRun: "<p><strong>Input:</strong> matrix=[[1,3,5,7],[10,11,16,20],[23,30,34,60]], target=3</p><p>m=3, n=4, total=12 elements. l=0, r=11.</p><table border='1' cellpadding='4'><tr><th>l</th><th>r</th><th>mid</th><th>row,col</th><th>val</th><th>Action</th></tr><tr><td>0</td><td>11</td><td>5</td><td>1,1</td><td>11</td><td>11&gt;3 → r=4</td></tr><tr><td>0</td><td>4</td><td>2</td><td>0,2</td><td>5</td><td>5&gt;3 → r=1</td></tr><tr><td>0</td><td>1</td><td>0</td><td>0,0</td><td>1</td><td>1&lt;3 → l=1</td></tr><tr><td>1</td><td>1</td><td>1</td><td>0,1</td><td>3</td><td>3==3 → return true</td></tr></table>",
    interviewTips: "<ul><li><strong>Key formula:</strong> <code>matrix[mid/n][mid%n]</code> converts a 1D index to 2D. Be able to derive this from scratch.</li><li><strong>Alternative approach:</strong> Start at top-right corner, move left if too big, move down if too small — O(m+n) but works even without the strict row ordering.</li><li><strong>Clarify:</strong> Is the first element of each row strictly greater than the last of the previous? (If yes, use binary search. If not, use the staircase approach.)</li></ul>",
    importantPoints: "<ul><li>The flattening formula: index <code>k</code> → <code>row = k/n</code>, <code>col = k%n</code>.</li><li><strong>Time:</strong> O(log(m×n)). <strong>Space:</strong> O(1).</li><li>Only works because the matrix is fully sorted end-to-end (not just each row).</li></ul>"
  },
  {
    id: 30,
    name: "Koko Eating Bananas",
    category: "Binary Search",
    categoryId: "binary-search",
    difficulty: "Medium",
    explanation: "<p><strong>Koko Eating Bananas:</strong> Koko has <code>piles</code> of bananas and <code>h</code> hours. She eats at speed <code>k</code> bananas/hour (1 pile per hour max). Find the minimum integer <code>k</code> such that she can eat all bananas in ≤ <code>h</code> hours.</p><p>For a pile of size <code>p</code> and speed <code>k</code>, it takes <code>ceil(p/k)</code> hours = <code>(p + k - 1) / k</code>.</p><p>As <code>k</code> increases, total hours decreases — this is a <strong>monotonic relationship</strong>, making it perfect for binary search on the answer space.</p>",
    bruteForce: "<p><strong>Linear Search on k:</strong> Try every speed from 1 to max(piles), return the first k where total hours ≤ h.</p><pre><code>for (int k = 1; k &lt;= max(piles); k++) {\n    if (canFinish(piles, k, h)) return k;\n}</code></pre><p><strong>Time:</strong> O(max(piles) × n). <strong>Space:</strong> O(1).</p>",
    bruteForceInefficiency: "<p>For piles up to 10^9, linear search would take 10^9 iterations. Binary search on the speed range [1, max(piles)] reduces this to O(log(max(piles)) × n) ≈ 30 × n iterations.</p>",
    optimalApproach: "<p><strong>Binary Search on Answer — O(n log(max(piles))) time, O(1) space:</strong></p><ol><li>Search space: <code>l = 1</code>, <code>r = max(piles)</code>.</li><li>For each candidate speed <code>mid</code>, compute total hours = Σ <code>ceil(pile/mid)</code>.</li><li>If total hours ≤ h, try slower (smaller k): <code>r = mid</code>.</li><li>If total hours &gt; h, need faster: <code>l = mid + 1</code>.</li><li>Return <code>l</code>.</li></ol>",
    javaCode: "class Solution {\n    public int minEatingSpeed(int[] piles, int h) {\n        int l = 1, r = 0;\n        for (int p : piles) r = Math.max(r, p);\n        while (l < r) {\n            int mid = l + (r - l) / 2;\n            long hours = 0;\n            for (int p : piles) hours += (p + mid - 1) / mid;\n            if (hours <= h) r = mid;\n            else l = mid + 1;\n        }\n        return l;\n    }\n}",
    dryRun: "<p><strong>Input:</strong> piles=[3,6,7,11], h=8</p><p>r = max = 11. Search [1,11].</p><table border='1' cellpadding='4'><tr><th>l</th><th>r</th><th>mid</th><th>hours</th><th>Action</th></tr><tr><td>1</td><td>11</td><td>6</td><td>ceil(3/6)+ceil(6/6)+ceil(7/6)+ceil(11/6) = 1+1+2+2=6</td><td>6≤8 → r=6</td></tr><tr><td>1</td><td>6</td><td>3</td><td>1+2+3+4=10</td><td>10&gt;8 → l=4</td></tr><tr><td>4</td><td>6</td><td>5</td><td>1+2+2+3=8</td><td>8≤8 → r=5</td></tr><tr><td>4</td><td>5</td><td>4</td><td>1+2+2+3=8</td><td>8≤8 → r=4</td></tr><tr><td>4</td><td>4</td><td>-</td><td>-</td><td>l==r → return 4</td></tr></table><p>Return <strong>4</strong> ✓</p>",
    interviewTips: "<ul><li><strong>Pattern:</strong> \"Minimum/Maximum feasible value\" → binary search on the answer space.</li><li><strong>Use long</strong> for hours sum to avoid overflow with large piles and small k.</li><li><strong>Why <code>l &lt; r</code> not <code>l &lt;= r</code>?</strong> We converge to a single value and <code>r = mid</code> (not mid-1), so <code>l == r</code> is the answer.</li><li>The feasibility check (can Koko finish in h hours at speed k) is the key helper function.</li></ul>",
    importantPoints: "<ul><li>Binary search on the <strong>answer space</strong>, not the input array.</li><li>Use <code>(p + mid - 1) / mid</code> for ceiling division without floating point.</li><li>Use <code>long</code> to avoid integer overflow in hours summation.</li><li><strong>Time:</strong> O(n log M) where M = max pile size. <strong>Space:</strong> O(1).</li></ul>"
  },
  {
    id: 31,
    name: "Find Minimum in Rotated Sorted Array",
    category: "Binary Search",
    categoryId: "binary-search",
    difficulty: "Medium",
    explanation: "<p><strong>Find Minimum in Rotated Sorted Array:</strong> A sorted array has been rotated at some unknown pivot. Find the minimum element in O(log n).</p><p>Example: <code>[3,4,5,1,2]</code> → 1. <code>[4,5,6,7,0,1,2]</code> → 0.</p><p><strong>Key insight:</strong> After rotation, the array has two sorted halves. The minimum is at the start of the right (rotated) half. Compare <code>nums[mid]</code> with <code>nums[r]</code> to determine which half we're in:<ul><li>If <code>nums[mid] &gt; nums[r]</code> → minimum is in the right half → <code>l = mid + 1</code>.</li><li>Else → minimum is in the left half (including mid) → <code>r = mid</code>.</li></ul></p>",
    bruteForce: "<p><strong>Linear Scan:</strong> Scan all elements and return the minimum.</p><pre><code>int min = Integer.MAX_VALUE;\nfor (int n : nums) min = Math.min(min, n);\nreturn min;</code></pre><p><strong>Time:</strong> O(n). <strong>Space:</strong> O(1).</p>",
    bruteForceInefficiency: "<p>Linear scan doesn't use the sorted (with rotation) structure. Binary search exploits the fact that comparing the middle element with the rightmost element tells us which half contains the minimum, halving the search space each step.</p>",
    optimalApproach: "<p><strong>Binary Search — O(log n) time, O(1) space:</strong></p><ol><li>Set <code>l = 0</code>, <code>r = nums.length - 1</code>.</li><li>While <code>l &lt; r</code>:<ul><li>Compute <code>mid</code>.</li><li>If <code>nums[mid] &gt; nums[r]</code>: minimum is to the right → <code>l = mid + 1</code>.</li><li>Else: minimum is at mid or to the left → <code>r = mid</code>.</li></ul></li><li>Return <code>nums[l]</code>.</li></ol>",
    javaCode: "class Solution {\n    public int findMin(int[] nums) {\n        int l = 0, r = nums.length - 1;\n        while (l < r) {\n            int mid = l + (r - l) / 2;\n            if (nums[mid] > nums[r]) l = mid + 1;\n            else r = mid;\n        }\n        return nums[l];\n    }\n}",
    dryRun: "<p><strong>Input:</strong> <code>[4, 5, 6, 7, 0, 1, 2]</code></p><table border='1' cellpadding='4'><tr><th>l</th><th>r</th><th>mid</th><th>nums[mid]</th><th>nums[r]</th><th>Action</th></tr><tr><td>0</td><td>6</td><td>3</td><td>7</td><td>2</td><td>7&gt;2 → l=4</td></tr><tr><td>4</td><td>6</td><td>5</td><td>1</td><td>2</td><td>1&lt;2 → r=5</td></tr><tr><td>4</td><td>5</td><td>4</td><td>0</td><td>1</td><td>0&lt;1 → r=4</td></tr><tr><td>4</td><td>4</td><td>-</td><td>-</td><td>-</td><td>l==r → return nums[4]=0</td></tr></table><p>Return <strong>0</strong> ✓</p>",
    interviewTips: "<ul><li><strong>Compare mid with r, not l.</strong> Comparing with l is trickier when the array isn't rotated.</li><li><strong>Why <code>l &lt; r</code>?</strong> We use <code>r = mid</code>, so to avoid infinite loop we use strict less-than.</li><li><strong>Follow-up:</strong> What if there are duplicates? (When <code>nums[mid] == nums[r]</code>, we can't determine which half — decrement r: <code>r--</code>.)</li><li>This pattern extends to \"Search in Rotated Sorted Array\" (problem 32).</li></ul>",
    importantPoints: "<ul><li>Comparing <code>nums[mid]</code> with <code>nums[r]</code> (not nums[l]) is the key decision.</li><li>Use <code>r = mid</code> (not mid-1) because mid could itself be the minimum.</li><li><strong>Time:</strong> O(log n). <strong>Space:</strong> O(1).</li><li>Works because rotation creates exactly one inflection point.</li></ul>"
  },
  {
    id: 32,
    name: "Search in Rotated Sorted Array",
    category: "Binary Search",
    categoryId: "binary-search",
    difficulty: "Medium",
    explanation: "<p><strong>Search in Rotated Sorted Array:</strong> Given a rotated sorted array with distinct elements and a target, return the index or -1 if not found, in O(log n).</p><p>Example: <code>[4,5,6,7,0,1,2]</code>, target=0 → 4.</p><p>Even after rotation, one half of the array is always sorted. We identify which half is sorted, check if the target falls in that range, and narrow our search accordingly.</p>",
    bruteForce: "<p><strong>Linear Search:</strong> Scan all elements.</p><pre><code>for (int i = 0; i &lt; nums.length; i++)\n    if (nums[i] == target) return i;\nreturn -1;</code></pre><p><strong>Time:</strong> O(n). <strong>Space:</strong> O(1).</p>",
    bruteForceInefficiency: "<p>Ignores the rotated-sorted structure. By identifying which half is sorted and whether the target lies in it, binary search eliminates half the elements each iteration — achieving O(log n).</p>",
    optimalApproach: "<p><strong>Modified Binary Search — O(log n) time, O(1) space:</strong></p><ol><li>Standard binary search setup with l and r.</li><li>At each step, determine if the <strong>left half</strong> [l..mid] is sorted (<code>nums[l] &lt;= nums[mid]</code>):<ul><li>If left is sorted and <code>target</code> is in [nums[l], nums[mid]) → search left.</li><li>Otherwise → search right.</li></ul></li><li>Else the <strong>right half</strong> [mid..r] is sorted:<ul><li>If target is in (nums[mid], nums[r]] → search right.</li><li>Otherwise → search left.</li></ul></li></ol>",
    javaCode: "class Solution {\n    public int search(int[] nums, int target) {\n        int l = 0, r = nums.length - 1;\n        while (l <= r) {\n            int mid = l + (r - l) / 2;\n            if (nums[mid] == target) return mid;\n            // Left half is sorted\n            if (nums[l] <= nums[mid]) {\n                if (target >= nums[l] && target < nums[mid]) r = mid - 1;\n                else l = mid + 1;\n            } else { // Right half is sorted\n                if (target > nums[mid] && target <= nums[r]) l = mid + 1;\n                else r = mid - 1;\n            }\n        }\n        return -1;\n    }\n}",
    dryRun: "<p><strong>Input:</strong> nums=[4,5,6,7,0,1,2], target=0</p><table border='1' cellpadding='4'><tr><th>l</th><th>r</th><th>mid</th><th>nums[mid]</th><th>Sorted half</th><th>Action</th></tr><tr><td>0</td><td>6</td><td>3</td><td>7</td><td>Left [4..7] sorted</td><td>0 not in [4,7) → l=4</td></tr><tr><td>4</td><td>6</td><td>5</td><td>1</td><td>Left [0..1] sorted? nums[4]=0≤1 yes</td><td>0 in [0,1) → r=4</td></tr><tr><td>4</td><td>4</td><td>4</td><td>0</td><td>-</td><td>0==0 → return 4</td></tr></table><p>Return <strong>4</strong> ✓</p>",
    interviewTips: "<ul><li><strong>Two decisions per iteration:</strong> (1) Which half is sorted? (2) Is target in that sorted half?</li><li><strong>Boundary conditions:</strong> Use <code>&gt;=</code> and <code>&lt;=</code> carefully for the range checks.</li><li><strong>Duplicates follow-up:</strong> When <code>nums[l] == nums[mid] == nums[r]</code>, we can't determine the sorted half — must do <code>l++; r--</code> (O(n) worst case).</li></ul>",
    importantPoints: "<ul><li>At every step, at least one half is guaranteed to be fully sorted.</li><li>The <code>nums[l] &lt;= nums[mid]</code> check identifies which half is sorted.</li><li><strong>Time:</strong> O(log n). <strong>Space:</strong> O(1).</li><li>Be careful with the boundary conditions in the range checks.</li></ul>"
  },
  {
    id: 33,
    name: "Time Based Key-Value Store",
    category: "Binary Search",
    categoryId: "binary-search",
    difficulty: "Medium",
    explanation: "<p><strong>Time Based Key-Value Store:</strong> Design a data structure that stores key-value pairs with timestamps. Support two operations:</p><ul><li><code>set(key, value, timestamp)</code>: Store the value at the given timestamp (timestamps are strictly increasing per key).</li><li><code>get(key, timestamp)</code>: Return the value with the largest timestamp ≤ the given timestamp. Return <code>\"\"</code> if none exists.</li></ul><p>Since timestamps are strictly increasing, each key maps to a list of (timestamp, value) pairs in sorted order — perfect for <strong>binary search</strong> in <code>get()</code>.</p>",
    bruteForce: "<p><strong>Linear scan for get():</strong> Store values in a list; for <code>get(key, t)</code>, scan all stored timestamps for that key and find the largest ≤ t.</p><pre><code>for (int i = list.size()-1; i >= 0; i--)\n    if (list.get(i).timestamp &lt;= t) return list.get(i).value;\nreturn \"\";</code></pre><p><strong>Time:</strong> set O(1), get O(n). <strong>Space:</strong> O(n).</p>",
    bruteForceInefficiency: "<p>For a key with many stored timestamps, linear scan is O(n). Since timestamps are guaranteed to be inserted in strictly increasing order, the list is always sorted, enabling binary search to find the floor timestamp in O(log n).</p>",
    optimalApproach: "<p><strong>HashMap + Binary Search — set O(1), get O(log n):</strong></p><ol><li>Store: <code>Map&lt;String, List&lt;String[]&gt;&gt;</code> where each entry is <code>[timestamp_str, value]</code>.</li><li><code>set()</code>: Append <code>[timestamp, value]</code> to the key's list.</li><li><code>get()</code>: Binary search for the largest timestamp ≤ given timestamp in the key's list. Use classic binary search tracking the best valid result.</li></ol>",
    javaCode: "class TimeMap {\n    private Map<String, List<String[]>> store;\n\n    public TimeMap() {\n        store = new HashMap<>();\n    }\n\n    public void set(String key, String value, int timestamp) {\n        store.computeIfAbsent(key, k -> new ArrayList<>())\n             .add(new String[]{String.valueOf(timestamp), value});\n    }\n\n    public String get(String key, int timestamp) {\n        if (!store.containsKey(key)) return \"\";\n        List<String[]> list = store.get(key);\n        int l = 0, r = list.size() - 1;\n        String res = \"\";\n        while (l <= r) {\n            int mid = l + (r - l) / 2;\n            if (Integer.parseInt(list.get(mid)[0]) <= timestamp) {\n                res = list.get(mid)[1]; // valid candidate\n                l = mid + 1;           // try to find larger timestamp\n            } else {\n                r = mid - 1;\n            }\n        }\n        return res;\n    }\n}",
    dryRun: "<p><strong>Operations:</strong></p><ol><li><code>set(\"foo\", \"bar\", 1)</code> → store: {\"foo\": [[\"1\",\"bar\"]]}</li><li><code>set(\"foo\", \"bar2\", 4)</code> → store: {\"foo\": [[\"1\",\"bar\"],[\"4\",\"bar2\"]]}</li><li><code>get(\"foo\", 4)</code> → Binary search in [[\"1\",\"bar\"],[\"4\",\"bar2\"]], target=4. mid=0→t=1≤4, res=\"bar\", l=1. mid=1→t=4≤4, res=\"bar2\", l=2. Return <strong>\"bar2\"</strong></li><li><code>get(\"foo\", 3)</code> → mid=0→t=1≤3, res=\"bar\", l=1. mid=1→t=4&gt;3, r=0. Return <strong>\"bar\"</strong></li></ol>",
    interviewTips: "<ul><li><strong>Why binary search works:</strong> The problem guarantees set() is called with strictly increasing timestamps, so each key's list is always sorted by timestamp.</li><li><strong>Alternative:</strong> Use <code>TreeMap&lt;Integer, String&gt;</code> per key and call <code>floorEntry(timestamp)</code> — cleaner code with same O(log n) complexity.</li><li><strong>Edge case:</strong> Key doesn't exist → return <code>\"\"</code>. Timestamp is before all stored times → return <code>\"\"</code>.</li></ul>",
    importantPoints: "<ul><li>Timestamps are strictly increasing per key → list is always sorted → binary search works.</li><li>Track <code>res</code> to save the last valid (≤ timestamp) value found.</li><li><strong>set:</strong> O(1). <strong>get:</strong> O(log n). <strong>Space:</strong> O(n) total.</li><li>Alternative: <code>TreeMap</code> with <code>floorEntry()</code> is a clean O(log n) solution.</li></ul>"
  },
  {
    id: 34,
    name: "Median of Two Sorted Arrays",
    category: "Binary Search",
    categoryId: "binary-search",
    difficulty: "Hard",
    explanation: "<p><strong>Median of Two Sorted Arrays:</strong> Given two sorted arrays <code>nums1</code> and <code>nums2</code>, find the median of the combined sorted array in O(log(m+n)) time.</p><p>The median divides the combined array into two equal halves. We need to find a partition in both arrays such that:</p><ul><li>All elements on the left side ≤ all elements on the right side.</li><li>The left side has exactly ⌊(m+n+1)/2⌋ elements.</li></ul><p>Binary search on the smaller array's partition point to find the correct split.</p>",
    bruteForce: "<p><strong>Merge and Find:</strong> Merge both sorted arrays and return the middle element(s).</p><pre><code>// Merge O(m+n), then access middle\nint[] merged = merge(nums1, nums2);\nint n = merged.length;\nif (n % 2 == 0) return (merged[n/2-1] + merged[n/2]) / 2.0;\nreturn merged[n/2];</code></pre><p><strong>Time:</strong> O(m+n). <strong>Space:</strong> O(m+n).</p>",
    bruteForceInefficiency: "<p>Merging requires visiting every element. The O(log(min(m,n))) binary search approach avoids this by directly computing the correct partition point without building the merged array.</p>",
    optimalApproach: "<p><strong>Binary Search on Partition — O(log(min(m,n))) time, O(1) space:</strong></p><ol><li>Ensure nums1 is the shorter array (swap if needed).</li><li>Binary search on the partition of nums1: <code>partX</code> elements from nums1 go to the left half.</li><li><code>partY = (m+n+1)/2 - partX</code> elements from nums2 go to the left half.</li><li>Check if partition is valid: <code>maxLeftX &lt;= minRightY</code> and <code>maxLeftY &lt;= minRightX</code>.</li><li>If valid, compute median from the boundary elements.</li><li>If <code>maxLeftX &gt; minRightY</code> → move partX left (<code>r = partX - 1</code>).</li><li>Else → move partX right (<code>l = partX + 1</code>).</li></ol>",
    javaCode: "class Solution {\n    public double findMedianSortedArrays(int[] nums1, int[] nums2) {\n        if (nums1.length > nums2.length)\n            return findMedianSortedArrays(nums2, nums1);\n        int m = nums1.length, n = nums2.length;\n        int l = 0, r = m;\n        while (l <= r) {\n            int partX = l + (r - l) / 2;\n            int partY = (m + n + 1) / 2 - partX;\n            int maxLeftX  = (partX == 0) ? Integer.MIN_VALUE : nums1[partX - 1];\n            int minRightX = (partX == m) ? Integer.MAX_VALUE : nums1[partX];\n            int maxLeftY  = (partY == 0) ? Integer.MIN_VALUE : nums2[partY - 1];\n            int minRightY = (partY == n) ? Integer.MAX_VALUE : nums2[partY];\n            if (maxLeftX <= minRightY && maxLeftY <= minRightX) {\n                if ((m + n) % 2 == 0)\n                    return (Math.max(maxLeftX, maxLeftY) + Math.min(minRightX, minRightY)) / 2.0;\n                else\n                    return Math.max(maxLeftX, maxLeftY);\n            } else if (maxLeftX > minRightY) {\n                r = partX - 1;\n            } else {\n                l = partX + 1;\n            }\n        }\n        return 0;\n    }\n}",
    dryRun: "<p><strong>Input:</strong> nums1=[1,3], nums2=[2], combined sorted=[1,2,3], median=2.0</p><p>m=2, n=1 (but m&gt;n so we swap: nums1=[2], nums2=[1,3], m=1, n=2)</p><p>l=0, r=1. partX=0→partY=2: maxLeftX=MIN, minRightX=2, maxLeftY=3, minRightY=MAX → maxLeftY(3)&gt;minRightX(2) → l=1</p><p>partX=1→partY=1: maxLeftX=2, minRightX=MAX, maxLeftY=1, minRightY=3 → 2≤3 and 1≤MAX → valid! (m+n)=3 odd → return max(2,1)=<strong>2.0</strong> ✓</p>",
    interviewTips: "<ul><li><strong>This is one of the hardest binary search problems.</strong> Explain the partition concept clearly before coding.</li><li>Always binary search on the <strong>smaller</strong> array to minimize iterations.</li><li>Use <code>Integer.MIN_VALUE</code> and <code>Integer.MAX_VALUE</code> as sentinels for edge partitions (partX=0 or partX=m).</li><li>The <code>(m+n+1)/2</code> formula works for both even and odd total lengths.</li></ul>",
    importantPoints: "<ul><li>Binary search on the partition point of the smaller array.</li><li>The valid partition condition: <code>maxLeftX &lt;= minRightY &amp;&amp; maxLeftY &lt;= minRightX</code>.</li><li><strong>Time:</strong> O(log(min(m,n))). <strong>Space:</strong> O(1).</li><li>Handle edge cases with sentinel values MIN_VALUE/MAX_VALUE when partition is at the boundary.</li></ul>"
  }
];
