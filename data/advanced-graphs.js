window.ADVANCED_GRAPHS_DATA = [
  {
    id: 93,
    name: "Reconstruct Itinerary",
    category: "Advanced Graphs",
    categoryId: "advanced-graphs",
    difficulty: "Hard",
    explanation: "<p><strong>Reconstruct Itinerary</strong>: given a list of airline tickets represented as [from, to] pairs, reconstruct the itinerary in order starting from 'JFK'. If multiple valid itineraries exist, return the one with the smallest lexical order. All tickets must be used exactly once.</p><p>This is an <strong>Eulerian path</strong> problem — we must traverse every edge exactly once. Hierholzer's algorithm finds an Eulerian path using a post-order DFS: when a node has no more outgoing edges, add it to the result. The trick is using a min-heap (PriorityQueue) per source to always choose the lexicographically smallest destination first.</p>",
    bruteForce: "<p>Try all permutations of tickets and verify which one forms a valid itinerary starting from JFK. O(n!) — impractical for large inputs. Even DFS with backtracking on all permutations is too slow without the Hierholzer optimization.</p>",
    bruteForceInefficiency: "<p>Naive DFS with backtracking could explore exponentially many paths. Hierholzer's algorithm runs in O(E log E) (the log factor comes from the priority queue) and never backtracks — once a node runs out of edges, it's added to the result directly.</p>",
    optimalApproach: "<p><strong>Hierholzer's algorithm with PriorityQueue:</strong> build a map from each source to a min-heap of destinations. DFS from 'JFK': while the current node has remaining destinations, pop the smallest, recurse. After all destinations exhausted, prepend node to result (or add and reverse). Time: O(E log E), Space: O(E).</p>",
    javaCode: "class Solution {\n    Map<String, PriorityQueue<String>> graph = new HashMap<>();\n    List<String> result = new ArrayList<>();\n    public List<String> findItinerary(List<List<String>> tickets) {\n        for (List<String> ticket : tickets)\n            graph.computeIfAbsent(ticket.get(0), k -> new PriorityQueue<>()).offer(ticket.get(1));\n        dfs(\"JFK\");\n        Collections.reverse(result);\n        return result;\n    }\n    private void dfs(String src) {\n        while (graph.containsKey(src) && !graph.get(src).isEmpty())\n            dfs(graph.get(src).poll()); // always take smallest destination\n        result.add(src); // post-order: add after all outgoing edges used\n    }\n}",
    dryRun: "<p><strong>Input:</strong> tickets = [[\"JFK\",\"SFO\"],[\"JFK\",\"ATL\"],[\"SFO\",\"ATL\"],[\"ATL\",\"JFK\"],[\"ATL\",\"SFO\"]]</p><ol><li>graph: JFK→[ATL,SFO], SFO→[ATL], ATL→[JFK,SFO].</li><li>dfs(\"JFK\"): smallest is ATL. dfs(\"ATL\"): smallest is JFK. dfs(\"JFK\"): smallest is SFO. dfs(\"SFO\"): smallest is ATL. dfs(\"ATL\"): smallest is SFO. dfs(\"SFO\"): no more → add \"SFO\".</li><li>Back to ATL: add \"ATL\". Back to SFO: add \"SFO\". Back to JFK: add \"JFK\". Back to ATL: add \"ATL\". Back to JFK: add \"JFK\".</li><li>result (pre-reverse) = [\"SFO\",\"ATL\",\"SFO\",\"JFK\",\"ATL\",\"JFK\"]. Reversed = [\"JFK\",\"ATL\",\"JFK\",\"SFO\",\"ATL\",\"SFO\"].</li></ol>",
    interviewTips: "<ul><li>Hierholzer's algorithm: key insight is that post-order insertion gives reverse topological/Eulerian path.</li><li>PriorityQueue ensures lexicographic ordering without sorting the entire path.</li><li>The algorithm works even if the graph has dead ends (nodes where you might get stuck) — post-order handles them correctly.</li><li>The reverse at the end is because we're doing reverse post-order traversal.</li></ul>",
    importantPoints: "<ul><li>Use PriorityQueue (min-heap) per source for lexicographic order.</li><li>Post-order DFS: add to result AFTER exhausting all outgoing edges.</li><li>Reverse the result at the end.</li><li>Handles Eulerian paths (not circuits) by starting from a specific node.</li><li>Time O(E log E) — dominated by priority queue operations.</li></ul>"
  },
  {
    id: 94,
    name: "Min Cost to Connect All Points",
    category: "Advanced Graphs",
    categoryId: "advanced-graphs",
    difficulty: "Medium",
    explanation: "<p><strong>Min Cost to Connect All Points</strong>: given n points on a 2D plane, connect all points with minimum total Manhattan distance cost. Each connection costs |x1-x2| + |y1-y2|. This is the <strong>Minimum Spanning Tree (MST)</strong> problem.</p><p>Two classic MST algorithms apply: <strong>Prim's algorithm</strong> (greedy from one node, always extend via minimum cost edge) and <strong>Kruskal's algorithm</strong> (sort all edges, add non-cycle edges using Union-Find). For dense graphs (like this one with n² edges), Prim's is more efficient.</p>",
    bruteForce: "<p>Try all spanning trees by DFS/backtracking, compute the total cost of each, and return the minimum. For n nodes, the number of spanning trees is n^(n-2) by Cayley's formula — completely impractical.</p>",
    bruteForceInefficiency: "<p>Brute force is super-exponential. Prim's algorithm solves MST greedily in O(n²) for dense graphs (all n² edges exist), which is optimal since we must consider all O(n²) possible connections.</p>",
    optimalApproach: "<p><strong>Prim's algorithm (lazy, without heap):</strong> maintain minDist[] array (minimum cost to add each node to MST). Start from node 0. Each iteration: pick the unvisited node with smallest minDist (greedy), add its cost, update minDist for all remaining nodes. Time: O(n²), Space: O(n) — optimal for this dense graph.</p>",
    javaCode: "class Solution {\n    public int minCostConnectPoints(int[][] points) {\n        int n = points.length;\n        boolean[] inMST = new boolean[n];\n        int[] minDist = new int[n];\n        Arrays.fill(minDist, Integer.MAX_VALUE);\n        minDist[0] = 0;\n        int totalCost = 0;\n        for (int i = 0; i < n; i++) {\n            // Pick unvisited node with minimum distance\n            int u = -1;\n            for (int v = 0; v < n; v++)\n                if (!inMST[v] && (u == -1 || minDist[v] < minDist[u])) u = v;\n            inMST[u] = true;\n            totalCost += minDist[u];\n            // Update distances for remaining nodes\n            for (int v = 0; v < n; v++) {\n                int dist = Math.abs(points[u][0]-points[v][0]) + Math.abs(points[u][1]-points[v][1]);\n                if (!inMST[v] && dist < minDist[v]) minDist[v] = dist;\n            }\n        }\n        return totalCost;\n    }\n}",
    dryRun: "<p><strong>Input:</strong> points=[[0,0],[2,2],[3,10],[5,2],[7,0]]</p><ol><li>minDist=[0,INF,INF,INF,INF]. Pick node 0 (cost 0). Update: minDist=[0,4,13,7,7].</li><li>Pick node 1 (cost 4). inMST={0,1}. Update distances from node 1: minDist[2]=min(13,9)=9, minDist[3]=min(7,3)=3, minDist[4]=min(7,9)=7.</li><li>Pick node 3 (cost 3). Update from node 3: minDist[2]=min(9,8)=8, minDist[4]=min(7,2)=2.</li><li>Pick node 4 (cost 2). Update from node 4: minDist[2]=min(8,10)=8.</li><li>Pick node 2 (cost 8). Total = 0+4+3+2+8 = <strong>17</strong>.</li></ol>",
    interviewTips: "<ul><li>For sparse graphs, use Kruskal's with Union-Find: sort edges O(E log E), union non-redundant edges.</li><li>For dense graphs (like this one: O(n²) edges), Prim's O(n²) is better than Kruskal's O(n² log n).</li><li>With a priority queue, Prim's runs in O(E log V) — better for sparse graphs.</li><li>The MST connects all n nodes using exactly n-1 edges with minimum total weight.</li></ul>",
    importantPoints: "<ul><li>MST problem: connect all nodes with minimum total edge weight.</li><li>Prim's algorithm: greedily pick minimum-cost extension at each step.</li><li>minDist[v] = minimum Manhattan distance from v to any node already in MST.</li><li>O(n²) time and O(n) space — optimal for complete graph with n² edges.</li></ul>"
  },
  {
    id: 95,
    name: "Network Delay Time",
    category: "Advanced Graphs",
    categoryId: "advanced-graphs",
    difficulty: "Medium",
    explanation: "<p><strong>Network Delay Time</strong>: given n network nodes, a list of directed weighted edges (u, v, w) representing travel times, a source node k — find the time it takes for all nodes to receive a signal sent from k. If not all nodes can receive the signal, return -1.</p><p>This is the classic <strong>Single-Source Shortest Path (SSSP)</strong> problem, solved optimally by <strong>Dijkstra's algorithm</strong>. The answer is the maximum shortest-path distance from k to any node (since all must receive the signal).</p>",
    bruteForce: "<p>Bellman-Ford algorithm: relax all edges n-1 times. Each relaxation scans all edges. Time: O(V·E) = O(n·E). Correct but slower than Dijkstra for non-negative weights.</p>",
    bruteForceInefficiency: "<p>Bellman-Ford's O(V·E) is suboptimal when all weights are non-negative (as they are here — travel times can't be negative). Dijkstra's O(E log V) using a priority queue is significantly faster.</p>",
    optimalApproach: "<p><strong>Dijkstra's algorithm:</strong> maintain dist[] array (shortest distance from k to each node). Use a min-heap priority queue ordered by distance. Process each node at its minimum distance; relax outgoing edges. After all nodes processed, return max of dist[]. Return -1 if any node is unreachable. Time: O(E log V), Space: O(V+E).</p>",
    javaCode: "class Solution {\n    public int networkDelayTime(int[][] times, int n, int k) {\n        Map<Integer, List<int[]>> adj = new HashMap<>();\n        for (int[] t : times)\n            adj.computeIfAbsent(t[0], x -> new ArrayList<>()).add(new int[]{t[1], t[2]});\n        int[] dist = new int[n + 1];\n        Arrays.fill(dist, Integer.MAX_VALUE);\n        dist[k] = 0;\n        PriorityQueue<int[]> pq = new PriorityQueue<>((a, b) -> a[1] - b[1]); // min-heap by distance\n        pq.offer(new int[]{k, 0});\n        while (!pq.isEmpty()) {\n            int[] cur = pq.poll();\n            int node = cur[0], d = cur[1];\n            if (d > dist[node]) continue; // stale entry\n            if (!adj.containsKey(node)) continue;\n            for (int[] edge : adj.get(node)) {\n                int next = edge[0], w = edge[1];\n                if (dist[node] + w < dist[next]) {\n                    dist[next] = dist[node] + w;\n                    pq.offer(new int[]{next, dist[next]});\n                }\n            }\n        }\n        int max = 0;\n        for (int i = 1; i <= n; i++) {\n            if (dist[i] == Integer.MAX_VALUE) return -1;\n            max = Math.max(max, dist[i]);\n        }\n        return max;\n    }\n}",
    dryRun: "<p><strong>Input:</strong> times=[[2,1,1],[2,3,1],[3,4,1]], n=4, k=2</p><ol><li>dist=[INF,INF,0,INF,INF]. PQ={(2,0)}.</li><li>Poll (2,0). Relax: dist[1]=0+1=1, dist[3]=0+1=1. PQ={(1,1),(3,1)}.</li><li>Poll (1,1). Node 1 has no outgoing edges.</li><li>Poll (3,1). Relax: dist[4]=1+1=2. PQ={(4,2)}.</li><li>Poll (4,2). No edges from 4.</li><li>dist=[INF,1,0,1,2]. max(1,0,1,2)=<strong>2</strong>.</li></ol>",
    interviewTips: "<ul><li>Dijkstra requires non-negative edge weights — which travel times guarantee.</li><li>The 'stale entry' check (<code>if (d > dist[node]) continue</code>) handles multiple entries for the same node in the PQ.</li><li>The answer is max(dist[]) because we need ALL nodes to receive the signal.</li><li>For negative weights, use Bellman-Ford; for dense graphs, consider Floyd-Warshall.</li></ul>",
    importantPoints: "<ul><li>Dijkstra's SSSP with min-heap: O(E log V) time.</li><li>Skip stale PQ entries with <code>d > dist[node]</code> check.</li><li>Return -1 if any node remains at INF distance (unreachable).</li><li>Answer = maximum of all shortest distances (bottleneck is the last node to receive signal).</li></ul>"
  },
  {
    id: 96,
    name: "Swim in Rising Water",
    category: "Advanced Graphs",
    categoryId: "advanced-graphs",
    difficulty: "Hard",
    explanation: "<p><strong>Swim in Rising Water</strong>: given an n×n grid where grid[i][j] is the elevation, find the minimum time T such that there's a path from (0,0) to (n-1,n-1) where every cell on the path has elevation ≤ T (water rises to level T at time T).</p><p>This is a <strong>minimax path problem</strong>: minimize the maximum elevation on any path. Dijkstra's algorithm (or binary search + BFS) solves this elegantly. The 'distance' to each cell is the maximum elevation encountered on the best path to that cell.</p>",
    bruteForce: "<p>Binary search on the answer T (0 to n²-1), and for each T check if there's a path using BFS/DFS where all cells have elevation ≤ T. Time: O(n² log n²) = O(n² log n). Correct but slightly more complex than the Dijkstra approach.</p>",
    bruteForceInefficiency: "<p>Binary search + BFS is O(n² log n) and requires running BFS for each binary search iteration. Dijkstra directly finds the minimax path in O(n² log n) with a single traversal and more elegant code.</p>",
    optimalApproach: "<p><strong>Modified Dijkstra (minimax):</strong> use a min-heap where each entry is {maxElevation, i, j}. The 'distance' to a cell is the maximum elevation on the best path. When dequeuing (n-1,n-1), return its maxElevation. For neighbors, push {max(currentMax, grid[ni][nj]), ni, nj} if not visited. Time: O(n² log n), Space: O(n²).</p>",
    javaCode: "class Solution {\n    public int swimInWater(int[][] grid) {\n        int n = grid.length;\n        PriorityQueue<int[]> pq = new PriorityQueue<>((a, b) -> a[0] - b[0]); // min by max-elevation-so-far\n        boolean[][] visited = new boolean[n][n];\n        pq.offer(new int[]{grid[0][0], 0, 0});\n        int[][] dirs = {{0,1},{0,-1},{1,0},{-1,0}};\n        while (!pq.isEmpty()) {\n            int[] cur = pq.poll();\n            int t = cur[0], i = cur[1], j = cur[2];\n            if (i == n-1 && j == n-1) return t; // reached destination\n            if (visited[i][j]) continue;\n            visited[i][j] = true;\n            for (int[] d : dirs) {\n                int ni = i+d[0], nj = j+d[1];\n                if (ni>=0 && ni<n && nj>=0 && nj<n && !visited[ni][nj])\n                    pq.offer(new int[]{Math.max(t, grid[ni][nj]), ni, nj});\n            }\n        }\n        return -1;\n    }\n}",
    dryRun: "<p><strong>Input:</strong> grid=[[0,2],[1,3]]</p><ol><li>PQ={(0,0,0)}. Poll (t=0,i=0,j=0). Visit (0,0). Push neighbors: {max(0,2)=2, 0,1} and {max(0,1)=1, 1,0}.</li><li>PQ={(1,1,0),(2,0,1)}. Poll (t=1,i=1,j=0). Visit (1,0). Push {max(1,3)=3, 1,1} and {max(1,2)=2, 0,1 — already in PQ}.</li><li>PQ={(2,0,1),(3,1,1)}. Poll (t=2,i=0,j=1). Visit (0,1). Push {max(2,3)=3, 1,1}.</li><li>PQ={(3,1,1),(3,1,1)}. Poll (t=3,i=1,j=1). i==n-1 && j==n-1 → return <strong>3</strong>.</li></ol>",
    interviewTips: "<ul><li>The minimax path problem: minimize the maximum edge weight on a path.</li><li>Modified Dijkstra: instead of sum of distances, track max elevation seen.</li><li>Binary search + BFS is an alternative: binary search on T, BFS to check reachability.</li><li>The grid values are a permutation of 0..n²-1, so the answer is between grid[0][0] and n²-1.</li></ul>",
    importantPoints: "<ul><li>Priority queue key = max elevation seen on path so far (not sum).</li><li>When popping (n-1, n-1) for the first time, that's the optimal answer.</li><li>This is a modified Dijkstra for minimax (bottleneck) shortest path.</li><li>Time O(n² log n), Space O(n²) for visited array and PQ.</li></ul>"
  },
  {
    id: 97,
    name: "Alien Dictionary",
    category: "Advanced Graphs",
    categoryId: "advanced-graphs",
    difficulty: "Hard",
    explanation: "<p><strong>Alien Dictionary</strong>: given a sorted list of words in an alien language, derive the order of characters in that language's alphabet. Return any valid ordering, or empty string if the ordering is invalid (cycle detected).</p><p>We extract character ordering constraints by comparing adjacent words: the first position where they differ tells us character A comes before character B in the alien alphabet. Build a directed graph of these constraints, then perform <strong>topological sort</strong>. A cycle means the ordering is contradictory → return empty string.</p>",
    bruteForce: "<p>Try all permutations of unique characters and check if each permutation is consistent with all extracted ordering constraints. O(k! · n · L) where k=unique chars, n=words, L=max word length — completely impractical even for k=10.</p>",
    bruteForceInefficiency: "<p>Permutation checking is factorial. Topological sort processes all edges once: O(V+E) = O(unique chars + word pairs) after an O(n·L) constraint extraction step.</p>",
    optimalApproach: "<p><strong>Topological sort (Kahn's BFS):</strong> extract constraints by comparing adjacent words. Build adjacency list and in-degree map. BFS from 0-in-degree characters. If output length equals number of unique characters → valid. If a word is a prefix of the previous word but longer → invalid. Time: O(n·L + V + E), Space: O(V+E).</p>",
    javaCode: "class Solution {\n    public String alienOrder(String[] words) {\n        Map<Character, Set<Character>> adj = new HashMap<>();\n        Map<Character, Integer> inDegree = new HashMap<>();\n        // Initialize all characters\n        for (String w : words) for (char c : w.toCharArray()) {\n            adj.putIfAbsent(c, new HashSet<>());\n            inDegree.putIfAbsent(c, 0);\n        }\n        // Extract ordering constraints\n        for (int i = 0; i < words.length - 1; i++) {\n            String w1 = words[i], w2 = words[i+1];\n            if (w1.length() > w2.length() && w1.startsWith(w2)) return \"\"; // invalid\n            for (int j = 0; j < Math.min(w1.length(), w2.length()); j++) {\n                if (w1.charAt(j) != w2.charAt(j)) {\n                    if (!adj.get(w1.charAt(j)).contains(w2.charAt(j))) {\n                        adj.get(w1.charAt(j)).add(w2.charAt(j));\n                        inDegree.merge(w2.charAt(j), 1, Integer::sum);\n                    }\n                    break; // only first differing char matters\n                }\n            }\n        }\n        // Kahn's BFS topological sort\n        Queue<Character> q = new LinkedList<>();\n        for (char c : inDegree.keySet()) if (inDegree.get(c) == 0) q.offer(c);\n        StringBuilder sb = new StringBuilder();\n        while (!q.isEmpty()) {\n            char c = q.poll(); sb.append(c);\n            for (char next : adj.get(c)) {\n                inDegree.merge(next, -1, Integer::sum);\n                if (inDegree.get(next) == 0) q.offer(next);\n            }\n        }\n        return sb.length() == inDegree.size() ? sb.toString() : \"\";\n    }\n}",
    dryRun: "<p><strong>Input:</strong> words=[\"wrt\",\"wrf\",\"er\",\"ett\",\"rftt\"]</p><ol><li>Compare \"wrt\" vs \"wrf\": first diff at j=2: t→f. Edge t→f.</li><li>Compare \"wrf\" vs \"er\": first diff at j=0: w→e. Edge w→e.</li><li>Compare \"er\" vs \"ett\": first diff at j=1: r→t. Edge r→t.</li><li>Compare \"ett\" vs \"rftt\": first diff at j=0: e→r. Edge e→r.</li><li>In-degrees: w=0,r=1,t=1,f=1,e=1. BFS starts with w.</li><li>Process w→e (inDegree[e]=0). Process e→r (inDegree[r]=0). Process r→t (inDegree[t]=0). Process t→f (inDegree[f]=0). Process f.</li><li>Result: \"wertf\".</li></ol>",
    interviewTips: "<ul><li>Critical edge case: if word1 is longer than word2 and word2 is a prefix of word1 → invalid (e.g., [\"abc\",\"ab\"]).</li><li>Only the FIRST differing character between adjacent words contributes an edge.</li><li>Use a Set for adjacency to avoid duplicate edges (which would corrupt in-degree counts).</li><li>If topological sort output length < number of unique characters → cycle → return empty string.</li></ul>",
    importantPoints: "<ul><li>Extract constraints only from ADJACENT words in the sorted list.</li><li>Only the first position of difference matters per pair.</li><li>Kahn's topological sort detects cycles: output length &lt; node count means cycle.</li><li>Initialize in-degree map for ALL characters, not just those appearing in constraints.</li></ul>"
  },
  {
    id: 98,
    name: "Cheapest Flights Within K Stops",
    category: "Advanced Graphs",
    categoryId: "advanced-graphs",
    difficulty: "Medium",
    explanation: "<p><strong>Cheapest Flights Within K Stops</strong>: given n cities, directed flights with costs, a source, destination, and maximum K stops (not counting source, but counting intermediate cities), find the cheapest price. Return -1 if no route exists.</p><p>Standard Dijkstra doesn't work directly because we also need to track the number of stops. The elegant solution is a <strong>modified Bellman-Ford</strong>: run K+1 relaxation rounds (K stops = K+1 edges). Each round only uses prices from the previous round (using a copy) to prevent using more than one edge per round.</p>",
    bruteForce: "<p>BFS/DFS trying all paths with at most K stops, tracking costs. Without memoization, this is exponential. With proper visited tracking (state = city + stops remaining), it's O(n·K) states but can be slow in practice.</p>",
    bruteForceInefficiency: "<p>BFS with (city, stops) state works but requires careful implementation. Modified Bellman-Ford is simpler and runs in O(K·E) — exactly K+1 rounds of relaxation, each scanning all E edges.</p>",
    optimalApproach: "<p><strong>Modified Bellman-Ford:</strong> prices[] = shortest cost to reach each city. Run K+1 rounds. Each round: copy prices to tmp[], scan all edges — if prices[f[0]] + f[2] &lt; tmp[f[1]], update tmp. Replace prices with tmp. Answer is prices[dst] (or -1 if INF). Time: O(K·E), Space: O(n).</p>",
    javaCode: "class Solution {\n    public int findCheapestPrice(int n, int[][] flights, int src, int dst, int k) {\n        int[] prices = new int[n];\n        Arrays.fill(prices, Integer.MAX_VALUE);\n        prices[src] = 0;\n        for (int i = 0; i <= k; i++) { // K+1 rounds = up to K stops\n            int[] tmp = Arrays.copyOf(prices, n); // use previous round's values\n            for (int[] f : flights) {\n                if (prices[f[0]] != Integer.MAX_VALUE && prices[f[0]] + f[2] < tmp[f[1]])\n                    tmp[f[1]] = prices[f[0]] + f[2];\n            }\n            prices = tmp;\n        }\n        return prices[dst] == Integer.MAX_VALUE ? -1 : prices[dst];\n    }\n}",
    dryRun: "<p><strong>Input:</strong> n=4, flights=[[0,1,100],[1,2,100],[2,0,100],[1,3,600],[2,3,200]], src=0, dst=3, k=1</p><ol><li>Initial: prices=[0,INF,INF,INF].</li><li>Round 0 (0 stops, 1 edge): tmp=[0,INF,INF,INF]. Scan flights: 0→1: tmp[1]=min(INF,0+100)=100. 1→2: prices[1]=INF skip. 1→3: skip. 2→3: skip. prices=[0,100,INF,INF].</li><li>Round 1 (1 stop, 2 edges): tmp=[0,100,INF,INF]. 0→1: tmp[1]=min(100,100)=100. 1→2: tmp[2]=min(INF,100+100)=200. 1→3: tmp[3]=min(INF,100+600)=700. 2→3: prices[2]=INF skip. prices=[0,100,200,700].</li><li>prices[3]=700 → return <strong>700</strong>.</li></ol>",
    interviewTips: "<ul><li>The tmp copy is critical — it prevents using multiple edges in a single round (which would violate the K-stops constraint).</li><li>K stops = K+1 edges = K+1 Bellman-Ford relaxation rounds.</li><li>Dijkstra with (cost, node, stops) state also works but is more complex.</li><li>Why not standard Dijkstra? Because the 'cheapest' path might use more stops than allowed — we can't greedily ignore stop count.</li></ul>",
    importantPoints: "<ul><li>K stops means at most K+1 edges (flights) in the path.</li><li>Run exactly K+1 Bellman-Ford rounds.</li><li>Use a copy of prices[] each round to enforce the edge-per-round constraint.</li><li>Time O(K·E) — scales with K and number of flights.</li><li>The source city has cost 0; all others start at INF.</li></ul>"
  }
];
