window.LINKED_LIST_DATA = [
  {
    id: 35,
    name: "Reverse Linked List",
    category: "Linked List",
    categoryId: "linked-list",
    difficulty: "Easy",
    explanation: "<p><strong>Reverse Linked List:</strong> Given the head of a singly linked list, reverse it and return the new head.</p><p>Example: <code>1→2→3→4→5→null</code> becomes <code>5→4→3→2→1→null</code>.</p><p>The key challenge: when we redirect a node's <code>next</code> pointer, we lose the reference to the rest of the list. We need a <code>prev</code> pointer to build the reversed links and a <code>next</code> temp variable to save the forward reference before breaking it.</p>",
    bruteForce: "<p><strong>Collect and Rebuild:</strong> Collect all node values into a list, reverse the list, then rebuild the linked list.</p><pre><code>List&lt;Integer&gt; vals = new ArrayList&lt;&gt;();\nwhile (head != null) { vals.add(head.val); head = head.next; }\n// rebuild in reverse...</code></pre><p><strong>Time:</strong> O(n). <strong>Space:</strong> O(n).</p>",
    bruteForceInefficiency: "<p>Uses O(n) extra space unnecessarily. The in-place iterative approach reverses the pointers directly with O(1) extra space using just three pointers.</p>",
    optimalApproach: "<p><strong>Iterative In-Place — O(n) time, O(1) space:</strong></p><ol><li>Initialize <code>prev = null</code>, <code>curr = head</code>.</li><li>While <code>curr != null</code>:<ul><li>Save <code>next = curr.next</code>.</li><li>Redirect: <code>curr.next = prev</code>.</li><li>Advance: <code>prev = curr</code>, <code>curr = next</code>.</li></ul></li><li>Return <code>prev</code> (new head).</li></ol>",
    javaCode: "class Solution {\n    public ListNode reverseList(ListNode head) {\n        ListNode prev = null, curr = head;\n        while (curr != null) {\n            ListNode next = curr.next; // save next\n            curr.next = prev;          // reverse pointer\n            prev = curr;               // advance prev\n            curr = next;               // advance curr\n        }\n        return prev;\n    }\n}",
    dryRun: "<p><strong>Input:</strong> 1→2→3→null</p><table border='1' cellpadding='4'><tr><th>prev</th><th>curr</th><th>next</th><th>After curr.next=prev</th></tr><tr><td>null</td><td>1</td><td>2</td><td>1→null</td></tr><tr><td>1</td><td>2</td><td>3</td><td>2→1→null</td></tr><tr><td>2</td><td>3</td><td>null</td><td>3→2→1→null</td></tr><tr><td>3</td><td>null</td><td>-</td><td>loop ends</td></tr></table><p>Return <code>prev</code> = node(3) = <strong>3→2→1→null</strong> ✓</p>",
    interviewTips: "<ul><li><strong>Always save <code>next</code> before redirecting</strong> — forgetting this loses the rest of the list.</li><li><strong>Recursive solution:</strong> <code>reverseList(head.next)</code> then <code>head.next.next = head; head.next = null;</code> — elegant but O(n) stack space.</li><li><strong>Edge cases:</strong> null head (return null), single node (return it unchanged).</li><li>Draw the pointer diagram step by step in interviews — it shows clarity of thought.</li></ul>",
    importantPoints: "<ul><li>Three pointers: <code>prev</code>, <code>curr</code>, <code>next</code>.</li><li>Save <code>next</code> BEFORE redirecting <code>curr.next</code>.</li><li>Return <code>prev</code>, not <code>curr</code> (curr is null at end).</li><li><strong>Time:</strong> O(n). <strong>Space:</strong> O(1) iterative, O(n) recursive.</li></ul>"
  },
  {
    id: 36,
    name: "Merge Two Sorted Lists",
    category: "Linked List",
    categoryId: "linked-list",
    difficulty: "Easy",
    explanation: "<p><strong>Merge Two Sorted Lists:</strong> Given the heads of two sorted linked lists <code>l1</code> and <code>l2</code>, merge them into one sorted linked list and return the head.</p><p>Example: <code>1→2→4</code> and <code>1→3→4</code> → <code>1→1→2→3→4→4</code>.</p><p>This is like the merge step in merge sort. Use a <strong>dummy head node</strong> to simplify edge cases — you never need to special-case an empty result list.</p>",
    bruteForce: "<p>Collect all values from both lists into an array, sort it, then build a new linked list. <strong>Time:</strong> O((m+n) log(m+n)). <strong>Space:</strong> O(m+n). Ignores the fact that both lists are already sorted.</p>",
    bruteForceInefficiency: "<p>Sorting is unnecessary since both lists are already sorted. The two-pointer merge runs in O(m+n) with O(1) extra space by directly rewiring next pointers.</p>",
    optimalApproach: "<p><strong>Two-Pointer Merge — O(m+n) time, O(1) space:</strong></p><ol><li>Create a <code>dummy</code> node; maintain a <code>cur</code> pointer at the tail of the merged list.</li><li>While both lists have nodes:<ul><li>Attach the smaller node to <code>cur.next</code> and advance that list's pointer.</li><li>Advance <code>cur</code>.</li></ul></li><li>Attach whichever list still has remaining nodes.</li><li>Return <code>dummy.next</code>.</li></ol>",
    javaCode: "class Solution {\n    public ListNode mergeTwoLists(ListNode l1, ListNode l2) {\n        ListNode dummy = new ListNode(0), cur = dummy;\n        while (l1 != null && l2 != null) {\n            if (l1.val <= l2.val) { cur.next = l1; l1 = l1.next; }\n            else                  { cur.next = l2; l2 = l2.next; }\n            cur = cur.next;\n        }\n        cur.next = (l1 != null) ? l1 : l2;\n        return dummy.next;\n    }\n}",
    dryRun: "<p><strong>Input:</strong> l1: 1→2→4, l2: 1→3→4</p><table border='1' cellpadding='4'><tr><th>l1</th><th>l2</th><th>Append</th><th>Merged so far</th></tr><tr><td>1→2→4</td><td>1→3→4</td><td>l1(1)≤l2(1) → l1</td><td>dummy→1</td></tr><tr><td>2→4</td><td>1→3→4</td><td>l2(1)&lt;l1(2) → l2</td><td>dummy→1→1</td></tr><tr><td>2→4</td><td>3→4</td><td>l1(2)≤l2(3) → l1</td><td>dummy→1→1→2</td></tr><tr><td>4</td><td>3→4</td><td>l2(3)&lt;l1(4) → l2</td><td>dummy→1→1→2→3</td></tr><tr><td>4</td><td>4</td><td>l1(4)≤l2(4) → l1</td><td>dummy→1→1→2→3→4</td></tr><tr><td>null</td><td>4</td><td>append l2</td><td>dummy→1→1→2→3→4→4</td></tr></table><p>Return <strong>1→1→2→3→4→4</strong> ✓</p>",
    interviewTips: "<ul><li><strong>Dummy node trick:</strong> Using a dummy head eliminates special cases for the first node. Always use this pattern for list construction problems.</li><li><strong>Recursive solution:</strong> Clean but uses O(m+n) stack space — mention it but prefer iterative.</li><li><strong>Edge cases:</strong> One or both lists empty — the <code>cur.next = l1 != null ? l1 : l2</code> line handles both.</li></ul>",
    importantPoints: "<ul><li>Dummy head simplifies code — no special handling for empty result.</li><li>Use <code>cur.next = remaining</code> at the end instead of a loop (both lists are already sorted).</li><li><strong>Time:</strong> O(m+n). <strong>Space:</strong> O(1) (reuses existing nodes).</li></ul>"
  },
  {
    id: 37,
    name: "Reorder List",
    category: "Linked List",
    categoryId: "linked-list",
    difficulty: "Medium",
    explanation: "<p><strong>Reorder List:</strong> Given a linked list <code>L0→L1→…→Ln-1→Ln</code>, reorder it to <code>L0→Ln→L1→Ln-1→L2→Ln-2→…</code> in-place.</p><p>Example: <code>1→2→3→4→5</code> → <code>1→5→2→4→3</code>.</p><p>The solution involves three classic linked list techniques chained together:</p><ol><li><strong>Find the middle</strong> (slow/fast pointers).</li><li><strong>Reverse the second half.</strong></li><li><strong>Merge the two halves</strong> by interleaving.</li></ol>",
    bruteForce: "<p>Store all node references in an array, then use two pointers (front and back) to relink nodes in the desired order. <strong>Time:</strong> O(n). <strong>Space:</strong> O(n).</p>",
    bruteForceInefficiency: "<p>Using an array requires O(n) extra space. The three-step in-place approach achieves O(1) extra space by directly manipulating pointers.</p>",
    optimalApproach: "<p><strong>Find Middle + Reverse + Merge — O(n) time, O(1) space:</strong></p><ol><li><strong>Find middle:</strong> Use slow/fast pointers. When fast reaches end, slow is at middle. Cut the list at slow.</li><li><strong>Reverse second half:</strong> Standard in-place reversal starting from <code>slow.next</code>.</li><li><strong>Merge:</strong> Interleave nodes from the first half and reversed second half.</li></ol>",
    javaCode: "class Solution {\n    public void reorderList(ListNode head) {\n        if (head == null || head.next == null) return;\n        // Step 1: Find middle\n        ListNode slow = head, fast = head;\n        while (fast.next != null && fast.next.next != null) {\n            slow = slow.next;\n            fast = fast.next.next;\n        }\n        // Step 2: Reverse second half\n        ListNode prev = null, curr = slow.next;\n        slow.next = null; // cut the list\n        while (curr != null) {\n            ListNode next = curr.next;\n            curr.next = prev;\n            prev = curr;\n            curr = next;\n        }\n        // Step 3: Merge two halves\n        ListNode first = head, second = prev;\n        while (second != null) {\n            ListNode tmp1 = first.next, tmp2 = second.next;\n            first.next = second;\n            second.next = tmp1;\n            first = tmp1;\n            second = tmp2;\n        }\n    }\n}",
    dryRun: "<p><strong>Input:</strong> 1→2→3→4→5</p><p><strong>Step 1 (find middle):</strong> slow=3, fast=5. Cut: first half: 1→2→3, second: 4→5</p><p><strong>Step 2 (reverse second half):</strong> 4→5 becomes 5→4</p><p><strong>Step 3 (merge):</strong></p><table border='1' cellpadding='4'><tr><th>first</th><th>second</th><th>Action</th><th>List</th></tr><tr><td>1</td><td>5</td><td>1→5, 5→2</td><td>1→5→2→3</td></tr><tr><td>2</td><td>4</td><td>2→4, 4→3</td><td>1→5→2→4→3</td></tr><tr><td>3</td><td>null</td><td>loop ends</td><td>1→5→2→4→3 ✓</td></tr></table>",
    interviewTips: "<ul><li><strong>Three distinct sub-problems:</strong> State each step clearly before coding — find middle, reverse, merge.</li><li><strong>Crucial detail:</strong> Set <code>slow.next = null</code> before reversing to properly cut the list into two halves.</li><li><strong>Slow/fast pointer:</strong> When <code>fast.next == null</code> or <code>fast.next.next == null</code>, slow is at the middle.</li><li>The merge loop runs while <code>second != null</code> (second half may be shorter by 1 for odd-length lists).</li></ul>",
    importantPoints: "<ul><li>Cut the list at the middle node before reversing the second half.</li><li>Save both <code>tmp1</code> and <code>tmp2</code> before relinking in the merge step.</li><li><strong>Time:</strong> O(n). <strong>Space:</strong> O(1).</li><li>The method is <code>void</code> — modifies the list in-place.</li></ul>"
  },
  {
    id: 38,
    name: "Remove Nth Node From End of List",
    category: "Linked List",
    categoryId: "linked-list",
    difficulty: "Medium",
    explanation: "<p><strong>Remove Nth Node From End:</strong> Given the head of a linked list and an integer <code>n</code>, remove the nth node from the end in one pass.</p><p>Example: 1→2→3→4→5, n=2 → 1→2→3→5 (remove node with value 4).</p><p>We don't know the length upfront. The <strong>two-pointer gap technique</strong> solves this in one pass: advance the fast pointer n+1 steps ahead, then move both until fast is null. The slow pointer lands just before the node to remove.</p>",
    bruteForce: "<p><strong>Two-pass:</strong> First pass counts the length L. Second pass goes to node (L - n) and removes the next node.</p><p><strong>Time:</strong> O(n). <strong>Space:</strong> O(1). Works but uses two passes.</p>",
    bruteForceInefficiency: "<p>Two passes work but require scanning the list twice. The gap technique achieves the same result in one pass using two pointers with a fixed gap of n+1 between them.</p>",
    optimalApproach: "<p><strong>Two-Pointer Gap Technique (One Pass) — O(n) time, O(1) space:</strong></p><ol><li>Create a <code>dummy</code> node pointing to head.</li><li>Set both <code>fast</code> and <code>slow</code> to dummy.</li><li>Advance <code>fast</code> n+1 times (so the gap between fast and slow is n+1).</li><li>Move both <code>fast</code> and <code>slow</code> until <code>fast == null</code>.</li><li><code>slow</code> now points to the node <em>before</em> the target. Set <code>slow.next = slow.next.next</code>.</li><li>Return <code>dummy.next</code>.</li></ol>",
    javaCode: "class Solution {\n    public ListNode removeNthFromEnd(ListNode head, int n) {\n        ListNode dummy = new ListNode(0, head);\n        ListNode fast = dummy, slow = dummy;\n        // Advance fast n+1 steps\n        for (int i = 0; i <= n; i++) fast = fast.next;\n        // Move both until fast is null\n        while (fast != null) {\n            fast = fast.next;\n            slow = slow.next;\n        }\n        // slow is just before the node to remove\n        slow.next = slow.next.next;\n        return dummy.next;\n    }\n}",
    dryRun: "<p><strong>Input:</strong> 1→2→3→4→5, n=2</p><p>dummy→1→2→3→4→5. Advance fast 3 steps (n+1=3): fast=node(3).</p><table border='1' cellpadding='4'><tr><th>slow</th><th>fast</th><th>Action</th></tr><tr><td>dummy</td><td>3</td><td>start</td></tr><tr><td>1</td><td>4</td><td>move both</td></tr><tr><td>2</td><td>5</td><td>move both</td></tr><tr><td>3</td><td>null</td><td>fast==null, stop</td></tr></table><p>slow=node(3). slow.next = slow.next.next = node(5). Result: 1→2→3→5 ✓</p>",
    interviewTips: "<ul><li><strong>Why n+1 steps?</strong> We want slow to stop at the node <em>before</em> the target (to enable deletion). Starting both at dummy and advancing fast by n+1 creates the right gap.</li><li><strong>Dummy node:</strong> Essential for removing the first node (when n == list length).</li><li><strong>Guarantee:</strong> The problem guarantees n is valid, so no need to check for null fast pointer during the initial advance.</li></ul>",
    importantPoints: "<ul><li>Advance fast by <code>n+1</code> (not n) so slow stops one node before the target.</li><li>Dummy head handles edge case of removing the first node cleanly.</li><li><strong>Time:</strong> O(L) one pass. <strong>Space:</strong> O(1).</li></ul>"
  },
  {
    id: 39,
    name: "Copy List with Random Pointer",
    category: "Linked List",
    categoryId: "linked-list",
    difficulty: "Medium",
    explanation: "<p><strong>Copy List with Random Pointer:</strong> Each node in the list has a <code>next</code> pointer and a <code>random</code> pointer that can point to any node or null. Create a <strong>deep copy</strong> of the list.</p><p>The challenge: when creating a new node, its <code>random</code> pointer needs to reference the <em>new copy</em> of the target node, not the original. We need a mapping from old nodes to new nodes.</p>",
    bruteForce: "<p>Similar to the optimal — there's no fundamentally simpler deep copy. The O(1) space solution weaves new nodes into the original list but is complex to implement.</p>",
    bruteForceInefficiency: "<p>The HashMap approach uses O(n) extra space for the mapping. The O(1) space technique (interleaving new nodes) is possible but much harder to code correctly under interview pressure. The HashMap solution is preferred.</p>",
    optimalApproach: "<p><strong>HashMap — O(n) time, O(n) space:</strong></p><ol><li><strong>First pass:</strong> Create a new copy node for each original node and store in a map: <code>old → new</code>.</li><li><strong>Second pass:</strong> For each original node, set <code>copy.next = map[original.next]</code> and <code>copy.random = map[original.random]</code>. The map handles null pointers correctly (<code>map.get(null) = null</code> in Java).</li><li>Return <code>map.get(head)</code>.</li></ol>",
    javaCode: "class Solution {\n    public Node copyRandomList(Node head) {\n        Map<Node, Node> map = new HashMap<>();\n        // First pass: create all copy nodes\n        Node cur = head;\n        while (cur != null) {\n            map.put(cur, new Node(cur.val));\n            cur = cur.next;\n        }\n        // Second pass: assign next and random pointers\n        cur = head;\n        while (cur != null) {\n            map.get(cur).next   = map.get(cur.next);\n            map.get(cur).random = map.get(cur.random);\n            cur = cur.next;\n        }\n        return map.get(head);\n    }\n}",
    dryRun: "<p><strong>Input:</strong> Node(7)→Node(13)→null, random: 7→null, 13→7</p><p><strong>Pass 1:</strong> map = {7:copy(7), 13:copy(13)}</p><p><strong>Pass 2:</strong></p><ul><li>copy(7).next = map[node(13)] = copy(13)</li><li>copy(7).random = map[null] = null</li><li>copy(13).next = map[null] = null</li><li>copy(13).random = map[node(7)] = copy(7)</li></ul><p>Return copy(7) = <strong>copy(7)→copy(13)</strong> with correct random pointers ✓</p>",
    interviewTips: "<ul><li><strong>Why two passes?</strong> In the first pass, we create all nodes. In the second, we can safely set pointers because all copy nodes already exist.</li><li><strong>HashMap.get(null) returns null</strong> in Java — this handles null next/random pointers elegantly.</li><li><strong>O(1) space alternative:</strong> Interleave copies into original list, set randoms, then unweave. Mention it but don't code unless asked.</li></ul>",
    importantPoints: "<ul><li>Two-pass approach: create all nodes first, then set pointers.</li><li><code>map.get(null)</code> returns null — handles null random/next correctly.</li><li><strong>Time:</strong> O(n). <strong>Space:</strong> O(n) for the map.</li></ul>"
  },
  {
    id: 40,
    name: "Add Two Numbers",
    category: "Linked List",
    categoryId: "linked-list",
    difficulty: "Medium",
    explanation: "<p><strong>Add Two Numbers:</strong> Two non-empty linked lists represent two non-negative integers in <strong>reverse order</strong> (least significant digit first). Add the two numbers and return the sum as a linked list.</p><p>Example: 2→4→3 (represents 342) + 5→6→4 (represents 465) = 7→0→8 (represents 807).</p><p>This simulates grade-school addition digit by digit with carry propagation.</p>",
    bruteForce: "<p>Convert both lists to integers, add them, convert back to a linked list. But this fails for very large numbers that exceed long's range. The digit-by-digit approach handles arbitrarily large numbers.</p>",
    bruteForceInefficiency: "<p>Integer conversion fails for numbers with more than 19 digits (exceeds long max). The digit-by-digit simulation handles numbers of any size.</p>",
    optimalApproach: "<p><strong>Digit-by-Digit with Carry — O(max(m,n)) time, O(max(m,n)) space:</strong></p><ol><li>Initialize <code>carry = 0</code>, dummy head, cur pointer.</li><li>While either list has nodes OR carry &gt; 0:<ul><li><code>sum = carry + (l1.val if l1 else 0) + (l2.val if l2 else 0)</code>.</li><li>Create new node with <code>sum % 10</code>.</li><li><code>carry = sum / 10</code>.</li><li>Advance l1, l2 if not null.</li></ul></li><li>Return dummy.next.</li></ol>",
    javaCode: "class Solution {\n    public ListNode addTwoNumbers(ListNode l1, ListNode l2) {\n        ListNode dummy = new ListNode(0), cur = dummy;\n        int carry = 0;\n        while (l1 != null || l2 != null || carry != 0) {\n            int sum = carry;\n            if (l1 != null) { sum += l1.val; l1 = l1.next; }\n            if (l2 != null) { sum += l2.val; l2 = l2.next; }\n            carry = sum / 10;\n            cur.next = new ListNode(sum % 10);\n            cur = cur.next;\n        }\n        return dummy.next;\n    }\n}",
    dryRun: "<p><strong>Input:</strong> 2→4→3 + 5→6→4</p><table border='1' cellpadding='4'><tr><th>l1</th><th>l2</th><th>carry</th><th>sum</th><th>digit</th><th>new carry</th></tr><tr><td>2</td><td>5</td><td>0</td><td>7</td><td>7</td><td>0</td></tr><tr><td>4</td><td>6</td><td>0</td><td>10</td><td>0</td><td>1</td></tr><tr><td>3</td><td>4</td><td>1</td><td>8</td><td>8</td><td>0</td></tr><tr><td>null</td><td>null</td><td>0</td><td>-</td><td>-</td><td>loop ends</td></tr></table><p>Result: <strong>7→0→8</strong> ✓</p>",
    interviewTips: "<ul><li><strong>Key detail:</strong> The loop condition includes <code>carry != 0</code> — handles the case where the sum produces an extra digit (e.g., 999 + 1 = 1000).</li><li><strong>Null check:</strong> Use <code>if (l1 != null)</code> before accessing <code>l1.val</code> — don't assume equal-length lists.</li><li><strong>Follow-up:</strong> What if the numbers were stored in forward order? (Stack each list or reverse them first.)</li></ul>",
    importantPoints: "<ul><li>Loop while <code>l1 != null || l2 != null || carry != 0</code> to handle carry propagation beyond both lists.</li><li>Advance l1/l2 only if they're non-null.</li><li><strong>Time:</strong> O(max(m,n)). <strong>Space:</strong> O(max(m,n)) for the result list.</li></ul>"
  },
  {
    id: 41,
    name: "Linked List Cycle",
    category: "Linked List",
    categoryId: "linked-list",
    difficulty: "Easy",
    explanation: "<p><strong>Linked List Cycle:</strong> Determine if a linked list has a cycle (a node's next pointer points back to a previous node).</p><p>The classic solution is <strong>Floyd's Cycle Detection Algorithm</strong> (tortoise and hare): use two pointers — slow moves one step at a time, fast moves two steps. If they ever meet, there's a cycle. If fast reaches null, there's no cycle.</p><p>Intuition: If there's a cycle, the fast pointer will eventually lap the slow pointer inside the cycle.</p>",
    bruteForce: "<p><strong>HashSet:</strong> Store visited nodes in a set. If we see a node we've visited before, there's a cycle.</p><pre><code>Set&lt;ListNode&gt; seen = new HashSet&lt;&gt;();\nwhile (head != null) {\n    if (seen.contains(head)) return true;\n    seen.add(head);\n    head = head.next;\n}\nreturn false;</code></pre><p><strong>Time:</strong> O(n). <strong>Space:</strong> O(n).</p>",
    bruteForceInefficiency: "<p>The HashSet approach uses O(n) extra space. Floyd's algorithm detects cycles with O(1) space by using the mathematical property that two pointers at different speeds must meet if and only if there's a cycle.</p>",
    optimalApproach: "<p><strong>Floyd's Cycle Detection — O(n) time, O(1) space:</strong></p><ol><li>Initialize both <code>slow</code> and <code>fast</code> at head.</li><li>While <code>fast != null && fast.next != null</code>:<ul><li>Move <code>slow = slow.next</code>.</li><li>Move <code>fast = fast.next.next</code>.</li><li>If <code>slow == fast</code> → cycle detected, return true.</li></ul></li><li>If loop ends (fast hits null) → no cycle, return false.</li></ol>",
    javaCode: "class Solution {\n    public boolean hasCycle(ListNode head) {\n        ListNode slow = head, fast = head;\n        while (fast != null && fast.next != null) {\n            slow = slow.next;\n            fast = fast.next.next;\n            if (slow == fast) return true;\n        }\n        return false;\n    }\n}",
    dryRun: "<p><strong>Input:</strong> 3→2→0→-4, -4.next = 2 (cycle at position 1)</p><table border='1' cellpadding='4'><tr><th>Step</th><th>slow</th><th>fast</th><th>slow==fast?</th></tr><tr><td>0</td><td>3</td><td>3</td><td>start</td></tr><tr><td>1</td><td>2</td><td>0</td><td>no</td></tr><tr><td>2</td><td>0</td><td>2</td><td>no</td></tr><tr><td>3</td><td>-4</td><td>-4</td><td><strong>yes → return true</strong></td></tr></table>",
    interviewTips: "<ul><li><strong>Check <code>fast != null &amp;&amp; fast.next != null</code></strong> before accessing <code>fast.next.next</code>.</li><li><strong>Cycle start:</strong> Follow-up question — to find where the cycle begins, after detection reset slow to head and advance both one step at a time until they meet again.</li><li><strong>Why do they meet?</strong> The relative speed of fast over slow is 1 step/iteration. Once both are in the cycle, fast gains one step per iteration, so they meet in at most cycle_length iterations.</li></ul>",
    importantPoints: "<ul><li>Check <code>fast != null &amp;&amp; fast.next != null</code> — both needed to safely call <code>fast.next.next</code>.</li><li>Compare by <strong>reference</strong> (<code>slow == fast</code>), not value.</li><li><strong>Time:</strong> O(n). <strong>Space:</strong> O(1).</li></ul>"
  },
  {
    id: 42,
    name: "Find the Duplicate Number",
    category: "Linked List",
    categoryId: "linked-list",
    difficulty: "Medium",
    explanation: "<p><strong>Find the Duplicate Number:</strong> Given an array of n+1 integers where each value is in [1, n], exactly one value is duplicated. Find it without modifying the array and using O(1) extra space.</p><p>The array can be modeled as a linked list: index <code>i</code> points to <code>nums[i]</code>. Since there's a duplicate value, two indices point to the same next node, forming a cycle. The entry point of the cycle is the duplicate number — exactly Floyd's algorithm!</p>",
    bruteForce: "<p><strong>HashSet:</strong> Add each number to a set; return when a duplicate is found. <strong>Time:</strong> O(n). <strong>Space:</strong> O(n). Violates the O(1) space constraint.</p><p><strong>Sort:</strong> Sort the array and find adjacent equal elements. <strong>Time:</strong> O(n log n). Modifies the array.</p>",
    bruteForceInefficiency: "<p>Both violate one of the constraints. Floyd's cycle detection cleverly maps the problem to a linked list cycle, using the index-as-pointer interpretation to find the duplicate in O(n) time and O(1) space without modification.</p>",
    optimalApproach: "<p><strong>Floyd's Cycle Detection on Array — O(n) time, O(1) space:</strong></p><ol><li><strong>Phase 1 — Find meeting point:</strong> Start slow=nums[0], fast=nums[0]. Move slow one step (<code>nums[slow]</code>), fast two steps (<code>nums[nums[fast]]</code>) until they meet.</li><li><strong>Phase 2 — Find cycle entry:</strong> Reset slow=nums[0], keep fast at meeting point. Move both one step at a time until they meet — this is the duplicate.</li></ol>",
    javaCode: "class Solution {\n    public int findDuplicate(int[] nums) {\n        // Phase 1: Find meeting point\n        int slow = nums[0], fast = nums[0];\n        do {\n            slow = nums[slow];\n            fast = nums[nums[fast]];\n        } while (slow != fast);\n        // Phase 2: Find cycle entry (the duplicate)\n        slow = nums[0];\n        while (slow != fast) {\n            slow = nums[slow];\n            fast = nums[fast];\n        }\n        return slow;\n    }\n}",
    dryRun: "<p><strong>Input:</strong> nums=[1,3,4,2,2]</p><p>Linked list interpretation: 0→1→3→2→4→2 (cycle at 2)</p><p><strong>Phase 1:</strong> slow=nums[0]=1, fast=nums[0]=1</p><table border='1' cellpadding='4'><tr><th>Step</th><th>slow=nums[slow]</th><th>fast=nums[nums[fast]]</th></tr><tr><td>1</td><td>nums[1]=3</td><td>nums[nums[1]]=nums[3]=2</td></tr><tr><td>2</td><td>nums[3]=2</td><td>nums[nums[2]]=nums[4]=2</td></tr><tr><td>3</td><td>nums[2]=4</td><td>nums[nums[2]]=nums[4]=2</td></tr><tr><td>4</td><td>nums[4]=2</td><td>nums[nums[2]]=nums[4]=2 ← meet!</td></tr></table><p><strong>Phase 2:</strong> slow=nums[0]=1, fast=2. slow=3,fast=4. slow=2,fast=2. Return <strong>2</strong> ✓</p>",
    interviewTips: "<ul><li><strong>The mapping:</strong> Think of each index as a node and <code>nums[i]</code> as the next pointer. The duplicate creates two indices pointing to the same next node = cycle entry.</li><li><strong>Why do-while?</strong> Both start at the same position, so we need at least one step before checking equality.</li><li><strong>Phase 2 explanation:</strong> The distance from the start of the array to the cycle entry equals the distance from the meeting point to the cycle entry — a mathematical property of Floyd's algorithm.</li></ul>",
    importantPoints: "<ul><li>Models the array as a linked list: <code>i → nums[i]</code>.</li><li>The duplicate is the node where the cycle begins.</li><li><strong>Time:</strong> O(n). <strong>Space:</strong> O(1). Does not modify the array.</li><li>Requires Floyd's two-phase algorithm — finding the meeting point first, then the cycle entry.</li></ul>"
  },
  {
    id: 43,
    name: "LRU Cache",
    category: "Linked List",
    categoryId: "linked-list",
    difficulty: "Medium",
    explanation: "<p><strong>LRU Cache:</strong> Design a data structure with <code>get(key)</code> and <code>put(key, value)</code> both in O(1). When the cache is full, evict the <strong>Least Recently Used</strong> item.</p><p>The solution combines a <strong>HashMap</strong> (O(1) key lookup) with a <strong>doubly linked list</strong> (O(1) insertion/deletion to track usage order). Most recently used items go to the front; LRU item is always at the back.</p><p>In Java, <code>LinkedHashMap</code> provides this exact behavior with the access-order constructor flag.</p>",
    bruteForce: "<p>Use a HashMap + ArrayList; on each access sort or scan for LRU. <strong>Time:</strong> O(n) per operation. Not acceptable.</p>",
    bruteForceInefficiency: "<p>Finding and removing the LRU item in an unordered structure takes O(n). The doubly linked list maintains order with O(1) moves. The HashMap provides O(1) lookup.</p>",
    optimalApproach: "<p><strong>LinkedHashMap (access-order) — O(1) get and put:</strong></p><ol><li>Use <code>LinkedHashMap</code> with <code>accessOrder=true</code> — automatically moves accessed entries to the end.</li><li>Override <code>removeEldestEntry</code> to evict when size exceeds capacity.</li><li><code>get()</code>: Return <code>cache.getOrDefault(key, -1)</code>. The access reorders the entry automatically.</li><li><code>put()</code>: Insert/update; LinkedHashMap evicts the eldest entry if over capacity.</li></ol>",
    javaCode: "class LRUCache {\n    private int capacity;\n    private LinkedHashMap<Integer, Integer> cache;\n\n    public LRUCache(int capacity) {\n        this.capacity = capacity;\n        // accessOrder=true: moves accessed entries to tail (most recent)\n        cache = new LinkedHashMap<>(capacity, 0.75f, true) {\n            protected boolean removeEldestEntry(Map.Entry<Integer, Integer> eldest) {\n                return size() > capacity;\n            }\n        };\n    }\n\n    public int get(int key) {\n        return cache.getOrDefault(key, -1);\n    }\n\n    public void put(int key, int value) {\n        cache.put(key, value);\n    }\n}",
    dryRun: "<p><strong>Operations (capacity=2):</strong></p><table border='1' cellpadding='4'><tr><th>Operation</th><th>Cache (LRU→MRU)</th><th>Returns</th></tr><tr><td>put(1,1)</td><td>[1:1]</td><td>-</td></tr><tr><td>put(2,2)</td><td>[1:1, 2:2]</td><td>-</td></tr><tr><td>get(1)</td><td>[2:2, 1:1]</td><td>1</td></tr><tr><td>put(3,3)</td><td>[1:1, 3:3] (evict 2)</td><td>-</td></tr><tr><td>get(2)</td><td>[1:1, 3:3]</td><td>-1 (not found)</td></tr><tr><td>put(4,4)</td><td>[3:3, 4:4] (evict 1)</td><td>-</td></tr><tr><td>get(1)</td><td>[3:3, 4:4]</td><td>-1</td></tr><tr><td>get(3)</td><td>[4:4, 3:3]</td><td>3</td></tr><tr><td>get(4)</td><td>[3:3, 4:4]</td><td>4</td></tr></table>",
    interviewTips: "<ul><li><strong>Mention the manual implementation:</strong> HashMap + doubly linked list with dummy head/tail nodes. This shows deeper understanding than just using LinkedHashMap.</li><li><strong>LinkedHashMap access-order:</strong> The third constructor parameter <code>true</code> enables access-order mode.</li><li><strong>Manual DLL key methods:</strong> <code>addToFront(node)</code> and <code>removeNode(node)</code> — both O(1) with doubly linked list.</li></ul>",
    importantPoints: "<ul><li>LinkedHashMap with <code>accessOrder=true</code> is the idiomatic Java solution.</li><li>Manual solution: HashMap maps keys to DLL nodes; DLL maintains usage order.</li><li>Both <code>get</code> and <code>put</code> are <strong>O(1)</strong>.</li><li>Eviction happens at the tail (LRU end); new/accessed items go to the head (MRU end).</li></ul>"
  },
  {
    id: 44,
    name: "Merge K Sorted Lists",
    category: "Linked List",
    categoryId: "linked-list",
    difficulty: "Hard",
    explanation: "<p><strong>Merge K Sorted Lists:</strong> Given an array of k linked lists, each sorted in ascending order, merge them into one sorted linked list.</p><p>The efficient solution uses a <strong>min-heap (PriorityQueue)</strong>: always extract the globally smallest node among the current heads of all k lists.</p><p>Alternative: divide and conquer — merge lists pairwise like merge sort. Both are O(N log k) where N is the total number of nodes.</p>",
    bruteForce: "<p><strong>Collect all values:</strong> Gather all node values, sort them, build a new list. <strong>Time:</strong> O(N log N). <strong>Space:</strong> O(N). Ignores the sorted property.</p><p><strong>Merge one by one:</strong> Repeatedly merge list[0] with list[1], result with list[2], etc. <strong>Time:</strong> O(kN) — expensive when k is large.</p>",
    bruteForceInefficiency: "<p>Merging one by one does redundant work: the first list's nodes are compared O(k) times. A min-heap always picks the global minimum in O(log k) per node, giving O(N log k) total. Divide-and-conquer also achieves O(N log k) by halving the number of lists each round.</p>",
    optimalApproach: "<p><strong>Min-Heap — O(N log k) time, O(k) space:</strong></p><ol><li>Add the head of each non-null list to a min-heap (sorted by node value).</li><li>While heap is not empty:<ul><li>Poll the minimum node, append to result.</li><li>If polled node has a <code>next</code>, push it into the heap.</li></ul></li><li>Return dummy.next.</li></ol>",
    javaCode: "class Solution {\n    public ListNode mergeKLists(ListNode[] lists) {\n        PriorityQueue<ListNode> pq =\n            new PriorityQueue<>((a, b) -> a.val - b.val);\n        for (ListNode node : lists)\n            if (node != null) pq.offer(node);\n        ListNode dummy = new ListNode(0), cur = dummy;\n        while (!pq.isEmpty()) {\n            cur.next = pq.poll();\n            cur = cur.next;\n            if (cur.next != null) pq.offer(cur.next);\n        }\n        return dummy.next;\n    }\n}",
    dryRun: "<p><strong>Input:</strong> lists = [1→4→5, 1→3→4, 2→6]</p><p><strong>Initial heap:</strong> {1(L1), 1(L2), 2(L3)}</p><table border='1' cellpadding='4'><tr><th>Poll</th><th>Heap after push</th><th>Result</th></tr><tr><td>1(L1)</td><td>{1(L2),2(L3),4(L1)}</td><td>1</td></tr><tr><td>1(L2)</td><td>{2(L3),4(L1),3(L2)}</td><td>1→1</td></tr><tr><td>2(L3)</td><td>{3(L2),4(L1),6(L3)}</td><td>1→1→2</td></tr><tr><td>3(L2)</td><td>{4(L1),6(L3),4(L2)}</td><td>1→1→2→3</td></tr><tr><td>4(L1)</td><td>{4(L2),6(L3),5(L1)}</td><td>1→1→2→3→4</td></tr><tr><td>4(L2)</td><td>{5(L1),6(L3)}</td><td>...→4→4</td></tr><tr><td>5(L1)</td><td>{6(L3)}</td><td>...→4→4→5</td></tr><tr><td>6(L3)</td><td>{}</td><td>...→5→6</td></tr></table><p>Result: <strong>1→1→2→3→4→4→5→6</strong> ✓</p>",
    interviewTips: "<ul><li><strong>Know both approaches:</strong> Min-heap O(N log k) and divide-and-conquer O(N log k). Mention both, then implement the heap approach.</li><li><strong>PriorityQueue comparator:</strong> <code>(a, b) -&gt; a.val - b.val</code> for min-heap. Safe here since values are bounded, but generally use <code>Integer.compare(a.val, b.val)</code> to avoid overflow.</li><li><strong>Null check:</strong> Skip null list heads when initializing the heap.</li></ul>",
    importantPoints: "<ul><li>Min-heap always has at most k elements — O(k) space.</li><li>Each of the N nodes is pushed and popped once — O(N log k) total.</li><li>Divide-and-conquer: merge pairs of lists in log k rounds, O(N) per round.</li><li><strong>Time:</strong> O(N log k). <strong>Space:</strong> O(k) for the heap.</li></ul>"
  },
  {
    id: 45,
    name: "Reverse Nodes in K-Group",
    category: "Linked List",
    categoryId: "linked-list",
    difficulty: "Hard",
    explanation: "<p><strong>Reverse Nodes in K-Group:</strong> Given a linked list and integer k, reverse every k consecutive nodes. If the remaining nodes are fewer than k, leave them as-is.</p><p>Example: 1→2→3→4→5 with k=2 → 2→1→4→3→5. With k=3 → 3→2→1→4→5.</p><p>The algorithm processes the list in groups: find the kth node, reverse the group, connect it to the previous group and next group, then move to the next group.</p>",
    bruteForce: "<p>Collect all nodes in an array, reverse in groups of k, then rebuild the list. <strong>Time:</strong> O(n). <strong>Space:</strong> O(n). An in-place solution is preferred.</p>",
    bruteForceInefficiency: "<p>Using an array requires O(n) extra space. The in-place pointer manipulation achieves O(1) extra space by carefully reconnecting groups one at a time.</p>",
    optimalApproach: "<p><strong>In-Place Group Reversal — O(n) time, O(1) space:</strong></p><ol><li>Use a dummy node; <code>groupPrev</code> tracks the node before the current group.</li><li>Loop:<ul><li>Find the <code>kth</code> node from <code>groupPrev</code> using a helper. If null, stop (fewer than k nodes).</li><li>Save <code>groupNext = kth.next</code>.</li><li>Reverse the group from <code>groupPrev.next</code> to <code>kth</code>: use standard reverse with <code>prev = groupNext</code> as the initial prev to connect the reversed group to the next group.</li><li>Reconnect: <code>groupPrev.next = kth</code> (new head of reversed group). Advance <code>groupPrev</code> to the old group head (now tail of reversed group).</li></ul></li></ol>",
    javaCode: "class Solution {\n    public ListNode reverseKGroup(ListNode head, int k) {\n        ListNode dummy = new ListNode(0, head), groupPrev = dummy;\n        while (true) {\n            ListNode kth = getKth(groupPrev, k);\n            if (kth == null) break; // fewer than k nodes remain\n            ListNode groupNext = kth.next;\n            // Reverse the group [groupPrev.next ... kth]\n            ListNode prev = groupNext, curr = groupPrev.next;\n            while (curr != groupNext) {\n                ListNode tmp = curr.next;\n                curr.next = prev;\n                prev = curr;\n                curr = tmp;\n            }\n            // Reconnect: groupPrev -> kth (new head) -> ... -> old head -> groupNext\n            ListNode tmp = groupPrev.next; // old group head (now tail)\n            groupPrev.next = kth;\n            groupPrev = tmp;\n        }\n        return dummy.next;\n    }\n\n    private ListNode getKth(ListNode curr, int k) {\n        while (curr != null && k > 0) {\n            curr = curr.next;\n            k--;\n        }\n        return curr;\n    }\n}",
    dryRun: "<p><strong>Input:</strong> 1→2→3→4→5, k=2. dummy→1→2→3→4→5, groupPrev=dummy</p><p><strong>Iteration 1:</strong> kth=node(2), groupNext=node(3). Reverse [1,2] with prev=node(3): 2→1→3→4→5. groupPrev.next=node(2). groupPrev=node(1).</p><p><strong>Iteration 2:</strong> kth=node(4), groupNext=node(5). Reverse [3,4] with prev=node(5): 4→3→5. groupPrev.next=node(4). groupPrev=node(3).</p><p><strong>Iteration 3:</strong> getKth(node(3), 2) — only 1 node (5) remains → returns null → break.</p><p>Result: dummy→2→1→4→3→5 = <strong>2→1→4→3→5</strong> ✓</p>",
    interviewTips: "<ul><li><strong>Draw the pointer state</strong> before and after each reversal — this problem is pointer-heavy and diagrams prevent mistakes.</li><li><strong>Key insight:</strong> During reversal, set <code>prev = groupNext</code> (not null) so the reversed group's tail automatically connects to the next group.</li><li><strong>groupPrev.next = kth</strong> after reversal because kth is now the head of the reversed group (the last node becomes the first after reversal).</li><li>The helper <code>getKth</code> moves k steps — returns null if fewer than k nodes remain.</li></ul>",
    importantPoints: "<ul><li>Set <code>prev = groupNext</code> at the start of reversal to auto-connect the reversed group's tail.</li><li>After reversal, <code>kth</code> is the new head; advance <code>groupPrev</code> to the old head (now the tail).</li><li><strong>Time:</strong> O(n) — each node visited once for reversal. <strong>Space:</strong> O(1).</li><li>Nodes after the last complete group are left unchanged automatically.</li></ul>"
  }
];
