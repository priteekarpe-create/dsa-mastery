window.STACK_DATA = [
  {
    id: 21,
    name: "Valid Parentheses",
    category: "Stack",
    categoryId: "stack",
    difficulty: "Easy",
    explanation: "<p><strong>Valid Parentheses</strong> asks you to determine whether a string containing only <code>'('</code>, <code>')'</code>, <code>'{'</code>, <code>'}'</code>, <code>'['</code>, and <code>']'</code> is valid.</p><p>A string is valid if:</p><ul><li>Every open bracket is closed by the <strong>same type</strong> of bracket.</li><li>Open brackets are closed in the <strong>correct order</strong> (most recently opened must be closed first — LIFO).</li></ul><p>Example: <code>\"({[]})\"</code> → valid. <code>\"([)]\"</code> → invalid because the order is wrong.</p><p>This is a classic <strong>stack</strong> problem. Whenever you see an opening bracket, push it. When you see a closing bracket, check if the top of the stack matches.</p>",
    bruteForce: "<p><strong>Brute Force:</strong> Repeatedly scan the string and remove matching pairs like <code>()</code>, <code>[]</code>, <code>{}</code> until no more pairs exist. If the string becomes empty, it's valid.</p><pre><code>while (s contains \"()\", \"[]\", \"{}\") {\n    s = s.replace(\"()\", \"\");\n    s = s.replace(\"[]\", \"\");\n    s = s.replace(\"{}\", \"\");\n}\nreturn s.isEmpty();</code></pre><p><strong>Time:</strong> O(n²) — each pass is O(n) and we may need O(n) passes. <strong>Space:</strong> O(n) for string copies.</p>",
    bruteForceInefficiency: "<p>The brute force repeatedly scans the whole string to find and remove pairs. For a string like <code>(((((...)))))</code> with n characters, each scan only removes one pair, leading to O(n) passes × O(n) work = <strong>O(n²)</strong> total. It also creates multiple string copies. The stack approach processes each character exactly once.</p>",
    optimalApproach: "<p><strong>Stack Approach — O(n) time, O(n) space:</strong></p><ol><li>Iterate through each character in the string.</li><li>If it's an <strong>opening</strong> bracket (<code>(</code>, <code>{</code>, <code>[</code>), push it onto the stack.</li><li>If it's a <strong>closing</strong> bracket, check:<ul><li>If the stack is empty → return <code>false</code> (no matching open bracket).</li><li>Pop the top of the stack. If it doesn't match the closing bracket → return <code>false</code>.</li></ul></li><li>After processing all characters, return <code>true</code> only if the stack is <strong>empty</strong> (all opened brackets were closed).</li></ol><p>The stack naturally enforces the LIFO ordering — the last opened bracket must be the first closed.</p>",
    javaCode: "class Solution {\n    public boolean isValid(String s) {\n        Stack<Character> stack = new Stack<>();\n        for (char c : s.toCharArray()) {\n            if (c == '(' || c == '{' || c == '[') stack.push(c);\n            else {\n                if (stack.isEmpty()) return false;\n                char top = stack.pop();\n                if (c == ')' && top != '(') return false;\n                if (c == '}' && top != '{') return false;\n                if (c == ']' && top != '[') return false;\n            }\n        }\n        return stack.isEmpty();\n    }\n}",
    dryRun: "<p><strong>Input:</strong> <code>s = \"({[]})\"</code></p><table border='1' cellpadding='4'><tr><th>Char</th><th>Action</th><th>Stack</th></tr><tr><td>(</td><td>Push</td><td>[(]</td></tr><tr><td>{</td><td>Push</td><td>[(, {]</td></tr><tr><td>[</td><td>Push</td><td>[(, {, []</td></tr><tr><td>]</td><td>Pop [ → matches ]</td><td>[(, {]</td></tr><tr><td>}</td><td>Pop { → matches }</td><td>[(]</td></tr><tr><td>)</td><td>Pop ( → matches )</td><td>[]</td></tr></table><p>Stack is empty → return <code>true</code> ✓</p><p><strong>Failure case:</strong> <code>\"([)]\"</code> — when we see <code>)</code>, top is <code>[</code>, mismatch → return <code>false</code>.</p>",
    interviewTips: "<ul><li><strong>Clarify:</strong> Can the string be empty? (Empty string is valid — stack is empty at end.)</li><li><strong>Edge cases:</strong> Single character like <code>'('</code> → false. Odd-length string → always false (mention this optimization).</li><li><strong>Common mistake:</strong> Forgetting to check <code>stack.isEmpty()</code> before calling <code>pop()</code>.</li><li><strong>Follow-up:</strong> Interviewer might ask you to handle additional bracket types — the solution scales easily.</li><li><strong>Optimization hint:</strong> If <code>s.length() % 2 != 0</code>, return false immediately.</li></ul>",
    importantPoints: "<ul><li>Stack is the perfect data structure here because brackets must be closed in <strong>LIFO order</strong>.</li><li>Always check for empty stack before popping.</li><li>Return <code>stack.isEmpty()</code> at the end — not just <code>true</code> — to handle unclosed brackets like <code>'((('</code>.</li><li><strong>Time:</strong> O(n) — one pass. <strong>Space:</strong> O(n) — worst case all opening brackets.</li></ul>"
  },
  {
    id: 22,
    name: "Min Stack",
    category: "Stack",
    categoryId: "stack",
    difficulty: "Medium",
    explanation: "<p><strong>Min Stack</strong> asks you to design a stack that supports <code>push</code>, <code>pop</code>, <code>top</code>, and <code>getMin</code> — all in <strong>O(1)</strong> time.</p><p>The tricky part is <code>getMin()</code>. A regular stack can't retrieve the minimum in O(1) without extra bookkeeping, because when you pop the current minimum, you need to know the <em>previous</em> minimum.</p><p><strong>Key insight:</strong> Maintain a second stack (<code>minStack</code>) that tracks the current minimum at every level. When you push a value, also push the new minimum onto <code>minStack</code>. When you pop, pop from both stacks simultaneously.</p>",
    bruteForce: "<p><strong>Brute Force:</strong> Use a single stack. For <code>getMin()</code>, scan the entire stack to find the minimum.</p><pre><code>public int getMin() {\n    int min = Integer.MAX_VALUE;\n    for (int val : stack) min = Math.min(min, val);\n    return min;\n}</code></pre><p><strong>Time:</strong> push/pop/top = O(1), but getMin = <strong>O(n)</strong>. <strong>Space:</strong> O(n).</p>",
    bruteForceInefficiency: "<p>Scanning the entire stack every time <code>getMin()</code> is called is O(n). If getMin is called frequently (e.g., in a tight loop), this becomes a bottleneck. The two-stack approach trades a constant factor of extra space to make every operation O(1).</p>",
    optimalApproach: "<p><strong>Two-Stack Approach — All operations O(1):</strong></p><ol><li>Maintain two stacks: <code>stack</code> (main) and <code>minStack</code> (tracks running minimum).</li><li><strong>push(val):</strong> Push <code>val</code> onto <code>stack</code>. Push <code>min(val, minStack.peek())</code> onto <code>minStack</code>. If <code>minStack</code> is empty, just push <code>val</code>.</li><li><strong>pop():</strong> Pop from both stacks.</li><li><strong>top():</strong> Return <code>stack.peek()</code>.</li><li><strong>getMin():</strong> Return <code>minStack.peek()</code>.</li></ol><p>At any point, <code>minStack.peek()</code> always holds the minimum of all elements currently in the main stack.</p>",
    javaCode: "class MinStack {\n    private Stack<Integer> stack = new Stack<>();\n    private Stack<Integer> minStack = new Stack<>();\n\n    public void push(int val) {\n        stack.push(val);\n        minStack.push(minStack.isEmpty() ? val : Math.min(val, minStack.peek()));\n    }\n\n    public void pop() {\n        stack.pop();\n        minStack.pop();\n    }\n\n    public int top() {\n        return stack.peek();\n    }\n\n    public int getMin() {\n        return minStack.peek();\n    }\n}",
    dryRun: "<p><strong>Operations:</strong> push(-2), push(0), push(-3), getMin(), pop(), top(), getMin()</p><table border='1' cellpadding='4'><tr><th>Operation</th><th>stack</th><th>minStack</th><th>Returns</th></tr><tr><td>push(-2)</td><td>[-2]</td><td>[-2]</td><td>-</td></tr><tr><td>push(0)</td><td>[-2,0]</td><td>[-2,-2]</td><td>-</td></tr><tr><td>push(-3)</td><td>[-2,0,-3]</td><td>[-2,-2,-3]</td><td>-</td></tr><tr><td>getMin()</td><td>[-2,0,-3]</td><td>[-2,-2,-3]</td><td><strong>-3</strong></td></tr><tr><td>pop()</td><td>[-2,0]</td><td>[-2,-2]</td><td>-</td></tr><tr><td>top()</td><td>[-2,0]</td><td>[-2,-2]</td><td><strong>0</strong></td></tr><tr><td>getMin()</td><td>[-2,0]</td><td>[-2,-2]</td><td><strong>-2</strong></td></tr></table>",
    interviewTips: "<ul><li><strong>Clarify:</strong> Are there duplicate values? (Yes — the two-stack approach handles duplicates correctly because we always store the running minimum, not just new minimums.)</li><li><strong>Alternative:</strong> You can also push pairs <code>(val, currentMin)</code> onto a single stack — saves one stack object but same idea.</li><li><strong>Follow-up:</strong> What if memory is extremely constrained? Discuss storing the difference instead of the actual value.</li><li><strong>Key point to state:</strong> Both stacks always have the same size — they grow and shrink together.</li></ul>",
    importantPoints: "<ul><li>The <code>minStack</code> stores the <strong>running minimum</strong>, not just new minimums. This means even if you push a value larger than the current min, <code>minStack</code> still records what the min was at that point in time.</li><li>All four operations run in <strong>O(1)</strong> time.</li><li>Space is <strong>O(n)</strong> for both stacks combined.</li><li>When popping the current minimum, <code>minStack</code> correctly reveals the <em>previous</em> minimum.</li></ul>"
  },
  {
    id: 23,
    name: "Evaluate Reverse Polish Notation",
    category: "Stack",
    categoryId: "stack",
    difficulty: "Medium",
    explanation: "<p><strong>Reverse Polish Notation (RPN)</strong>, also called postfix notation, places operators <em>after</em> their operands. For example:</p><ul><li><code>[\"2\",\"1\",\"+\",\"3\",\"*\"]</code> means <code>(2 + 1) * 3 = 9</code></li><li><code>[\"4\",\"13\",\"5\",\"/\",\"+\"]</code> means <code>4 + (13 / 5) = 6</code></li></ul><p>RPN is naturally evaluated using a <strong>stack</strong>: push numbers, and when you see an operator, pop two numbers, apply the operator, and push the result.</p><p>No parentheses are needed — the order of operations is implicit in the token order.</p>",
    bruteForce: "<p>There's no simpler approach than the stack-based method for RPN. A recursive approach or building an expression tree would be more complex with no benefit. The stack solution is already optimal.</p><p>A naive approach might try to convert back to infix notation and then evaluate, but that's far more work.</p>",
    bruteForceInefficiency: "<p>Converting to infix and evaluating would require tracking operator precedence and parentheses — exactly the complexity that RPN was designed to eliminate. The stack approach is the natural, canonical O(n) solution.</p>",
    optimalApproach: "<p><strong>Stack Approach — O(n) time, O(n) space:</strong></p><ol><li>Create an empty stack.</li><li>Iterate through each token:<ul><li>If it's a <strong>number</strong>, push it onto the stack.</li><li>If it's an <strong>operator</strong> (+, -, *, /), pop <code>b</code> (top) then <code>a</code> (second), compute <code>a op b</code>, push result.</li></ul></li><li>At the end, the stack contains exactly one value — the answer.</li></ol><p><strong>Important:</strong> Order matters for subtraction and division. The first popped value is the <em>right</em> operand (<code>b</code>), and the second popped is the <em>left</em> operand (<code>a</code>).</p>",
    javaCode: "class Solution {\n    public int evalRPN(String[] tokens) {\n        Stack<Integer> stack = new Stack<>();\n        for (String t : tokens) {\n            if (t.equals(\"+\") || t.equals(\"-\") || t.equals(\"*\") || t.equals(\"/\")) {\n                int b = stack.pop(), a = stack.pop();\n                if (t.equals(\"+\"))      stack.push(a + b);\n                else if (t.equals(\"-\")) stack.push(a - b);\n                else if (t.equals(\"*\")) stack.push(a * b);\n                else                    stack.push(a / b);\n            } else {\n                stack.push(Integer.parseInt(t));\n            }\n        }\n        return stack.pop();\n    }\n}",
    dryRun: "<p><strong>Input:</strong> <code>[\"2\",\"1\",\"+\",\"3\",\"*\"]</code></p><table border='1' cellpadding='4'><tr><th>Token</th><th>Action</th><th>Stack</th></tr><tr><td>\"2\"</td><td>Push 2</td><td>[2]</td></tr><tr><td>\"1\"</td><td>Push 1</td><td>[2, 1]</td></tr><tr><td>\"+\"</td><td>Pop 1(b), Pop 2(a) → 2+1=3, Push 3</td><td>[3]</td></tr><tr><td>\"3\"</td><td>Push 3</td><td>[3, 3]</td></tr><tr><td>\"*\"</td><td>Pop 3(b), Pop 3(a) → 3*3=9, Push 9</td><td>[9]</td></tr></table><p>Return <code>stack.pop()</code> = <strong>9</strong> ✓</p>",
    interviewTips: "<ul><li><strong>Watch out:</strong> Pop order matters! For <code>a - b</code>, the second-popped value is <code>a</code> (left operand). Getting this backwards causes wrong answers for subtraction and division.</li><li><strong>Division truncates toward zero</strong> in Java (and this problem). e.g., <code>6 / -132 = 0</code>, not -1.</li><li><strong>Edge case:</strong> Negative number tokens like <code>\"-3\"</code> — <code>Integer.parseInt(\"-3\")</code> handles these correctly.</li><li><strong>Clarify:</strong> Is the input always a valid RPN expression? (LeetCode guarantees yes.)</li></ul>",
    importantPoints: "<ul><li>Always pop <code>b</code> first (top of stack), then <code>a</code> — <code>a</code> is the left operand.</li><li><strong>Time:</strong> O(n) — one pass through tokens. <strong>Space:</strong> O(n) — at most n/2 numbers on stack at once.</li><li>Java's <code>int</code> division already truncates toward zero, matching the problem requirement.</li><li>Using <code>t.equals()</code> instead of <code>t == </code> is essential for string comparison in Java.</li></ul>"
  },
  {
    id: 24,
    name: "Generate Parentheses",
    category: "Stack",
    categoryId: "stack",
    difficulty: "Medium",
    explanation: "<p><strong>Generate Parentheses</strong> asks you to generate all combinations of <code>n</code> pairs of well-formed (valid) parentheses.</p><p>For <code>n = 3</code>, the answer is: <code>[\"((()))\", \"(()())\", \"(())()\", \"()(())\", \"()()()\"]</code></p><p>This is a classic <strong>backtracking</strong> problem. We build the string character by character and prune invalid branches early:</p><ul><li>We can add <code>'('</code> as long as we haven't used all <code>n</code> open brackets.</li><li>We can add <code>')'</code> only when there are more open brackets than close brackets so far.</li></ul>",
    bruteForce: "<p><strong>Brute Force:</strong> Generate all 2^(2n) combinations of <code>'('</code> and <code>')'</code>, then filter the valid ones.</p><pre><code>// Generate all strings of length 2n\n// Check each with isValid() → O(n) per check\n// Total: O(2^(2n) * n)</code></pre><p>For <code>n=3</code>, that's 64 combinations to check. For <code>n=10</code>, over a million — and most are invalid.</p>",
    bruteForceInefficiency: "<p>The brute force generates many obviously invalid sequences (like starting with <code>')'</code>). Backtracking avoids these by <strong>pruning early</strong> — we never even generate a path that would lead to an invalid sequence. The number of valid parentheses combinations is the Catalan number C(n), much smaller than 2^(2n).</p>",
    optimalApproach: "<p><strong>Backtracking — O(4^n / sqrt(n)) time (Catalan number):</strong></p><ol><li>Use a recursive function with parameters: current string, count of open brackets used, count of close brackets used.</li><li><strong>Base case:</strong> If <code>current.length() == 2*n</code>, add to result.</li><li><strong>Add '(':</strong> If <code>open &lt; n</code>, recurse with <code>open+1</code>.</li><li><strong>Add ')':</strong> If <code>close &lt; open</code>, recurse with <code>close+1</code>.</li></ol><p>The constraint <code>close &lt; open</code> ensures we never close a bracket that hasn't been opened — this is the key pruning rule.</p>",
    javaCode: "class Solution {\n    public List<String> generateParenthesis(int n) {\n        List<String> res = new ArrayList<>();\n        backtrack(res, \"\", 0, 0, n);\n        return res;\n    }\n\n    private void backtrack(List<String> res, String cur, int open, int close, int n) {\n        if (cur.length() == 2 * n) {\n            res.add(cur);\n            return;\n        }\n        if (open < n)\n            backtrack(res, cur + \"(\", open + 1, close, n);\n        if (close < open)\n            backtrack(res, cur + \")\", open, close + 1, n);\n    }\n}",
    dryRun: "<p><strong>Input:</strong> n = 2. Tree of recursive calls:</p><pre>backtrack(\"\", 0, 0)\n├─ add '(' → backtrack(\"(\", 1, 0)\n│   ├─ add '(' → backtrack(\"((\", 2, 0)\n│   │   └─ add ')' → backtrack(\"(()\", 2, 1)\n│   │       └─ add ')' → backtrack(\"(())\", 2, 2) ✓ ADD\n│   └─ add ')' → backtrack(\"()\", 1, 1)\n│       └─ add '(' → backtrack(\"()(\", 2, 1)\n│           └─ add ')' → backtrack(\"()()\", 2, 2) ✓ ADD</pre><p>Result: <code>[\"(())\", \"()()\"]</code> ✓</p>",
    interviewTips: "<ul><li><strong>Explain the two pruning rules</strong> clearly: open can't exceed n; close can't exceed open. This shows you understand WHY the solution works.</li><li><strong>Complexity:</strong> The number of valid strings is the n-th Catalan number ≈ 4^n / (n^1.5 * sqrt(π)).</li><li><strong>Iterative alternative:</strong> You can use an explicit stack/queue with BFS or DFS to avoid recursion.</li><li><strong>Connection to Valid Parentheses:</strong> The same rules that validate parentheses guide the generation.</li></ul>",
    importantPoints: "<ul><li>Two pruning rules: <code>open &lt; n</code> and <code>close &lt; open</code> ensure only valid strings are built.</li><li>String concatenation in Java creates new strings — using a <code>StringBuilder</code> and backtracking is more memory-efficient but slightly more complex to write.</li><li>The output count equals the <strong>nth Catalan number</strong>: 1, 2, 5, 14, 42...</li><li>This pattern (two counters + pruning) generalizes to many bracket/combination problems.</li></ul>"
  },
  {
    id: 25,
    name: "Daily Temperatures",
    category: "Stack",
    categoryId: "stack",
    difficulty: "Medium",
    explanation: "<p><strong>Daily Temperatures:</strong> Given an array <code>temps</code> where <code>temps[i]</code> is the temperature on day <code>i</code>, return an array <code>res</code> where <code>res[i]</code> is the number of days you have to wait after day <code>i</code> to get a warmer temperature. If no warmer day exists, <code>res[i] = 0</code>.</p><p>Example: <code>[73,74,75,71,69,72,76,73]</code> → <code>[1,1,4,2,1,1,0,0]</code></p><p>This is a <strong>monotonic stack</strong> problem. We maintain a stack of indices of temperatures in decreasing order. When we find a warmer day, we resolve all pending cooler days.</p>",
    bruteForce: "<p><strong>Brute Force:</strong> For each day <code>i</code>, scan forward to find the next warmer day.</p><pre><code>for (int i = 0; i < n; i++) {\n    for (int j = i+1; j < n; j++) {\n        if (temps[j] > temps[i]) {\n            res[i] = j - i;\n            break;\n        }\n    }\n}</code></pre><p><strong>Time:</strong> O(n²). <strong>Space:</strong> O(1) extra.</p>",
    bruteForceInefficiency: "<p>The inner loop redundantly checks days that were already considered by earlier iterations. For a decreasing sequence like <code>[100, 99, 98, ..., 1]</code>, the last element requires scanning almost the entire array. The monotonic stack processes each element <strong>at most twice</strong> (pushed once, popped once), giving O(n).</p>",
    optimalApproach: "<p><strong>Monotonic Decreasing Stack — O(n) time, O(n) space:</strong></p><ol><li>Initialize result array with all zeros and an empty stack (stores indices).</li><li>For each day <code>i</code>:<ul><li>While the stack is not empty AND <code>temps[i] &gt; temps[stack.peek()]</code>:<ul><li>Pop index <code>idx</code> from stack.</li><li>Set <code>res[idx] = i - idx</code> (days until warmer).</li></ul></li><li>Push <code>i</code> onto the stack.</li></ul></li><li>Any indices remaining in the stack have no warmer future day → <code>res[idx] = 0</code> (already initialized).</li></ol>",
    javaCode: "class Solution {\n    public int[] dailyTemperatures(int[] temps) {\n        int n = temps.length;\n        int[] res = new int[n];\n        Stack<Integer> stack = new Stack<>(); // stores indices\n        for (int i = 0; i < n; i++) {\n            while (!stack.isEmpty() && temps[i] > temps[stack.peek()]) {\n                int idx = stack.pop();\n                res[idx] = i - idx;\n            }\n            stack.push(i);\n        }\n        return res;\n    }\n}",
    dryRun: "<p><strong>Input:</strong> <code>[73, 74, 75, 71, 69, 72, 76, 73]</code></p><table border='1' cellpadding='4'><tr><th>i</th><th>temp</th><th>Stack (indices)</th><th>Actions</th></tr><tr><td>0</td><td>73</td><td>[0]</td><td>Push 0</td></tr><tr><td>1</td><td>74</td><td>[1]</td><td>74&gt;73 → pop 0, res[0]=1. Push 1</td></tr><tr><td>2</td><td>75</td><td>[2]</td><td>75&gt;74 → pop 1, res[1]=1. Push 2</td></tr><tr><td>3</td><td>71</td><td>[2,3]</td><td>71&lt;75 → Push 3</td></tr><tr><td>4</td><td>69</td><td>[2,3,4]</td><td>69&lt;71 → Push 4</td></tr><tr><td>5</td><td>72</td><td>[2,5]</td><td>72&gt;69 → pop 4, res[4]=1. 72&gt;71 → pop 3, res[3]=2. Push 5</td></tr><tr><td>6</td><td>76</td><td>[6]</td><td>76&gt;72 → pop 5, res[5]=1. 76&gt;75 → pop 2, res[2]=4. Push 6</td></tr><tr><td>7</td><td>73</td><td>[6,7]</td><td>73&lt;76 → Push 7</td></tr></table><p>Remaining in stack: [6,7] → res[6]=0, res[7]=0. Result: <code>[1,1,4,2,1,1,0,0]</code> ✓</p>",
    interviewTips: "<ul><li><strong>Pattern recognition:</strong> Any \"next greater element\" problem can be solved with a monotonic stack.</li><li><strong>Store indices, not values</strong> — you need both the value (to compare) and the index (to compute distance).</li><li><strong>Monotonic stack invariant:</strong> The stack always holds indices of temperatures in non-increasing order.</li><li><strong>Space optimization:</strong> You can use an array as a stack (with a pointer) to avoid Stack overhead.</li></ul>",
    importantPoints: "<ul><li>The stack stores <strong>indices</strong>, not temperature values.</li><li>Stack maintains a <strong>monotonically decreasing</strong> sequence of temperatures.</li><li>Each element is pushed and popped at most once → <strong>O(n)</strong> total operations.</li><li>This is a template for all \"next greater element\" variants on LeetCode.</li><li><strong>Time:</strong> O(n), <strong>Space:</strong> O(n).</li></ul>"
  },
  {
    id: 26,
    name: "Car Fleet",
    category: "Stack",
    categoryId: "stack",
    difficulty: "Medium",
    explanation: "<p><strong>Car Fleet:</strong> There are <code>n</code> cars at various positions heading toward a target. Each car has a position and speed. A faster car that catches up to a slower car forms a <strong>fleet</strong> (they travel together at the slower car's speed). Return the number of fleets that reach the target.</p><p>Key insight: Sort cars by starting position (closest to target first). Compute the time each car takes to reach the target. If a car behind takes <strong>less or equal time</strong> than the car ahead, it catches up and joins the same fleet. Otherwise, it forms a new fleet.</p>",
    bruteForce: "<p><strong>Brute Force:</strong> Simulate the cars moving in small time steps, updating positions, and checking for merges. Very complex to implement correctly and extremely slow for large inputs or long distances.</p><p><strong>Time:</strong> O(target × n) in simulation steps. <strong>Space:</strong> O(n).</p>",
    bruteForceInefficiency: "<p>Simulation requires tiny time steps for precision and is impractical. The key observation is that we only need to know whether one car <em>catches up</em> to another before reaching the target — which is determined purely by comparing their arrival times. This reduces the problem to a simple O(n log n) sort + O(n) scan.</p>",
    optimalApproach: "<p><strong>Sort + Stack — O(n log n) time, O(n) space:</strong></p><ol><li>Pair each car's position with its time-to-target: <code>time = (target - position) / speed</code>.</li><li>Sort by position in <strong>descending</strong> order (car closest to target first).</li><li>Use a stack to track fleet times. For each car:<ul><li>Push its time onto the stack.</li><li>If the top two elements show the new car's time ≤ the car ahead's time, it merges — pop the new car's time (it joins the fleet ahead).</li></ul></li><li>Return <code>stack.size()</code> — each element represents one fleet.</li></ol>",
    javaCode: "class Solution {\n    public int carFleet(int target, int[] position, int[] speed) {\n        int n = position.length;\n        double[][] cars = new double[n][2];\n        for (int i = 0; i < n; i++)\n            cars[i] = new double[]{position[i], (double)(target - position[i]) / speed[i]};\n        Arrays.sort(cars, (a, b) -> Double.compare(b[0], a[0]));\n        Stack<Double> stack = new Stack<>();\n        for (double[] car : cars) {\n            stack.push(car[1]);\n            if (stack.size() >= 2 && stack.peek() <= stack.get(stack.size() - 2))\n                stack.pop(); // merges into fleet ahead\n        }\n        return stack.size();\n    }\n}",
    dryRun: "<p><strong>Input:</strong> target=12, position=[10,8,0,5,3], speed=[2,4,1,1,3]</p><p>Times to target: pos 10→1.0, pos 8→1.0, pos 5→7.0, pos 3→3.0, pos 0→12.0</p><p>Sorted by position desc: [(10,1.0),(8,1.0),(5,7.0),(3,3.0),(0,12.0)]</p><table border='1' cellpadding='4'><tr><th>Car (pos,time)</th><th>Stack after push</th><th>Merge?</th><th>Stack final</th></tr><tr><td>(10, 1.0)</td><td>[1.0]</td><td>No (size&lt;2)</td><td>[1.0]</td></tr><tr><td>(8, 1.0)</td><td>[1.0, 1.0]</td><td>1.0 ≤ 1.0 → merge</td><td>[1.0]</td></tr><tr><td>(5, 7.0)</td><td>[1.0, 7.0]</td><td>7.0 &gt; 1.0 → no merge</td><td>[1.0, 7.0]</td></tr><tr><td>(3, 3.0)</td><td>[1.0, 7.0, 3.0]</td><td>3.0 ≤ 7.0 → merge</td><td>[1.0, 7.0]</td></tr><tr><td>(0, 12.0)</td><td>[1.0, 7.0, 12.0]</td><td>12.0 &gt; 7.0 → no merge</td><td>[1.0, 7.0, 12.0]</td></tr></table><p>Return <code>stack.size() = 3</code> ✓</p>",
    interviewTips: "<ul><li><strong>Sort direction matters:</strong> Sort by position descending (closest to target first) so you can compare a car to the one directly ahead.</li><li><strong>Float precision:</strong> Use <code>double</code> for time calculations to avoid integer division errors.</li><li><strong>Simplification:</strong> You don't actually need a stack — just count how many times a car's time exceeds the previous max time.</li><li><strong>Clarify:</strong> Can two cars start at the same position? (Not per constraints, but worth asking.)</li></ul>",
    importantPoints: "<ul><li>A car joins the fleet ahead if its arrival time ≤ the fleet ahead's arrival time.</li><li>Sorting by position descending is essential — we process cars from front to back.</li><li>The stack size equals the number of fleets.</li><li><strong>Time:</strong> O(n log n) for sort, O(n) for the scan. <strong>Space:</strong> O(n).</li></ul>"
  },
  {
    id: 27,
    name: "Largest Rectangle in Histogram",
    category: "Stack",
    categoryId: "stack",
    difficulty: "Hard",
    explanation: "<p><strong>Largest Rectangle in Histogram:</strong> Given an array of bar heights representing a histogram, find the area of the largest rectangle that can be formed within the histogram.</p><p>Example: <code>[2,1,5,6,2,3]</code> → 10 (the rectangle spanning heights 5 and 6).</p><p>For each bar, the maximum rectangle using that bar's height extends as far left and right as possible while all bars in that range are ≥ that bar's height.</p><p>A <strong>monotonic increasing stack</strong> helps efficiently find, for each bar, how far left it can extend when it becomes the shortest bar.</p>",
    bruteForce: "<p><strong>Brute Force:</strong> For every pair of bars (i, j), find the minimum height in [i,j] and compute area = minHeight × (j - i + 1).</p><pre><code>for (int i = 0; i < n; i++) {\n    int minH = heights[i];\n    for (int j = i; j < n; j++) {\n        minH = Math.min(minH, heights[j]);\n        maxArea = Math.max(maxArea, minH * (j - i + 1));\n    }\n}</code></pre><p><strong>Time:</strong> O(n²). <strong>Space:</strong> O(1).</p>",
    bruteForceInefficiency: "<p>The brute force checks every possible rectangle, which is O(n²) pairs. For each bar being the height-limiter, we redundantly recompute how far it extends. The monotonic stack finds, for each bar, the exact left and right boundaries in amortized O(1) per bar, giving O(n) total.</p>",
    optimalApproach: "<p><strong>Monotonic Stack — O(n) time, O(n) space:</strong></p><ol><li>Use a stack storing <code>[start, height]</code> pairs. Maintain an increasing stack.</li><li>For each bar at index <code>i</code>:<ul><li>Set <code>start = i</code>.</li><li>While stack is not empty and <code>stack.peek().height &gt; heights[i]</code>: pop bar, compute its area using the popped bar's height × (i - bar.start), update maxArea, and extend <code>start</code> back to <code>bar.start</code>.</li><li>Push <code>[start, heights[i]]</code>.</li></ul></li><li>After the loop, process remaining stack bars: each extends to the end of the array.</li></ol>",
    javaCode: "class Solution {\n    public int largestRectangleArea(int[] heights) {\n        int maxArea = 0;\n        Stack<int[]> stack = new Stack<>(); // [start, height]\n        for (int i = 0; i < heights.length; i++) {\n            int start = i;\n            while (!stack.isEmpty() && stack.peek()[1] > heights[i]) {\n                int[] bar = stack.pop();\n                maxArea = Math.max(maxArea, bar[1] * (i - bar[0]));\n                start = bar[0]; // extend left boundary\n            }\n            stack.push(new int[]{start, heights[i]});\n        }\n        // Process remaining bars — they extend to end\n        for (int[] bar : stack)\n            maxArea = Math.max(maxArea, bar[1] * (heights.length - bar[0]));\n        return maxArea;\n    }\n}",
    dryRun: "<p><strong>Input:</strong> <code>[2, 1, 5, 6, 2, 3]</code></p><table border='1' cellpadding='4'><tr><th>i</th><th>h</th><th>Stack [start,h]</th><th>Actions</th><th>maxArea</th></tr><tr><td>0</td><td>2</td><td>[(0,2)]</td><td>Push (0,2)</td><td>0</td></tr><tr><td>1</td><td>1</td><td>[(0,1)]</td><td>Pop (0,2): area=2*(1-0)=2. start=0. Push (0,1)</td><td>2</td></tr><tr><td>2</td><td>5</td><td>[(0,1),(2,5)]</td><td>Push (2,5)</td><td>2</td></tr><tr><td>3</td><td>6</td><td>[(0,1),(2,5),(3,6)]</td><td>Push (3,6)</td><td>2</td></tr><tr><td>4</td><td>2</td><td>[(0,1),(2,2)]</td><td>Pop (3,6): 6*(4-3)=6. Pop (2,5): 5*(4-2)=10. start=2. Push (2,2)</td><td>10</td></tr><tr><td>5</td><td>3</td><td>[(0,1),(2,2),(5,3)]</td><td>Push (5,3)</td><td>10</td></tr></table><p>Process remaining: (0,1)→1*6=6, (2,2)→2*4=8, (5,3)→3*1=3. Final maxArea = <strong>10</strong> ✓</p>",
    interviewTips: "<ul><li><strong>This is a hard problem</strong> — walk through the logic slowly and clearly. Explain why you store <code>[start, height]</code> pairs: the start tracks how far left a height can extend.</li><li><strong>Why extend start?</strong> When a tall bar is popped by a shorter bar, the shorter bar can extend left to where the tall bar started (since all bars up to there are ≥ the shorter bar's height).</li><li><strong>Don't forget</strong> to process the remaining stack after the loop.</li><li><strong>Alternative:</strong> Pre-compute \"previous smaller\" and \"next smaller\" arrays using two passes — also O(n) but requires more code.</li></ul>",
    importantPoints: "<ul><li>Store <code>[startIndex, height]</code> in the stack — the start index tracks the leftmost extent of a rectangle at that height.</li><li>When a shorter bar forces taller bars out, the shorter bar inherits their start index.</li><li>Process remaining stack elements at the end — they extend all the way to <code>heights.length</code>.</li><li><strong>Time:</strong> O(n) — each bar pushed/popped at most once. <strong>Space:</strong> O(n).</li></ul>"
  }
];
