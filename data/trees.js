window.TREES_DATA = [
  {
    id: 46,
    name: "Invert Binary Tree",
    category: "Trees",
    categoryId: "trees",
    difficulty: "Easy",
    explanation: "<p><strong>Invert Binary Tree</strong> asks you to mirror a binary tree: every left child becomes the right child and vice versa, recursively throughout the entire tree.</p><p>Think of holding a tree up to a mirror — every node stays in place but its children swap sides. This is a classic recursive problem that elegantly demonstrates depth-first traversal.</p><p><strong>Example:</strong> Tree <code>[4,2,7,1,3,6,9]</code> becomes <code>[4,7,2,9,6,3,1]</code>.</p>",
    bruteForce: "<p>A brute-force attempt might try to collect all nodes into an array by level, reverse each level, and rebuild the tree. This works but is unnecessarily complex. Time: O(n), Space: O(n) for the array storage.</p>",
    bruteForceInefficiency: "<p>The array-based approach requires extra passes and bookkeeping. It also requires reconstructing the tree from scratch, which is error-prone. There is a far simpler recursive solution that does the same in one pass.</p>",
    optimalApproach: "<p>Use <strong>recursive DFS</strong>: at each node, swap its left and right children, then recurse into both subtrees. The base case is a null node — return immediately.</p><ul><li><strong>Time:</strong> O(n) — visit every node exactly once.</li><li><strong>Space:</strong> O(h) — call stack depth equals tree height (O(log n) balanced, O(n) worst case).</li></ul><p>The key insight: the problem has perfect recursive structure. Inverting a tree = swapping children + inverting each subtree.</p>",
    javaCode: "class Solution {\n    public TreeNode invertTree(TreeNode root) {\n        if (root == null) return null;\n        TreeNode tmp = root.left;\n        root.left = invertTree(root.right);\n        root.right = invertTree(tmp);\n        return root;\n    }\n}",
    dryRun: "<p><strong>Input:</strong> root = [4,2,7,1,3,6,9]</p><ol><li>Visit node 4: save left=2, set left=invertTree(7), set right=invertTree(2).</li><li>invertTree(7): save left=6, set left=invertTree(9), set right=invertTree(6). Returns node 7 with children [9,6].</li><li>invertTree(2): save left=1, set left=invertTree(3), set right=invertTree(1). Returns node 2 with children [3,1].</li><li>Node 4 now has left=7(with [9,6]) and right=2(with [3,1]).</li></ol><p><strong>Output:</strong> [4,7,2,9,6,3,1] ✓</p>",
    interviewTips: "<ul><li>Mention both recursive and iterative (BFS with queue) solutions — shows breadth.</li><li>State the base case first: <code>if (root == null) return null</code>.</li><li>Clarify: does the problem want in-place mutation or a new tree? (In-place is standard.)</li><li>This problem was famously tweeted about by a developer who was rejected for not knowing it — it's a staple.</li></ul>",
    importantPoints: "<ul><li>Save <code>root.left</code> to a temp variable <em>before</em> overwriting it — otherwise you lose the reference.</li><li>The recursive call returns the inverted subtree, so assignment order matters.</li><li>An iterative BFS solution using a queue is equally valid and avoids stack overflow on very deep trees.</li><li>Edge cases: null root → return null; single node → return the node unchanged.</li></ul>"
  },
  {
    id: 47,
    name: "Maximum Depth of Binary Tree",
    category: "Trees",
    categoryId: "trees",
    difficulty: "Easy",
    explanation: "<p><strong>Maximum Depth of Binary Tree</strong> asks for the number of nodes along the longest path from the root down to the farthest leaf node.</p><p>Depth is measured in nodes (not edges), so a single-node tree has depth 1. A null tree has depth 0.</p><p><strong>Example:</strong> Tree <code>[3,9,20,null,null,15,7]</code> has max depth 3 (path: 3→20→15 or 3→20→7).</p>",
    bruteForce: "<p>A naive BFS approach visits every node level by level, counting levels until the queue is empty. This is O(n) time and O(n) space. It works correctly but uses more memory than necessary.</p>",
    bruteForceInefficiency: "<p>BFS stores an entire level in memory at once, which can be O(n/2) = O(n) for a wide tree. The recursive DFS approach achieves the same time complexity with O(h) stack space, which is better for balanced trees.</p>",
    optimalApproach: "<p>Use <strong>recursive DFS</strong>: the depth of a tree rooted at <code>node</code> is <code>1 + max(depth(left), depth(right))</code>. Base case: null node returns 0.</p><ul><li><strong>Time:</strong> O(n) — every node visited once.</li><li><strong>Space:</strong> O(h) — recursion stack depth (O(log n) balanced, O(n) skewed).</li></ul><p>This is a perfect example of how tree problems reduce to subproblems on left and right subtrees.</p>",
    javaCode: "class Solution {\n    public int maxDepth(TreeNode root) {\n        if (root == null) return 0;\n        return 1 + Math.max(maxDepth(root.left), maxDepth(root.right));\n    }\n}",
    dryRun: "<p><strong>Input:</strong> root = [3,9,20,null,null,15,7]</p><ol><li>maxDepth(3): 1 + max(maxDepth(9), maxDepth(20))</li><li>maxDepth(9): 1 + max(maxDepth(null), maxDepth(null)) = 1 + max(0,0) = 1</li><li>maxDepth(20): 1 + max(maxDepth(15), maxDepth(7)) = 1 + max(1,1) = 2</li><li>maxDepth(3): 1 + max(1, 2) = <strong>3</strong></li></ol><p><strong>Output:</strong> 3 ✓</p>",
    interviewTips: "<ul><li>This is often a warm-up question — solve it quickly and cleanly to build confidence.</li><li>Mention the iterative BFS alternative: use a queue, increment depth counter each time you process a level.</li><li>Clarify: depth vs height? In this problem they are equivalent (depth of tree = height of root).</li><li>For follow-up: minimum depth requires checking that BOTH children are null for leaf detection.</li></ul>",
    importantPoints: "<ul><li>Base case: <code>null</code> returns 0, not -1. Depth is node-count based.</li><li>Don't forget the <code>+1</code> for the current node itself.</li><li>The two-line recursive solution is considered clean and idiomatic.</li><li>Edge case: single node returns 1 (not 0).</li></ul>"
  },
  {
    id: 48,
    name: "Diameter of Binary Tree",
    category: "Trees",
    categoryId: "trees",
    difficulty: "Easy",
    explanation: "<p><strong>Diameter of Binary Tree</strong> asks for the length of the longest path between any two nodes in the tree. The path may or may not pass through the root.</p><p>The diameter at any node is <code>leftHeight + rightHeight</code> — the longest path that passes through that node going left and right. We want the maximum of this value over all nodes.</p><p><strong>Example:</strong> Tree <code>[1,2,3,4,5]</code>: the longest path is 4→2→1→3 or 5→2→1→3, length = 3 edges.</p>",
    bruteForce: "<p>For each node, compute the height of its left and right subtrees separately, then calculate the diameter through that node. Repeat for all nodes. This leads to repeated height computations. Time: O(n²) for unbalanced trees, Space: O(n).</p>",
    bruteForceInefficiency: "<p>Computing height independently for each node reprocesses the same subtrees over and over. For a skewed tree with n nodes, we compute heights O(n) times, each taking O(n), giving O(n²) total. We can fix this by computing height and updating diameter in a single DFS pass.</p>",
    optimalApproach: "<p>Use a <strong>single DFS</strong> that returns height while updating a global <code>diameter</code> variable. At each node, the height is <code>1 + max(leftH, rightH)</code> and the diameter candidate is <code>leftH + rightH</code>.</p><ul><li><strong>Time:</strong> O(n) — single pass through all nodes.</li><li><strong>Space:</strong> O(h) — recursion stack.</li></ul><p>Key insight: combine two operations (height calculation and diameter update) into one recursive function.</p>",
    javaCode: "class Solution {\n    int diameter = 0;\n    public int diameterOfBinaryTree(TreeNode root) {\n        height(root);\n        return diameter;\n    }\n    private int height(TreeNode node) {\n        if (node == null) return 0;\n        int left = height(node.left), right = height(node.right);\n        diameter = Math.max(diameter, left + right);\n        return 1 + Math.max(left, right);\n    }\n}",
    dryRun: "<p><strong>Input:</strong> root = [1,2,3,4,5]</p><ol><li>height(4): left=0, right=0, diameter=max(0,0+0)=0, return 1</li><li>height(5): left=0, right=0, diameter=max(0,0+0)=0, return 1</li><li>height(2): left=1, right=1, diameter=max(0,1+1)=<strong>2</strong>, return 2</li><li>height(3): left=0, right=0, diameter=max(2,0)=2, return 1</li><li>height(1): left=2, right=1, diameter=max(2,2+1)=<strong>3</strong>, return 3</li></ol><p><strong>Output:</strong> 3 ✓</p>",
    interviewTips: "<ul><li>The diameter doesn't have to pass through the root — this trips up many candidates.</li><li>Use an instance variable (or pass a 1-element int array) to track the running max diameter.</li><li>Distinguish between diameter (edges) and path (nodes): diameter = path_nodes - 1.</li><li>Follow-up: what if the tree has weighted edges? The approach generalizes similarly.</li></ul>",
    importantPoints: "<ul><li>The answer is in <strong>edges</strong>, not nodes: a path through 3 nodes has diameter 2.</li><li>Using a class-level variable avoids passing extra parameters through recursion.</li><li>The height function must return height (not diameter) — diameter is a side effect stored globally.</li><li>Edge case: single node → diameter = 0 (no edges).</li></ul>"
  },
  {
    id: 49,
    name: "Balanced Binary Tree",
    category: "Trees",
    categoryId: "trees",
    difficulty: "Easy",
    explanation: "<p><strong>Balanced Binary Tree</strong> asks you to determine whether a binary tree is height-balanced — meaning for every node, the heights of the left and right subtrees differ by at most 1.</p><p>Note: <em>every</em> node must satisfy this condition, not just the root. A tree can look balanced at the root but be unbalanced deeper down.</p><p><strong>Example:</strong> <code>[3,9,20,null,null,15,7]</code> → balanced (true). <code>[1,2,2,3,3,null,null,4,4]</code> → not balanced (false).</p>",
    bruteForce: "<p>For each node, compute the height of the left and right subtrees, check if their difference exceeds 1, and recurse into both subtrees. This correctly checks all nodes but recomputes heights repeatedly. Time: O(n²), Space: O(n).</p>",
    bruteForceInefficiency: "<p>The naive approach calls a height function inside a balance-check function, and the height function itself recurses through subtrees — leading to O(n) height computations per node, giving O(n²) overall. We can merge the two into one pass.</p>",
    optimalApproach: "<p>Use a <strong>bottom-up DFS</strong> that returns the height if the subtree is balanced, or <code>-1</code> as a sentinel value if it's unbalanced. Once a subtree returns -1, propagate -1 up immediately — this is an early exit.</p><ul><li><strong>Time:</strong> O(n) — single DFS pass.</li><li><strong>Space:</strong> O(h) — recursion stack.</li></ul>",
    javaCode: "class Solution {\n    public boolean isBalanced(TreeNode root) {\n        return check(root) != -1;\n    }\n    private int check(TreeNode node) {\n        if (node == null) return 0;\n        int left = check(node.left), right = check(node.right);\n        if (left == -1 || right == -1 || Math.abs(left - right) > 1) return -1;\n        return 1 + Math.max(left, right);\n    }\n}",
    dryRun: "<p><strong>Input:</strong> root = [1,2,2,3,3,null,null,4,4]</p><ol><li>check(4): return 1; check(4): return 1</li><li>check(3 left): left=1, right=1, diff=0, return 2</li><li>check(3 right): left=0, right=0, return 1</li><li>check(2 left): left=2, right=1, diff=1, return 3</li><li>check(2 right): left=0, right=0, return 1</li><li>check(1): left=3, right=1, |3-1|=2 &gt; 1 → return <strong>-1</strong></li></ol><p><strong>Output:</strong> false ✓</p>",
    interviewTips: "<ul><li>The -1 sentinel trick is the key optimization — mention it explicitly to impress interviewers.</li><li>Clarify the definition: balanced means EVERY node's subtrees differ by at most 1 in height.</li><li>You can also return a pair (isBalanced, height) instead of the -1 trick — more readable in some opinions.</li><li>Don't confuse with a complete binary tree or perfect binary tree.</li></ul>",
    importantPoints: "<ul><li>Return -1 to signal 'unbalanced' — this doubles as both height and error code.</li><li>Check for <code>left == -1 || right == -1</code> first to short-circuit early.</li><li>The difference must be <em>at most</em> 1, so <code>Math.abs(left - right) &gt; 1</code> triggers the unbalanced return.</li><li>A null node has height 0 — it's trivially balanced.</li></ul>"
  },
  {
    id: 50,
    name: "Same Tree",
    category: "Trees",
    categoryId: "trees",
    difficulty: "Easy",
    explanation: "<p><strong>Same Tree</strong> asks whether two binary trees are structurally identical and have the same node values at every position.</p><p>Two trees are the same if: both are null, OR both are non-null with equal root values AND their left subtrees are the same AND their right subtrees are the same.</p><p><strong>Example:</strong> <code>p=[1,2,3]</code>, <code>q=[1,2,3]</code> → true. <code>p=[1,2]</code>, <code>q=[1,null,2]</code> → false.</p>",
    bruteForce: "<p>Serialize both trees to strings (e.g., preorder traversal with null markers) and compare the strings. This works but uses O(n) extra space for the strings and requires careful serialization. Time: O(n), Space: O(n).</p>",
    bruteForceInefficiency: "<p>Serialization involves building strings which have concatenation overhead. A direct recursive comparison is simpler, requires no extra space beyond the call stack, and short-circuits early when a mismatch is found.</p>",
    optimalApproach: "<p>Use <strong>recursive DFS</strong> with three base cases: (1) both null → true; (2) one null, one not → false; (3) values differ → false. Then recurse on both subtrees and AND the results.</p><ul><li><strong>Time:</strong> O(n) — compare every node.</li><li><strong>Space:</strong> O(h) — recursion stack.</li></ul>",
    javaCode: "class Solution {\n    public boolean isSameTree(TreeNode p, TreeNode q) {\n        if (p == null && q == null) return true;\n        if (p == null || q == null || p.val != q.val) return false;\n        return isSameTree(p.left, q.left) && isSameTree(p.right, q.right);\n    }\n}",
    dryRun: "<p><strong>Input:</strong> p=[1,2,3], q=[1,2,3]</p><ol><li>isSameTree(1,1): vals equal → recurse left and right</li><li>isSameTree(2,2): vals equal → recurse on null,null → true on both sides → return true</li><li>isSameTree(3,3): vals equal → recurse on null,null → true on both sides → return true</li><li>isSameTree(1,1): true &amp;&amp; true = <strong>true</strong></li></ol><p><strong>Output:</strong> true ✓</p>",
    interviewTips: "<ul><li>Handle the null cases first before accessing <code>.val</code> to avoid NullPointerException.</li><li>The combined null check <code>p == null || q == null</code> (after ruling out both-null) implies exactly one is null.</li><li>An iterative solution with two stacks is also valid and avoids recursion depth issues.</li><li>Follow-up: How would you compare trees with floating-point values? (Use epsilon comparison.)</li></ul>",
    importantPoints: "<ul><li>Order of null checks matters: check both-null first, then one-null.</li><li>Short-circuit evaluation: if left subtrees differ, Java won't even check right subtrees.</li><li>This is a building block for the Subtree of Another Tree problem.</li><li>Works for trees of any structure — doesn't assume BST ordering.</li></ul>"
  },
  {
    id: 51,
    name: "Subtree of Another Tree",
    category: "Trees",
    categoryId: "trees",
    difficulty: "Easy",
    explanation: "<p><strong>Subtree of Another Tree</strong> asks: given trees <code>root</code> and <code>subRoot</code>, does <code>subRoot</code> appear as a subtree anywhere in <code>root</code>?</p><p>A subtree means a node in <code>root</code> where that node and all its descendants are identical to <code>subRoot</code>. The entire subtree must match — not just a partial match.</p><p><strong>Example:</strong> root=[3,4,5,1,2], subRoot=[4,1,2] → true (node 4 in root matches subRoot exactly).</p>",
    bruteForce: "<p>For every node in <code>root</code>, check if the subtree rooted there is identical to <code>subRoot</code> using the Same Tree logic. This is O(n * m) where n = nodes in root, m = nodes in subRoot.</p>",
    bruteForceInefficiency: "<p>O(n * m) is acceptable for small inputs but becomes slow when both trees are large. A string serialization approach using KMP or suffix trees can achieve O(n + m), but the O(n * m) recursive solution is typically accepted in interviews.</p>",
    optimalApproach: "<p>Combine DFS with the <strong>isSameTree</strong> helper: at each node in <code>root</code>, check if the subtree rooted there equals <code>subRoot</code>. If not, recurse into left and right children.</p><ul><li><strong>Time:</strong> O(n * m) — for each of n nodes, potentially compare m nodes.</li><li><strong>Space:</strong> O(h) — recursion stack depth of root tree.</li></ul>",
    javaCode: "class Solution {\n    public boolean isSubtree(TreeNode root, TreeNode subRoot) {\n        if (root == null) return false;\n        if (isSame(root, subRoot)) return true;\n        return isSubtree(root.left, subRoot) || isSubtree(root.right, subRoot);\n    }\n    private boolean isSame(TreeNode s, TreeNode t) {\n        if (s == null && t == null) return true;\n        if (s == null || t == null || s.val != t.val) return false;\n        return isSame(s.left, t.left) && isSame(s.right, t.right);\n    }\n}",
    dryRun: "<p><strong>Input:</strong> root=[3,4,5,1,2], subRoot=[4,1,2]</p><ol><li>isSubtree(3): isSame(3,4)? 3≠4 → false. Recurse left and right.</li><li>isSubtree(4): isSame(4,4)? 4==4, check children. isSame(1,1)→true, isSame(2,2)→true → <strong>true!</strong></li><li>Short-circuit: right branch not checked.</li></ol><p><strong>Output:</strong> true ✓</p>",
    interviewTips: "<ul><li>Reuse the isSameTree logic — demonstrate code reuse.</li><li>Mention the O(n+m) string serialization optimization as a follow-up.</li><li>Careful: a node with value matching subRoot's root doesn't guarantee a subtree match — you must compare the whole structure.</li><li>The subRoot must be an <em>exact</em> subtree, not just contained within a subtree.</li></ul>",
    importantPoints: "<ul><li>If root is null but subRoot is not null → return false (subRoot can't be found).</li><li>If subRoot is null → return true (null is a subtree of any tree, by convention — though LeetCode constraints say subRoot is non-null).</li><li>Short-circuit with <code>||</code>: if left subtree finds a match, don't search right.</li><li>The isSame helper must check structural equality, not just value equality at the root.</li></ul>"
  },
  {
    id: 52,
    name: "Lowest Common Ancestor of a BST",
    category: "Trees",
    categoryId: "trees",
    difficulty: "Medium",
    explanation: "<p><strong>Lowest Common Ancestor (LCA) of a BST</strong>: given a BST and two nodes p and q, find their lowest common ancestor — the deepest node that has both p and q as descendants (a node is a descendant of itself).</p><p>The BST property is key: for any node, all left descendants are smaller and all right descendants are larger. This lets us efficiently navigate toward p and q.</p><p><strong>Example:</strong> BST root=[6,2,8,0,4,7,9], p=2, q=8 → LCA=6. p=2, q=4 → LCA=2.</p>",
    bruteForce: "<p>For a general binary tree (not BST), you'd recursively search both subtrees and return a node when it equals p or q — or when both subtrees return non-null results. Time: O(n), Space: O(n). For a BST we can do better by leveraging ordering.</p>",
    bruteForceInefficiency: "<p>The general O(n) approach ignores the BST's sorted property and searches blindly. Since we know p and q's values relative to any node, we can always navigate in exactly one direction, making the BST solution O(log n) for balanced trees.</p>",
    optimalApproach: "<p>Iteratively (or recursively) navigate the BST using its ordering property:</p><ul><li>If both p and q are less than <code>root.val</code>: LCA is in the left subtree.</li><li>If both p and q are greater than <code>root.val</code>: LCA is in the right subtree.</li><li>Otherwise (they split across the current node, or one equals the current node): current node IS the LCA.</li></ul><p><strong>Time:</strong> O(log n) balanced / O(n) worst. <strong>Space:</strong> O(1) iterative.</p>",
    javaCode: "class Solution {\n    public TreeNode lowestCommonAncestor(TreeNode root, TreeNode p, TreeNode q) {\n        while (root != null) {\n            if (p.val < root.val && q.val < root.val) root = root.left;\n            else if (p.val > root.val && q.val > root.val) root = root.right;\n            else return root;\n        }\n        return null;\n    }\n}",
    dryRun: "<p><strong>Input:</strong> root=[6,2,8,0,4,7,9], p=2, q=4</p><ol><li>root=6: p.val=2 &lt; 6 and q.val=4 &lt; 6 → go left, root=2</li><li>root=2: p.val=2 == 2 (not both less, not both greater) → return <strong>node 2</strong></li></ol><p><strong>Output:</strong> 2 ✓</p><p><strong>p=2, q=8:</strong> root=6, 2&lt;6 but 8&gt;6 → they split → return <strong>6</strong> ✓</p>",
    interviewTips: "<ul><li>Explicitly state you're using the BST property — this is what makes the O(log n) solution possible.</li><li>The iterative version is preferred over recursive to avoid stack overhead.</li><li>Handle the edge case where p or q equals the current node — that node is its own ancestor.</li><li>For a general binary tree (not BST), you'd need a different O(n) approach.</li></ul>",
    importantPoints: "<ul><li>The 'split' condition (else clause) handles three cases: p is ancestor of q, q is ancestor of p, or they're in different subtrees.</li><li>Guaranteed by problem constraints: both p and q exist in the tree.</li><li>Iterative approach uses O(1) space — highlight this improvement over recursion.</li><li>BST property: left subtree &lt; root &lt; right subtree (strictly, no duplicates).</li></ul>"
  },
  {
    id: 53,
    name: "Binary Tree Level Order Traversal",
    category: "Trees",
    categoryId: "trees",
    difficulty: "Medium",
    explanation: "<p><strong>Binary Tree Level Order Traversal</strong> visits nodes level by level, left to right, returning a list of lists where each inner list contains values at that depth.</p><p>This is <strong>Breadth-First Search (BFS)</strong> applied to trees. BFS naturally processes nodes level by level using a queue data structure.</p><p><strong>Example:</strong> Tree <code>[3,9,20,null,null,15,7]</code> → <code>[[3],[9,20],[15,7]]</code>.</p>",
    bruteForce: "<p>A DFS approach can also achieve level-order by passing the current depth as a parameter and appending to a list at index <code>depth</code>. While this works and is O(n), it's less intuitive than BFS for this problem.</p>",
    bruteForceInefficiency: "<p>DFS with depth tracking requires maintaining a list-of-lists and indexing by depth — adds cognitive overhead. BFS with a queue is the natural fit for level-order traversal: the queue inherently maintains the current level's nodes.</p>",
    optimalApproach: "<p>Use <strong>BFS with a queue</strong>. At each iteration: record the queue's current size (= number of nodes at this level), dequeue exactly that many nodes into a level list, enqueue their children. Repeat until queue is empty.</p><ul><li><strong>Time:</strong> O(n) — process each node once.</li><li><strong>Space:</strong> O(n) — queue holds up to one full level (at most n/2 nodes for a complete tree).</li></ul>",
    javaCode: "class Solution {\n    public List<List<Integer>> levelOrder(TreeNode root) {\n        List<List<Integer>> res = new ArrayList<>();\n        if (root == null) return res;\n        Queue<TreeNode> q = new LinkedList<>();\n        q.offer(root);\n        while (!q.isEmpty()) {\n            int size = q.size();\n            List<Integer> level = new ArrayList<>();\n            for (int i = 0; i < size; i++) {\n                TreeNode node = q.poll();\n                level.add(node.val);\n                if (node.left != null) q.offer(node.left);\n                if (node.right != null) q.offer(node.right);\n            }\n            res.add(level);\n        }\n        return res;\n    }\n}",
    dryRun: "<p><strong>Input:</strong> root = [3,9,20,null,null,15,7]</p><ol><li>q=[3], size=1: dequeue 3, add to level, enqueue 9,20. level=[3]. res=[[3]]</li><li>q=[9,20], size=2: dequeue 9 (no children), dequeue 20 (enqueue 15,7). level=[9,20]. res=[[3],[9,20]]</li><li>q=[15,7], size=2: dequeue 15 (no children), dequeue 7 (no children). level=[15,7]. res=[[3],[9,20],[15,7]]</li></ol><p><strong>Output:</strong> [[3],[9,20],[15,7]] ✓</p>",
    interviewTips: "<ul><li>The <code>size = q.size()</code> snapshot is crucial — it tells you exactly how many nodes are on the current level before you start adding the next level's nodes.</li><li>This pattern (BFS with level size snapshot) is used in many tree problems: zigzag traversal, right side view, etc.</li><li>Mention that <code>LinkedList</code> implements <code>Queue</code> in Java; use <code>offer/poll</code>, not <code>add/remove</code>.</li></ul>",
    importantPoints: "<ul><li>Capture <code>q.size()</code> before the inner loop — the queue grows as you enqueue children during the same iteration.</li><li>Only enqueue non-null children to avoid null pointer issues.</li><li>For zigzag traversal: alternate between adding to front or back of level list based on level parity.</li><li>Edge case: empty tree → return empty list (not null).</li></ul>"
  },
  {
    id: 54,
    name: "Binary Tree Right Side View",
    category: "Trees",
    categoryId: "trees",
    difficulty: "Medium",
    explanation: "<p><strong>Binary Tree Right Side View</strong>: imagine standing to the right of the tree and looking left. Return the values of nodes you can see — one node per level (the rightmost node at each depth).</p><p>This is a variation of level order traversal where you only care about the last node in each level.</p><p><strong>Example:</strong> Tree <code>[1,2,3,null,5,null,4]</code> → <code>[1,3,4]</code>. From the right, you see 1 (level 0), 3 (level 1), 4 (level 2).</p>",
    bruteForce: "<p>Perform a full level order traversal collecting all nodes, then pick the last element from each level's list. This is correct but stores the entire level unnecessarily.</p>",
    bruteForceInefficiency: "<p>Storing all nodes at every level wastes space when we only need the rightmost. We can optimize by only recording the last node seen in each BFS level, or by using a DFS that visits right children first.</p>",
    optimalApproach: "<p>Use <strong>BFS with level size tracking</strong>: process each level's nodes, and when processing the last node in a level (index == size-1), add it to the result. Alternatively, use DFS going right-first and tracking depth.</p><ul><li><strong>Time:</strong> O(n).</li><li><strong>Space:</strong> O(n) — queue holds one level at a time.</li></ul>",
    javaCode: "class Solution {\n    public List<Integer> rightSideView(TreeNode root) {\n        List<Integer> res = new ArrayList<>();\n        if (root == null) return res;\n        Queue<TreeNode> q = new LinkedList<>();\n        q.offer(root);\n        while (!q.isEmpty()) {\n            int size = q.size();\n            for (int i = 0; i < size; i++) {\n                TreeNode node = q.poll();\n                if (i == size - 1) res.add(node.val);\n                if (node.left != null) q.offer(node.left);\n                if (node.right != null) q.offer(node.right);\n            }\n        }\n        return res;\n    }\n}",
    dryRun: "<p><strong>Input:</strong> root = [1,2,3,null,5,null,4]</p><ol><li>Level 0: size=1, process node 1, i==0==size-1 → add 1. Enqueue 2,3. res=[1]</li><li>Level 1: size=2, process node 2 (i=0, not last), process node 3 (i=1==size-1) → add 3. Enqueue 5, 4. res=[1,3]</li><li>Level 2: size=2, process node 5 (i=0), process node 4 (i=1==size-1) → add 4. res=[1,3,4]</li></ol><p><strong>Output:</strong> [1,3,4] ✓</p>",
    interviewTips: "<ul><li>Note: the rightmost visible node isn't always a right child — if a node only has a left child, that left child is visible from the right at its level.</li><li>Mention both BFS and DFS (right-first preorder) approaches.</li><li>DFS approach: visit right child before left, add node.val when depth == res.size() (first time visiting a new depth from the right).</li></ul>",
    importantPoints: "<ul><li>The last node processed in each BFS level is the rightmost visible node.</li><li><code>if (i == size - 1)</code> correctly identifies the last node in each level's for-loop.</li><li>The right-side view of a single node is just <code>[root.val]</code>.</li><li>Don't confuse 'rightmost' with 'right child' — they differ when nodes have only left children.</li></ul>"
  },
  {
    id: 55,
    name: "Count Good Nodes in Binary Tree",
    category: "Trees",
    categoryId: "trees",
    difficulty: "Medium",
    explanation: "<p><strong>Count Good Nodes in Binary Tree</strong>: a node X is 'good' if on the path from the root to X, there is no node with a value greater than X's value. Count all good nodes.</p><p>Equivalently, a node is good if its value is greater than or equal to the maximum value seen on the path from root to that node.</p><p><strong>Example:</strong> Tree <code>[3,1,4,3,null,1,5]</code>: good nodes are 3 (root), 4, 3, 5 → output 4.</p>",
    bruteForce: "<p>For each node, traverse the path from root to that node and track the maximum. Check if the node's value is at least that maximum. This requires storing the full path or recomputing paths, leading to O(n²) time.</p>",
    bruteForceInefficiency: "<p>Recomputing the path maximum for every node is redundant — we process the same edges repeatedly. Instead, carry the running maximum as a parameter during a single DFS traversal.</p>",
    optimalApproach: "<p>Use <strong>DFS with a running maximum</strong>: pass <code>maxSoFar</code> (the max value seen from root to current node) down the recursion. At each node, check if <code>node.val >= maxSoFar</code> (good node), then update <code>maxSoFar</code> and recurse.</p><ul><li><strong>Time:</strong> O(n) — visit each node once.</li><li><strong>Space:</strong> O(h) — recursion stack.</li></ul>",
    javaCode: "class Solution {\n    public int goodNodes(TreeNode root) {\n        return dfs(root, Integer.MIN_VALUE);\n    }\n    private int dfs(TreeNode node, int maxSoFar) {\n        if (node == null) return 0;\n        int good = node.val >= maxSoFar ? 1 : 0;\n        maxSoFar = Math.max(maxSoFar, node.val);\n        return good + dfs(node.left, maxSoFar) + dfs(node.right, maxSoFar);\n    }\n}",
    dryRun: "<p><strong>Input:</strong> root = [3,1,4,3,null,1,5]</p><ol><li>dfs(3, MIN): 3 >= MIN → good=1, maxSoFar=3. Recurse left(1,3), right(4,3).</li><li>dfs(1, 3): 1 &lt; 3 → good=0, maxSoFar=3. Recurse left(3,3), right(null,3).</li><li>dfs(3, 3): 3 >= 3 → good=1. Recurse on nulls → 0+0. Return 1.</li><li>dfs(4, 3): 4 >= 3 → good=1, maxSoFar=4. Recurse left(1,4), right(5,4).</li><li>dfs(1, 4): 1 &lt; 4 → good=0. Return 0.</li><li>dfs(5, 4): 5 >= 4 → good=1. Return 1.</li><li>Total: 1 + (0 + 1) + (1 + 0 + 1) = <strong>4</strong></li></ol><p><strong>Output:</strong> 4 ✓</p>",
    interviewTips: "<ul><li>Initialize <code>maxSoFar</code> as <code>Integer.MIN_VALUE</code> so the root is always counted as good.</li><li>The root is ALWAYS a good node (no larger values exist on the path from root to root).</li><li>This problem tests your ability to propagate state (maxSoFar) downward through the tree.</li></ul>",
    importantPoints: "<ul><li>Use <code>Integer.MIN_VALUE</code> (not 0) as initial max, since node values can be negative.</li><li>Update <code>maxSoFar</code> AFTER checking whether the current node is good.</li><li>This is a top-down DFS pattern: information flows from root downward.</li><li>Contrast with Diameter/Balanced problems which are bottom-up: information flows upward.</li></ul>"
  },
  {
    id: 56,
    name: "Validate Binary Search Tree",
    category: "Trees",
    categoryId: "trees",
    difficulty: "Medium",
    explanation: "<p><strong>Validate Binary Search Tree</strong>: determine whether a given binary tree is a valid BST. A valid BST requires that for every node: all nodes in its left subtree have values strictly less than it, and all nodes in its right subtree have values strictly greater than it.</p><p><strong>Common mistake:</strong> only checking the immediate children is insufficient. A node deep in the right subtree must also satisfy the ancestor's constraints.</p><p><strong>Example:</strong> <code>[5,1,4,null,null,3,6]</code> → false (node 3 is in right subtree of 5 but 3 &lt; 5).</p>",
    bruteForce: "<p>For each node, check all nodes in its left subtree (must all be less) and all nodes in its right subtree (must all be greater). This is O(n²) and misses the subtlety of inherited constraints from ancestors.</p>",
    bruteForceInefficiency: "<p>Checking every pair of ancestors and descendants is expensive and complex. The key insight is that each node inherits a valid range <code>(min, max)</code> from its ancestors, which can be propagated in a single DFS pass.</p>",
    optimalApproach: "<p>Use <strong>DFS with min/max bounds</strong>: each node must satisfy <code>min &lt; node.val &lt; max</code>. When going left, update max to current node's value. When going right, update min to current node's value.</p><ul><li><strong>Time:</strong> O(n).</li><li><strong>Space:</strong> O(h).</li></ul><p>Use <code>Long.MIN_VALUE / Long.MAX_VALUE</code> to handle integer boundary values.</p>",
    javaCode: "class Solution {\n    public boolean isValidBST(TreeNode root) {\n        return validate(root, Long.MIN_VALUE, Long.MAX_VALUE);\n    }\n    private boolean validate(TreeNode node, long min, long max) {\n        if (node == null) return true;\n        if (node.val <= min || node.val >= max) return false;\n        return validate(node.left, min, node.val) && validate(node.right, node.val, max);\n    }\n}",
    dryRun: "<p><strong>Input:</strong> root = [5,1,4,null,null,3,6]</p><ol><li>validate(5, LONG_MIN, LONG_MAX): 5 in range. Left: validate(1, LONG_MIN, 5). Right: validate(4, 5, LONG_MAX).</li><li>validate(1, LONG_MIN, 5): 1 in range. No children → true.</li><li>validate(4, 5, LONG_MAX): 4 &lt;= 5 (min) → return <strong>false</strong></li><li>validate(5, ...): true &amp;&amp; false = <strong>false</strong></li></ol><p><strong>Output:</strong> false ✓</p>",
    interviewTips: "<ul><li>The classic mistake: checking only parent-child comparisons misses cases like a node being greater than a grandparent on the left side.</li><li>Use <code>long</code> bounds (not <code>int</code>) to handle the edge case where node values are <code>Integer.MIN_VALUE</code> or <code>Integer.MAX_VALUE</code>.</li><li>In-order traversal alternative: inorder of a valid BST produces a strictly increasing sequence.</li></ul>",
    importantPoints: "<ul><li>BST uses strict inequality: equal values are NOT allowed (no duplicates).</li><li>Use <code>Long</code> bounds to avoid integer overflow edge cases with min/max integer values.</li><li>Left subtree gets upper bound of <code>node.val</code>; right subtree gets lower bound of <code>node.val</code>.</li><li>Alternative: inorder traversal, check each value > previous value.</li></ul>"
  },
  {
    id: 57,
    name: "Kth Smallest Element in a BST",
    category: "Trees",
    categoryId: "trees",
    difficulty: "Medium",
    explanation: "<p><strong>Kth Smallest Element in a BST</strong>: given a BST and integer k, return the kth smallest value (1-indexed) among all node values.</p><p>Key insight: <strong>inorder traversal of a BST yields values in sorted ascending order</strong>. The kth smallest element is the kth value produced by inorder traversal.</p><p><strong>Example:</strong> BST = [3,1,4,null,2], k=1 → 1. BST = [5,3,6,2,4,null,null,1], k=3 → 3.</p>",
    bruteForce: "<p>Collect all node values via any traversal into a list, sort the list, then return the element at index k-1. Time: O(n log n) for sorting, Space: O(n).</p>",
    bruteForceInefficiency: "<p>Sorting is unnecessary because the BST's structure already provides sorted order via inorder traversal. We can get sorted order in O(n) without any sorting step. Further, we can stop early once we've found the kth element.</p>",
    optimalApproach: "<p>Use <strong>inorder traversal</strong> (left → node → right) with a counter. Increment the counter at each visited node. When the counter reaches k, record the result and stop. This avoids processing any more nodes after finding the answer.</p><ul><li><strong>Time:</strong> O(k + h) where h is the height — we stop at the kth node.</li><li><strong>Space:</strong> O(h) for the recursion stack.</li></ul>",
    javaCode: "class Solution {\n    int count = 0, result = 0;\n    public int kthSmallest(TreeNode root, int k) {\n        inorder(root, k);\n        return result;\n    }\n    private void inorder(TreeNode node, int k) {\n        if (node == null) return;\n        inorder(node.left, k);\n        if (++count == k) { result = node.val; return; }\n        inorder(node.right, k);\n    }\n}",
    dryRun: "<p><strong>Input:</strong> BST = [3,1,4,null,2], k=2</p><p>Inorder traversal visits: 1, 2, 3, 4 (sorted order)</p><ol><li>inorder(3) → go left to inorder(1) → go left to null</li><li>Back at 1: count=1, not 2 yet. Go right to inorder(2).</li><li>inorder(2) → go left to null. Back at 2: count=2 == k → result=2, return!</li></ol><p><strong>Output:</strong> 2 ✓</p>",
    interviewTips: "<ul><li>State immediately: 'inorder traversal of BST gives sorted order — this is the key insight.'</li><li>The iterative inorder with a stack allows explicit early termination and is preferred for large trees.</li><li>Follow-up: if the BST is frequently modified and you need to find kth smallest often, augment each node with a subtree size counter for O(log n) queries.</li></ul>",
    importantPoints: "<ul><li>Inorder traversal order: left subtree → current node → right subtree.</li><li>Early termination: once count == k, no need to continue traversal.</li><li>The result variable must be accessible after inorder completes — use class field or wrapper array.</li><li>k is 1-indexed, so use <code>++count == k</code> (pre-increment) not <code>count++ == k</code>.</li></ul>"
  },
  {
    id: 58,
    name: "Construct Binary Tree from Preorder and Inorder Traversal",
    category: "Trees",
    categoryId: "trees",
    difficulty: "Medium",
    explanation: "<p><strong>Construct Binary Tree from Preorder and Inorder Traversal</strong>: given the preorder and inorder traversal arrays of a binary tree, reconstruct the original tree.</p><p><strong>Key properties:</strong> (1) The first element of preorder is always the root. (2) In the inorder array, the root splits it into left and right subtrees — elements to the left of root's index are the left subtree, elements to the right are the right subtree.</p><p><strong>Example:</strong> preorder=[3,9,20,15,7], inorder=[9,3,15,20,7] → tree [3,9,20,null,null,15,7].</p>",
    bruteForce: "<p>Recursively find the root from preorder[0], search linearly through the inorder array to find its index, split, and recurse. This works but searching the inorder array takes O(n) each time. Total time: O(n²).</p>",
    bruteForceInefficiency: "<p>Linear search for the root's inorder index is repeated for every node — O(n) per node × O(n) nodes = O(n²). A HashMap can precompute all inorder indices in O(n), reducing each lookup to O(1).</p>",
    optimalApproach: "<p>Precompute a <strong>HashMap</strong> of value → inorder index for O(1) lookups. Use a global preorder index pointer. Recursively build: take preorder[preIdx++] as root, find its inorder position, build left subtree from inorder left portion, build right subtree from inorder right portion.</p><ul><li><strong>Time:</strong> O(n).</li><li><strong>Space:</strong> O(n) for the hashmap.</li></ul>",
    javaCode: "class Solution {\n    int preIdx = 0;\n    Map<Integer, Integer> inMap = new HashMap<>();\n    public TreeNode buildTree(int[] preorder, int[] inorder) {\n        for (int i = 0; i < inorder.length; i++) inMap.put(inorder[i], i);\n        return build(preorder, 0, inorder.length - 1);\n    }\n    private TreeNode build(int[] pre, int inLeft, int inRight) {\n        if (inLeft > inRight) return null;\n        int val = pre[preIdx++];\n        TreeNode node = new TreeNode(val);\n        int idx = inMap.get(val);\n        node.left = build(pre, inLeft, idx - 1);\n        node.right = build(pre, idx + 1, inRight);\n        return node;\n    }\n}",
    dryRun: "<p><strong>Input:</strong> preorder=[3,9,20,15,7], inorder=[9,3,15,20,7]</p><p>inMap: {9:0, 3:1, 15:2, 20:3, 7:4}</p><ol><li>build(0,4): val=3(preIdx=0→1), idx=1. Left: build(0,0). Right: build(2,4).</li><li>build(0,0): val=9(preIdx=1→2), idx=0. Left: build(0,-1)=null. Right: build(1,0)=null. Return node(9).</li><li>build(2,4): val=20(preIdx=2→3), idx=3. Left: build(2,2). Right: build(4,4).</li><li>build(2,2): val=15(preIdx=3→4), idx=2. Left/Right: null. Return node(15).</li><li>build(4,4): val=7(preIdx=4→5), idx=4. Return node(7).</li></ol><p><strong>Output:</strong> Tree [3,9,20,null,null,15,7] ✓</p>",
    interviewTips: "<ul><li>Draw an example on paper first — it makes the algorithm very clear visually.</li><li>The preIdx must be a class field (or passed by reference) because it needs to increment across all recursive calls globally.</li><li>This problem assumes all values are unique (no duplicates in the tree).</li><li>A sister problem exists for postorder + inorder: postorder's last element is the root.</li></ul>",
    importantPoints: "<ul><li>Left subtree in preorder is the next (idx - inLeft) elements after the root.</li><li>Process LEFT subtree before RIGHT to match preorder sequence (root, left, right).</li><li>The inorder range [inLeft, inRight] tracks which part of the inorder array we're working in.</li><li>HashMap lookup is O(1) vs O(n) linear scan — critical optimization.</li></ul>"
  },
  {
    id: 59,
    name: "Binary Tree Maximum Path Sum",
    category: "Trees",
    categoryId: "trees",
    difficulty: "Hard",
    explanation: "<p><strong>Binary Tree Maximum Path Sum</strong>: a path in a binary tree is a sequence of nodes where each pair of adjacent nodes has an edge between them. A node can appear at most once. The path does not need to go through the root. Find the maximum sum of any path.</p><p>The challenge: the maximum path might form an inverted 'V' (arch) through any node as the peak, going into both left and right subtrees. We must consider all such arches across every node.</p><p><strong>Example:</strong> Tree <code>[-10,9,20,null,null,15,7]</code> → max path sum = 42 (path: 15→20→7).</p>",
    bruteForce: "<p>Enumerate all possible paths: for each pair of nodes, compute the path sum between them. This is O(n²) pairs and computing each path can take O(n), giving O(n³) overall — far too slow for large trees.</p>",
    bruteForceInefficiency: "<p>Most paths share subtrees — we recompute the same subtree sums repeatedly. A single post-order DFS can compute the maximum contribution of each subtree in one pass, allowing us to evaluate every potential 'arch' peak in O(n).</p>",
    optimalApproach: "<p>Use <strong>post-order DFS</strong>. The function returns the maximum <em>single-path</em> contribution from the current node going downward (max(left, right) + node.val). But at each node, we also compute the potential arch: node.val + left_contribution + right_contribution, and update a global maximum.</p><p>Negative contributions are ignored by clamping with <code>Math.max(0, ...)</code>.</p><ul><li><strong>Time:</strong> O(n).</li><li><strong>Space:</strong> O(h).</li></ul>",
    javaCode: "class Solution {\n    int maxSum = Integer.MIN_VALUE;\n    public int maxPathSum(TreeNode root) {\n        dfs(root);\n        return maxSum;\n    }\n    private int dfs(TreeNode node) {\n        if (node == null) return 0;\n        int left = Math.max(dfs(node.left), 0);\n        int right = Math.max(dfs(node.right), 0);\n        maxSum = Math.max(maxSum, node.val + left + right);\n        return node.val + Math.max(left, right);\n    }\n}",
    dryRun: "<p><strong>Input:</strong> root = [-10,9,20,null,null,15,7]</p><ol><li>dfs(9): left=0, right=0, maxSum=max(MIN,-10+9+0)=... update later. return 9. Actually: maxSum=max(MIN,9+0+0)=9. return 9.</li><li>dfs(15): left=0,right=0, maxSum=max(9,15)=15, return 15.</li><li>dfs(7): left=0,right=0, maxSum=max(15,7)=15, return 7.</li><li>dfs(20): left=15,right=7, maxSum=max(15, 20+15+7)=<strong>42</strong>, return 20+15=35.</li><li>dfs(-10): left=max(9,0)=9, right=max(35,0)=35, maxSum=max(42,-10+9+35)=max(42,34)=42, return -10+35=25.</li></ol><p><strong>Output:</strong> 42 ✓</p>",
    interviewTips: "<ul><li>The tricky part: when returning up the recursion, you can only extend the path in ONE direction (left or right), because a path can't fork upward.</li><li>When computing the arch at each node, you CAN include both left and right contributions.</li><li>Clamp negative contributions to 0 — including a negative branch only hurts the sum.</li><li>Initialize maxSum to <code>Integer.MIN_VALUE</code> because all node values could be negative.</li></ul>",
    importantPoints: "<ul><li>The returned value from dfs is the max single-branch extension: <code>node.val + max(left, right)</code>.</li><li>The arch candidate (global max update) uses BOTH left and right: <code>node.val + left + right</code>.</li><li>The two operations (return value vs maxSum update) are different — don't confuse them.</li><li>A tree with all negative values still has a valid path (single node with maximum value).</li></ul>"
  },
  {
    id: 60,
    name: "Serialize and Deserialize Binary Tree",
    category: "Trees",
    categoryId: "trees",
    difficulty: "Hard",
    explanation: "<p><strong>Serialize and Deserialize Binary Tree</strong>: design an algorithm to convert a binary tree to a string (serialize) and reconstruct the tree from that string (deserialize). The encoding scheme is up to you — any format that allows perfect reconstruction is valid.</p><p>This tests your ability to design a complete, round-trip encoding for a complex data structure.</p><p><strong>Example:</strong> Tree [1,2,3,null,null,4,5] → serialize to '1,2,N,N,3,4,N,N,5,N,N' → deserialize back to the original tree.</p>",
    bruteForce: "<p>A level-order (BFS) serialization stores nodes level by level, using null markers for empty nodes — similar to how LeetCode represents trees. This works but requires careful parsing during deserialization, tracking which nodes are parents of which nulls.</p>",
    bruteForceInefficiency: "<p>BFS serialization stores many null markers for sparse trees and requires index arithmetic to pair parents with children. A preorder DFS serialization with null markers is simpler to implement correctly and more naturally recursive.</p>",
    optimalApproach: "<p>Use <strong>preorder DFS serialization</strong>: encode each node as its value, use 'N' for null, separate by commas. For deserialization, split on comma to get a token array, then recursively build: if token is 'N' return null, else create a node and recursively build left then right subtrees.</p><ul><li><strong>Time:</strong> O(n) for both operations.</li><li><strong>Space:</strong> O(n) for the string / token array.</li></ul>",
    javaCode: "public class Codec {\n    public String serialize(TreeNode root) {\n        if (root == null) return \"N\";\n        return root.val + \",\" + serialize(root.left) + \",\" + serialize(root.right);\n    }\n    private int idx;\n    public TreeNode deserialize(String data) {\n        String[] vals = data.split(\",\");\n        idx = 0;\n        return des(vals);\n    }\n    private TreeNode des(String[] vals) {\n        if (vals[idx].equals(\"N\")) { idx++; return null; }\n        TreeNode node = new TreeNode(Integer.parseInt(vals[idx++]));\n        node.left = des(vals);\n        node.right = des(vals);\n        return node;\n    }\n}",
    dryRun: "<p><strong>Serialize [1,2,3,null,null,4,5]:</strong></p><ol><li>serialize(1): '1,' + serialize(2) + ',' + serialize(3)</li><li>serialize(2): '2,' + serialize(null) + ',' + serialize(null) = '2,N,N'</li><li>serialize(3): '3,' + serialize(4) + ',' + serialize(5) = '3,4,N,N,5,N,N'</li><li>Result: '1,2,N,N,3,4,N,N,5,N,N'</li></ol><p><strong>Deserialize:</strong> tokens=['1','2','N','N','3','4','N','N','5','N','N']</p><ol><li>idx=0: create node(1). Left=des(idx=1), Right=des(after left)</li><li>idx=1: create node(2). Left=des(idx=2)='N'→null, Right=des(idx=3)='N'→null. Return node(2).</li><li>idx=4: create node(3)... etc.</li></ol>",
    interviewTips: "<ul><li>Mention that any consistent encoding scheme is valid — you're not restricted to a specific format.</li><li>The class-level <code>idx</code> variable in deserialization acts as a shared pointer across all recursive calls.</li><li>BFS-based serialization is also valid — describe both and explain why you chose preorder.</li><li>This problem is highly valued in system design contexts: similar patterns appear in distributed systems for data serialization.</li></ul>",
    importantPoints: "<ul><li>Preorder traversal (root, left, right) naturally maps to the recursive structure of deserialization.</li><li>The <code>idx</code> pointer must be shared across all recursive des() calls — use a class field or a wrapper object.</li><li>Split on comma: be careful with <code>String.split(\",\")</code> — it works for this format.</li><li>Use 'N' (or '#' or 'null') consistently as the null marker throughout serialize and deserialize.</li></ul>"
  }
];
